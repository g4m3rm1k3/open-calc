export default {
  id: "ch2-019",
  slug: "mixed-kinematics-problems",
  chapter: 'p2',
  order: 19,
  title: "Mixed Kinematics Problems",
  subtitle:
    "Combine graph-reading, equation selection, and sign discipline in one workflow.",
  tags: ["mixed practice", "kinematics", "problem solving"],
  aliases: "mixed suvat problems kinematics review",
  hook: {
    question:
      "How do you decide quickly whether to start from a graph, equation, or known-value table?",
    realWorldContext:
      "Real tasks rarely announce a method; they require method selection under uncertainty.",
    previewVisualizationId: 'SVGDiagram',
  },
  intuition: {
    prose: [
      "Translate the statement into knowns/unknowns first.",
      "If data is graphical, extract slope/area quantities before algebra.",
    ],
    visualizations: [
      {
        id: 'SVGDiagram',
        props: { type: 'suvat-map' },
        title: 'Your decision tree starts here',
        caption: 'Mixed problems have many paths. The SUVAT map shows all valid paths given your knowns. Start by listing what you know and don\'t know — the equation that links exactly those variables is your first move.',
      },
      {
        id: 'SVGDiagram',
        title: "Mixed-method selector quiz",
        mathBridge:
          "Practice deciding the best first move: derivative, integral, or SUVAT substitution.",
        caption: "Decision quality drives solution speed.",
      },
      {
        id: 'SVGDiagram',
        title: "Knowns-to-unknowns solver",
        mathBridge:
          "After selecting method, execute with consistent variables and signs.",
        caption: "Convert strategy to answer.",
      },
      {
        id: 'VerticalThrow',
        props: {},
        title: "Projectile case solver",
        mathBridge:
          "Blend kinematics method selection with horizontal, level-ground, and elevated projectile solve modes.",
        caption: "A unified solver workflow across motion families.",
      },
      {
        id: 'SVGDiagram',
        title: "Range step-through",
        mathBridge:
          "Walk through decomposition, flight-time, and range formulas in an explicit multi-step chain.",
        caption: "Method transparency reduces setup errors.",
      },
    ],
  },
  math: {
    prose: [
      "Use this hierarchy: conventions → knowns → equation/domain check → solve → sanity check.",
    ],
    visualizations: [
      {
        id: 'SVGDiagram',
        title: "Formula fit and recall",
        mathBridge:
          "Instantly verify which formulas are admissible for a given variable set.",
        caption: "A robust anti-error checklist.",
      },
    ],
  },
  rigor: {
    prose: [
      "All constant-acceleration mixed problems reduce to a small, closed equation set plus calculus links.",
    ],
    visualizationId: 'SVGDiagram',
    visualizationProps: { type: 'suvat-map' },
    proofSteps: [
      { expression: "v=v_0+at", annotation: "Rate update backbone." },
      {
        expression: "\\Delta x=v_0t+\\frac12at^2",
        annotation: "Position update backbone.",
      },
      {
        expression: "\\Delta x=\\int v\\,dt",
        annotation: "Graph/integration route for variable representations.",
      },
    ],
    title: "Unified solving backbone",
  },
  examples: [
    {
      id: "ch2-019-ex1",
      title: "Graph + equation hybrid",
      problem: "From v–t graph, find Δx on [0,4], then use v=v0+at to find a.",
      steps: [
        {
          expression: "\\Delta x=\\text{area under }v\\text{-}t",
          annotation: "Extract displacement first.",
        },
        { expression: "a=(v-v_0)/t", annotation: "Then infer acceleration." },
      ],
      conclusion: "A mixed route can be shortest and safest.",
    },
  ],
  challenges: [
    {
      id: "ch2-019-ch1",
      difficulty: "hard",
      problem: "Name one diagnostic that tells you SUVAT is invalid.",
      hint: "Check acceleration assumptions.",
      answer: "Acceleration is not constant over the interval.",
    },
  ],

  // ── Python Lab ────────────────────────────────────────────────────────────
  python: {
    title: `Python Lab — Mixed Kinematics Problems`,
    description: `Practice the full solve cycle: extract info from graphs, apply SUVAT, verify with calculus. Three worked examples using graph + equation hybrid approaches.`,
    placement: 'after-examples',
    visualizations: [
      {
        id: 'PythonNotebook',
        title: 'Mixed kinematics — graph and equation hybrid',
        props: {
          initialCells: [
            {
              id: 'cell-01',
              type: 'code',
              cellTitle: 'Extract acceleration from a v–t graph',
              prose: `A v–t dataset is given. Compute acceleration from the slope, then use SUVAT to find displacement over the interval.`,
              code: [
                `import numpy as np`,
                ``,
                `# v–t data points  (t in s, v in m/s)`,
                `t_data = np.array([0, 1, 2, 3, 4])`,
                `v_data = np.array([2, 5, 8, 11, 14])`,
                ``,
                `# Fit a line: v = v0 + a*t`,
                `a_fit, v0_fit = np.polyfit(t_data, v_data, 1)`,
                ``,
                `print(f"From graph fit:")`,
                `print(f"  v₀ = {v0_fit:.3f} m/s")`,
                `print(f"  a  = {a_fit:.3f} m/s²")`,
                ``,
                `# Displacement = area under v–t = trapezoid`,
                `# Method 1: geometric (trapezoid rule)`,
                `t0, tf = t_data[0], t_data[-1]`,
                `v0_actual = v_data[0]`,
                `vf_actual = v_data[-1]`,
                `dt = tf - t0`,
                `dx_trap = 0.5 * (v0_actual + vf_actual) * dt`,
                ``,
                `# Method 2: SUVAT with fitted a`,
                `dx_suvat = v0_fit*dt + 0.5*a_fit*dt**2`,
                ``,
                `print(f"\\nDisplacement over {dt} s:")`,
                `print(f"  Trapezoid area : {dx_trap:.3f} m")`,
                `print(f"  SUVAT formula  : {dx_suvat:.3f} m")`,
                `print(f"  Agreement: {np.isclose(dx_trap, dx_suvat, atol=0.1)}")`,
              ].join('\n'),
              output: '',
              status: 'idle',
              figureJson: null,
            },
            {
              id: 'cell-02',
              type: 'code',
              cellTitle: 'Mixed: free-fall + constant-velocity phases',
              prose: `A ball is thrown upward at 14 m/s. A platform is 5 m above and 8 m wide (horizontal range). Using the vertical equations find the time windows when the ball is above 5 m. This mixes SUVAT with conditional logic.`,
              code: [
                `import numpy as np`,
                ``,
                `g  = 9.8`,
                `v0 = 14.0   # m/s upward`,
                `h_platform = 5.0   # m`,
                ``,
                `# y(t) = v0*t - 0.5*g*t² = h_platform  →  0.5*g*t² - v0*t + h = 0`,
                `coeffs = [0.5*g, -v0, h_platform]`,
                `roots  = np.roots(coeffs)`,
                `t_enter, t_exit = sorted(roots)`,
                ``,
                `y_max = v0**2 / (2*g)`,
                ``,
                `print(f"Ball above {h_platform} m from t = {t_enter:.3f} s to t = {t_exit:.3f} s")`,
                `print(f"Time above platform : {t_exit - t_enter:.3f} s")`,
                `print(f"Max height          : {y_max:.3f} m")`,
                ``,
                `# Velocities at entry and exit`,
                `v_enter = v0 - g*t_enter`,
                `v_exit  = v0 - g*t_exit`,
                `print(f"v at entry  : {v_enter:.3f} m/s  (upward ✓)")`,
                `print(f"v at exit   : {v_exit:.3f} m/s  (downward ✓)")`,
                `print(f"|v_exit| == |v_enter|: {np.isclose(abs(v_enter), abs(v_exit))}")`,
              ].join('\n'),
              output: '',
              status: 'idle',
              figureJson: null,
            },
            {
              id: 'cell-03',
              type: 'code',
              cellTitle: 'Hybrid solve — extract a from Δx and Δv',
              prose: `A car's velocity increases from 5 m/s to 20 m/s over a displacement of 112.5 m. Find the acceleration and the time, using two different SUVAT equations. Cross-check both.`,
              code: [
                `import numpy as np`,
                ``,
                `v0 = 5.0    # m/s`,
                `v  = 20.0   # m/s`,
                `dx = 112.5  # m`,
                ``,
                `# Method 1: v² = v0² + 2a*dx  →  a = (v²-v0²)/(2dx)`,
                `a_m1 = (v**2 - v0**2) / (2*dx)`,
                ``,
                `# From a, find time: v = v0 + a*t  →  t = (v-v0)/a`,
                `t_m1 = (v - v0) / a_m1`,
                ``,
                `# Method 2: Δx = ½(v0+v)*t  →  t = 2dx/(v0+v)`,
                `t_m2 = 2*dx / (v0 + v)`,
                `a_m2 = (v - v0) / t_m2`,
                ``,
                `print(f"Method 1 (v² equation): a = {a_m1:.3f} m/s²   t = {t_m1:.3f} s")`,
                `print(f"Method 2 (avg velocity): a = {a_m2:.3f} m/s²   t = {t_m2:.3f} s")`,
                `print(f"Agreement: {np.isclose(a_m1, a_m2) and np.isclose(t_m1, t_m2)}")`,
              ].join('\n'),
              output: '',
              status: 'idle',
              figureJson: null,
            },
            {
              id: 'cell-04',
              type: 'code',
              cellTitle: 'Challenge — when is SUVAT invalid?',
              prose: `Generate two scenarios: one with constant acceleration (SUVAT valid) and one with variable acceleration (SUVAT invalid). Show that SUVAT gives incorrect displacement for the variable-a case by comparing it to the exact numerical integral.`,
              code: [
                `import numpy as np`,
                `from scipy import integrate`,
                ``,
                `t = np.linspace(0, 5, 1000)`,
                ``,
                `# Case 1: constant a = 2 m/s², v0 = 0`,
                `a_const = 2.0; v0 = 0.0`,
                `v_const = v0 + a_const*t`,
                `dx_suvat = v0*t[-1] + 0.5*a_const*t[-1]**2`,
                `dx_exact1 = integrate.trapezoid(v_const, t)`,
                `print("Case 1 — constant acceleration:")`,
                `print(f"  SUVAT Δx = {dx_suvat:.4f} m")`,
                `print(f"  Exact Δx = {dx_exact1:.4f} m")`,
                `print(f"  SUVAT valid: {np.isclose(dx_suvat, dx_exact1, rtol=0.001)}")`,
                `print()`,
                ``,
                `# Case 2: variable a(t) = 2*sin(t), v0 = 0`,
                `a_var = 2.0 * np.sin(t)`,
                `v_var = np.cumsum(a_var) * (t[1]-t[0])   # numerical integration`,
                `a_avg = np.mean(a_var)  # SUVAT uses this incorrectly`,
                `dx_suvat_wrong = v0*t[-1] + 0.5*a_avg*t[-1]**2`,
                `dx_exact2 = integrate.trapezoid(v_var, t)`,
                `print("Case 2 — variable acceleration a(t) = 2sin(t):")`,
                `print(f"  SUVAT Δx (using avg a) = {dx_suvat_wrong:.4f} m")`,
                `print(f"  Exact Δx (integral)    = {dx_exact2:.4f} m")`,
                `print(f"  SUVAT valid: {np.isclose(dx_suvat_wrong, dx_exact2, rtol=0.05)}")`,
                `print("  → SUVAT is WRONG here. Use integration instead.")`,
              ].join('\n'),
              output: '',
              status: 'idle',
              figureJson: null,
            },
          ],
        },
      },
    ],
  },

  // ── Quiz ──────────────────────────────────────────────────────────────────
  quiz: [
    {
      id: 'p1-ch2-019-q1',
      question: `What is the first step in a mixed kinematics problem before writing any equation?`,
      options: [
        `Write the most complex equation available`,
        `List all knowns and unknowns with their signs`,
        `Convert all units to CGS`,
        `Draw a free-body diagram`,
      ],
      answer: 1,
      explanation: `Identify what is given (knowns) and what is asked (unknowns), assigning correct signs based on your axis convention. This step dictates which SUVAT equation to use and prevents substitution errors.`,
    },
    {
      id: 'p1-ch2-019-q2',
      question: `On a v–t graph, a velocity dataset shows $v$ rising linearly from 2 m/s to 14 m/s over 4 s. What is the acceleration?`,
      options: [`2 m/s²`, `3 m/s²`, `4 m/s²`, `6 m/s²`],
      answer: 1,
      explanation: `$a = \\Delta v / \\Delta t = (14 - 2) / 4 = 12 / 4 = 3$ m/s². The slope of a v–t graph gives acceleration.`,
    },
    {
      id: 'p1-ch2-019-q3',
      question: `For the same graph (v from 2 to 14 m/s over 4 s), what is the displacement?`,
      options: [`24 m`, `32 m`, `48 m`, `16 m`],
      answer: 1,
      explanation: `$\\Delta x = \\tfrac{1}{2}(v_0 + v)t = \\tfrac{1}{2}(2 + 14)(4) = \\tfrac{1}{2}(16)(4) = 32$ m. The displacement equals the trapezoid area under the v–t graph.`,
    },
    {
      id: 'p1-ch2-019-q4',
      question: `When is SUVAT guaranteed to give the correct answer?`,
      options: [
        `Always — it covers all motion`,
        `Only when acceleration is constant throughout the interval`,
        `When time and distance are both known`,
        `When the problem involves free fall`,
      ],
      answer: 1,
      explanation: `SUVAT assumes constant acceleration. If $a$ varies (e.g. drag, springs, variable thrust), SUVAT gives incorrect results. You must integrate $a(t)$ to get $v(t)$ and then $x(t)$.`,
    },
    {
      id: 'p1-ch2-019-q5',
      question: `A car accelerates from 5 m/s to 20 m/s over 112.5 m. Which SUVAT equation gives acceleration directly (without finding time first)?`,
      options: [
        `$v = v_0 + at$`,
        `$v^2 = v_0^2 + 2a \\Delta x$`,
        `$\\Delta x = v_0 t + \\tfrac{1}{2}at^2$`,
        `$\\Delta x = \\tfrac{1}{2}(v_0+v)t$`,
      ],
      answer: 1,
      explanation: `$v^2 = v_0^2 + 2a\\Delta x$ contains $v_0$, $v$, and $\\Delta x$ (all known) — solving directly for $a$ without needing $t$: $a = (v^2 - v_0^2) / (2\\Delta x)$.`,
    },
    {
      id: 'p1-ch2-019-q6',
      question: `How do you find displacement from a v–t graph when the velocity is not constant?`,
      options: [
        `Use $v \\times t$`,
        `Calculate the area under the v–t curve (integrate v over time)`,
        `Take the slope of the v–t graph`,
        `Use $v^2 = v_0^2 + 2a\\Delta x$`,
      ],
      answer: 1,
      explanation: `$\\Delta x = \\int v \\, dt$ — displacement is the area under the v–t curve regardless of whether velocity is constant. For straight-line segments, use triangle and trapezoid areas. For curves, integrate numerically.`,
    },
    {
      id: 'p1-ch2-019-q7',
      question: `A ball is thrown upward at 14 m/s. When is the ball above height $h = 5$ m? Which equation determines those times?`,
      options: [
        `$v = 0$ at the apex`,
        `$\\tfrac{1}{2}g t^2 - v_0 t + h = 0$ — a quadratic in $t$`,
        `$v^2 = v_0^2 - 2g h$`,
        `$t = h / v_0$`,
      ],
      answer: 1,
      explanation: `Set $y(t) = h$: $v_0 t - \\tfrac{1}{2}g t^2 = h \\Rightarrow \\tfrac{1}{2}g t^2 - v_0 t + h = 0$. This quadratic has two positive roots: the time going up through $h$ and the time coming back down through $h$.`,
    },
    {
      id: 'p1-ch2-019-q8',
      question: `You have $v_0$, $v$, and $t$ — but NOT $\\Delta x$ or $a$. What should you solve for first?`,
      options: [
        `$\\Delta x = v \\cdot t$`,
        `$a = (v - v_0)/t$ — then find $\\Delta x$ if needed`,
        `$\\Delta x = \\tfrac{1}{2}(v_0 + v)t$ — directly gives displacement`,
        `Must find $\\Delta x$ before finding $a$`,
      ],
      answer: 2,
      explanation: `Equation 2: $\\Delta x = \\tfrac{1}{2}(v_0 + v)t$ uses exactly $v_0$, $v$, and $t$ — no $a$ needed. It directly gives displacement. If $a$ is also needed: $a = (v - v_0)/t$.`,
    },
  ],
};
