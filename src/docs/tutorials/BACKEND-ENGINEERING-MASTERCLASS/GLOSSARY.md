# Glossary

Auto-generated by `build_glossary.py` - do not edit by hand;
re-run the script after any lesson changes instead.

## __name__
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - a variable Python automatically creates inside every

## assert
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - a Python statement that evaluates an expression and,

## Automated test
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - a piece of code that checks another piece of

## Blueprint
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - A Flask class representing a named, separately-defined
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - A Flask class representing a named, separately-defined

## Blueprint.route
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - A method on `Blueprint` that both registers a URL
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - A method registering a URL rule and returning a

## Characterization test
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - an automated test written to pin down and

## create_app
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - The real project's own application factory — an
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - A new application factory — an ordinary Python

## Decorator
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - (`@something`) — Python syntax that wraps a function in

## Deterministic
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - always producing the same output for the same

## Duplication
- **Term** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - the same real knowledge, expressed independently in

## End-to-end test
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - an automated test that exercises the entire real

## Extract Function
- **Term** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - a specific, named refactoring technique: taking

## Flask
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - The class at the center of the framework — an instance

## Flask.register_blueprint
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - An instance method that copies a `Blueprint`'s own
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - An instance method copying a `Blueprint`'s recorded

## Flask.route
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - A method directly on a `Flask` app instance that

## Flask.run
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - An instance method that starts a real, live HTTP

## Flask.test_client
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - `test_client()` builds an object that can simulate
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - `test_client()` builds an in-process HTTP simulator for

## FlaskClient.get
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - `test_client()` builds an object that can simulate
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - A method that simulates a real HTTP GET request
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - `test_client()` builds an in-process HTTP simulator for

## GET
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - the specific HTTP method used in this lesson's code. It

## HTTP method
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - a word at the start of a request (`GET`, `POST`,

## HTTP request
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - a structured message a client (a browser, a test,

## HTTP status code
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - a three-digit number in every HTTP response

## Integration test
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - an automated test that exercises two or more

## Interface
- **Term** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - the part of a route's contract an external caller can

## JSON (JavaScript Object Notation)
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - a plain-text format for

## jsonify
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - A Flask function converting a Python value into a
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - A Flask function that converts a Python value (here, a
- **Object/method** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - A Flask function converting a Python value into a real

## Mock
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - a fake, controlled stand-in for a real dependency (a slow

## Port
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - a number (0-65535) identifying *which* listening socket on

## Process
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - one running instance of a program, with its own private

## Refactor
- **Term** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - restructuring existing code's internal shape *without

## Regression
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - a bug that was, at some point, fixed or absent, and

## response.get_json()
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - Two related members of the same real response object —

## response.status_code
- **Object/method** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - Two related members of the same real response object —

## Route / routing
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - the act of a web framework matching an incoming

## Single source of truth
- **Term** in `LESSON-02-ONE-FUNCTION-TWO-DOORS` - a rule that any one piece of real

## Socket
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - the real, addressable endpoint (an IP address plus a

## subprocess.Popen
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - A class that starts a real, separate operating-system

## The `Mock` object
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - An object standing in for the real function that got

## Unit test
- **Term** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - an automated test that exercises one function or one

## unittest.mock.patch
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - A function (usable as a context manager, via `with`)

## URL path
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - the part of a request naming *which* resource is being

## URL prefix
- **Term** in `LESSON-01-MAPPING-A-URL-TO-A-FUNCTION` - a path segment a framework prepends to every route

## urllib.request.urlopen
- **Object/method** in `LESSON-00-A-CHECK-YOU-CAN-RUN-AGAIN` - A standard-library function that sends a real HTTP

## Possible collisions (shared words, different names)

Not necessarily a problem - review each one. A real violation looks like two different names that could be confused with each other (e.g. "freestanding" vs "free function").

- `Blueprint` / `Blueprint.route` - shares: blueprint
- `Blueprint` / `Flask.register_blueprint` - shares: blueprint
- `Blueprint.route` / `Flask.register_blueprint` - shares: blueprint
