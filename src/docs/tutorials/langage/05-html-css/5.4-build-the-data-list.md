# Lesson 5.4: The Same Shape, Repeated, Is a New Fact

**What you will build:** `code/05-html-css/Data/index.html`'s own
single `Data` record, from Lesson 5.2, becomes three — wrapped in a new
`<section>`, each one identically shaped, separated by real, consistent
spacing rather than sitting flush against its own neighbors. The
transferable problem this lesson is actually about: one `Data` record
needed no real relationship to anything else on the page; three records
of the identical real shape, sitting one after another, immediately
raise two new real questions nothing so far has answered — what groups
them together as "the list," and what visually tells a reader where one
record actually ends and the next one begins? This lesson answers both
with real, new HTML and CSS, and reuses `<h1>`/`<h2>`'s own real
hierarchy from Lessons 5.1 and 5.2 to answer a third question the first
two immediately create: once a *group* of records has its own heading,
what happens to each individual record's own heading level?

**What you need to know first:** Lesson 5.2's own real `<article>`,
still the correct real container for one `Data` record — reused three
times here, unchanged in its own internal shape. Lesson 5.1's own real
heading-level rule (no skipped levels) and Lesson 5.3's own real flex
container mechanism (`display: flex`, `flex-direction: column`), reused
here on a new element to solve a new, real spacing problem.

## Terms used in this lesson

- **Element** — per Lesson 5.1's own Terms entry, restated here in
  full: the basic structural unit of HTML. `<section>`, this lesson's
  own new one, is an element in exactly this sense.
- **Heading element (`<h1>`–`<h6>`)** — per Lesson 5.1's own Terms
  entry, restated here in full because this lesson's own real code
  changes an existing one: six real, numbered levels, each naming one
  real depth in a document's own inferred outline. This lesson's own
  real code both adds a new `<h2>` and moves three already-existing
  `<h2>`s down to `<h3>` — the first time anywhere in this curriculum a
  heading's own level has changed after it was first written.
- **`<article>` element** — per Lesson 5.2's own Terms entry, restated
  here in full: a real element representing a self-contained
  composition, independently distributable or reusable on its own. This
  lesson's own real code uses three, each one shaped identically to the
  single one Lesson 5.2 already built.
- **`<section>` element** — a real element representing a generic,
  standalone section of a document with no more specific element (like
  `<article>`, above) that better fits it. MDN's own documentation,
  fetched and quoted this session, draws the exact real line this
  lesson's own choice depends on: "If you are only using the element as
  a styling wrapper, use a `<div>` instead" — meaning `<section>` is the
  real, correct choice here specifically because this lesson's own new
  wrapper carries real, thematic meaning ("this is the group of `Data`
  entries," not just "a box for CSS to target"), proven directly by the
  same documentation's own further real rule: "Sections should always
  have a heading, with very few exceptions" — this lesson's own real
  code gives it one.
- **Repeated structure** — several real elements sharing the identical
  underlying shape (the same child elements, in the same order),
  differing only in their own real content. This lesson's own three
  `<article>` elements are a real, direct example — proven, later in
  this lesson's own second Concept Unit, to matter for a real reason
  beyond visual tidiness.
- **`gap` property** — a real CSS property setting real, consistent
  space *between* a flex container's own real children, without adding
  any extra space on the outside of the first or last one. MDN's own
  documentation, fetched and quoted this session, states its own real
  behavior for this lesson's own real layout direction directly: "For
  columns... the first value defines the gap between flex items within
  a flex line." It exists so that consistent spacing between repeated
  items — this lesson's own real problem — doesn't have to be faked
  with a margin on every individual item, a real, different, and more
  error-prone approach not used anywhere in this lesson.
- **`border` property** — a real CSS property drawing a real, visible
  line around an element's own edge. It exists to give an element a
  real, visible boundary a reader can actually see, distinct from the
  padding and margin properties already in this project's own real CSS
  since Lesson 5.3, neither of which draws anything visible on its own.
- **`border-radius` property** — a real CSS property rounding an
  element's own real corners by the given real distance, applied here
  to soften `<article>`'s own new visible border rather than leaving it
  sharp-cornered.

## Objects and methods used

None, for the same real reason as every lesson so far in this phase:
this lesson's own entire real project change is markup and CSS, calling
no method and constructing no object.

---

## Concept Unit: `<section>` and a Second Real Level of Heading Change

### The Problem

Lesson 5.2 built one real `<article>`, correctly self-contained on its
own. This lesson's own real project needs two more of the identical
real shape — but nothing about three separate, unrelated `<article>`
elements, sitting one after another directly inside `<main>`, tells a
reader, a screen reader, or a search engine that the three of them
together form one real, coherent group: "the list of `Data` entries," as
opposed to three unrelated `<article>`s that just happen to be near each
other. Given that Lesson 5.1's own real fourth Concept Unit already
solved a structurally similar problem — giving a real name to "the
page's own primary content region" instead of leaving it as an unnamed
part of `<body>` — what would you predict the real, analogous solution
looks like here, one level down?

> **Stop and try it yourself first:** MDN's own real rule, already
> quoted in this lesson's own Terms entry for **`<section>` element**,
> states that a section should almost always have its own heading. Given
> that `<main>` already has one real child heading structure — `<h1>`
> from `<header>`, and (before this unit) `<h2>Morning Run</h2>` from
> Lesson 5.2's own single article — and given Lesson 5.1's own real rule
> against skipping heading levels, what real problem would adding a
> *new* `<h2>` for this unit's own new `<section>` create, if
> `<article>`'s own existing `<h2>` stayed exactly where it was?

### Project Change

- **Reference Source** — no reference counterpart, per Lesson 5.1's own
  opening note, still true for this whole phase.
- **Files affected** — `code/05-html-css/Data/index.html`.
- **Change type** — add (the new `<section>` and its own heading) and
  refactor (each existing `<article>`'s own heading level).
- **Location** — wrapping the single `<article>` Lesson 5.2 already
  placed inside `<main>`.
- **Dependencies** — Lesson 5.3's own completed real project.

### The New Code

```html
<section>
  <h2>Entries</h2>
</section>
```

```html
<h3>Morning Run</h3>
```

### The Updated Project

```html
1   <main>
2     <section>
3       <h2>Entries</h2>
4       <article>
5         <h3>Morning Run</h3>
6         <p>Category: <data value="cardio">Cardio</data></p>
7         <p>Duration: <data value="32">32 minutes</data></p>
8       </article>
9     </section>
10  </main>
```

`<main>`'s own single child is now `<section>` (lines 2–9) rather than
`<article>` directly; `<section>` itself carries the new `<h2>` (line
3), and the article's own former `<h2>` is now `<h3>` (line 5) — one
real level lower than before, per this unit's own Socratic prompt.

### Introduce the Concept in Isolation

This is exactly what the real project's own two changes, just shown
above, are doing — isolated here by real, direct comparison against
Lesson 5.2's own real, previous screenshot, since the fact this unit
needs to prove (a real, visible size difference between the section's
own new heading and the article's own demoted one) is about real,
rendered appearance.

Lesson 5.2's own real screenshot, `<h2>Morning Run</h2>` at that time:

![Lesson 5.2's own real screenshot: "Data" as h1, "Morning Run" as h2, both real, different sizes](../../verification/5.2/one_entry.png)

This lesson's own real, current screenshot, the identical text now one
real level lower:

![This lesson's own real, current project: "Data" (h1), "Entries" (h2), "Morning Run" (h3) — three real, visibly different sizes](../../verification/5.4/three_entries.png)

This is the real, concrete answer to this unit's own Socratic prompt:
leaving `<article>`'s own heading at `<h2>` while giving the new
`<section>` its own separate `<h2>` would have created exactly the real
problem MDN's own already-quoted documentation implicitly warns
against — two real, different, unrelated things ("the whole group" and
"one specific record within it") both claiming the identical heading
level, with nothing in the markup itself distinguishing "this h2 names
a group" from "this h2 names one item in it." Demoting the article's
own heading to `<h3>` — proven, by the real screenshot above, to render
visibly smaller than `<h2>`'s own "Entries" — keeps the real, inferred
outline correctly nested: `<h1>Data>` → `<h2>Entries>` → `<h3>Morning
Run>`, three real, distinct levels for three real, distinct things.

### Discard the Throwaway Example

There is no separate throwaway file to discard here — this unit's own
real evidence was a direct, real comparison between two already-real,
already-committed states of the same project, the same real technique
Lesson 5.2's own second unit already used once.

### Mechanical Walkthrough

- **`<section>`** — per this lesson's own Terms entry: wraps the whole
  real group of `Data` entries, chosen specifically, per MDN's own
  quoted rule, because it carries real thematic meaning rather than
  existing only as a styling wrapper (`<div>`, this lesson's own
  rejected alternative, per that same rule).
- **`<h2>Entries</h2>`** — per this lesson's own Terms entry for
  **Heading element**: names the section's own real group, one real
  level below `<h1>Data</h1>` from Lesson 5.1, matching that lesson's
  own established pattern of placing each new real thing one level
  below whatever it's actually nested inside.
- **`<h3>Morning Run</h3>`** (and, further down, `<h3>Team Meeting
  </h3>`, `<h3>Grocery Shopping</h3>`, all three structurally identical)
  — per this lesson's own Terms entry for **Heading element**: each
  real article's own title, now one real level below `<section>`'s own
  `<h2>`, for the identical real reason `<h2>Morning Run</h2>` sat one
  level below `<h1>Data</h1>` back in Lesson 5.2.

### CS Lens

**Renumbering existing structure to make room for a new, more general
layer above it** is a real, recurring idea beyond HTML's own heading
levels.

```
Also recognized in: a numbered outline in a word processor, where
inserting a new top-level heading among existing ones renumbers
everything below it automatically; a database migration inserting a new
foreign-key column and renumbering or reassigning existing rows to
reference it; a company reorganization inserting a new middle-
management layer, with everyone previously reporting to the top now
reporting one level lower instead
```

### SE Lens

The principle is **structure that reflects real, current scope, not
just historical order** — Lesson 5.2's own `<h2>` was the correct real
choice *at the time*, when one record was this page's own entire real
content below `<h1>`; this lesson's own real refactor is not a
correction of a past mistake, but a real, honest adjustment now that
the page's own real scope has genuinely grown. The real alternative not
chosen: leave `<article>`'s own heading at `<h2>` and simply not give
the new `<section>` a heading of its own at all — real and technically
legal, but a direct violation of MDN's own already-quoted "sections
should always have a heading" rule, and a real, concrete loss for
anyone navigating this page by heading alone (Lesson 5.1's own already-
quoted real fact about screen readers), who would have no way to tell,
from headings alone, that "the group of entries" exists as its own real
concept at all.

### Commands Needed

Same `chrome --headless --disable-gpu --screenshot` command already
established in this phase, run against the real, current project.

### Run It

Verified this session — both real screenshots already shown above,
`verification/5.2/one_entry.png` (Lesson 5.2's own real, prior state)
and `verification/5.4/three_entries.png` (this lesson's own real,
current state).

### Connect the Pieces (this unit → next)

`<section>` now correctly groups this project's own `Data` entries
under a real, properly-nested heading — but it still holds only the one
real article Lesson 5.2 built. The next unit adds two more.

---

## Concept Unit: Repeating the Structure

### The Problem

One `<article>`, fully built by Lesson 5.2 and correctly renumbered by
this lesson's own previous unit, exists inside `<section>`. This
lesson's own real project needs to represent more than one real `Data`
record — a real, growing collection, not a single fixed item. Given
that every real field this project's own single article already has
(a title, a category, a duration) applies equally well to any other
real activity entry, what real, structural fact about `<article>`
itself makes it possible to just repeat it, rather than inventing a
different real shape for every new entry?

> **Stop and try it yourself first:** Lesson 5.2's own real `<article>`
> is a real, complete, self-contained unit — per that lesson's own
> Terms entry, "independently distributable or reusable." Given that
> real, independent reusability is exactly the property a template
> needs in order to be repeated safely, what do you predict happens,
> structurally, if this exact same shape — one `<h3>`, two `<p>`s, each
> wrapping a `<data>` — is copied twice more, with only the real text
> inside it changed each time? Does anything about `<article>`'s own
> real definition (Lesson 5.2) prevent more than one from existing side
> by side in the same real document?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's own
  opening note.
- **Files affected** — `code/05-html-css/Data/index.html`.
- **Change type** — add.
- **Location** — inside `<section>`, as two new siblings after the
  existing `<article>`.
- **Dependencies** — the previous unit.

### The New Code

```html
<article>
  <h3>Team Meeting</h3>
  <p>Category: <data value="work">Work</data></p>
  <p>Duration: <data value="45">45 minutes</data></p>
</article>
<article>
  <h3>Grocery Shopping</h3>
  <p>Category: <data value="errand">Errand</data></p>
  <p>Duration: <data value="20">20 minutes</data></p>
</article>
```

### The Updated Project

```html
1   <main>
2     <section>
3       <h2>Entries</h2>
4       <article>
5         <h3>Morning Run</h3>
6         <p>Category: <data value="cardio">Cardio</data></p>
7         <p>Duration: <data value="32">32 minutes</data></p>
8       </article>
9       <article>
10        <h3>Team Meeting</h3>
11        <p>Category: <data value="work">Work</data></p>
12        <p>Duration: <data value="45">45 minutes</data></p>
13      </article>
14      <article>
15        <h3>Grocery Shopping</h3>
16        <p>Category: <data value="errand">Errand</data></p>
17        <p>Duration: <data value="20">20 minutes</data></p>
18      </article>
19    </section>
20  </main>
```

`<section>` (lines 2–19) now holds three real, sibling `<article>`
elements (lines 4–8, 9–13, 14–18) instead of one, each one structurally
identical to the others, differing only in real content. This is the
complete, real markup change this whole lesson makes to `index.html`.

### Introduce the Concept in Isolation

This is exactly what the real project's own two new siblings, just
shown above, are doing — isolated here by real, direct inspection of
the shape they share with the article the previous unit already
established, rather than a separate throwaway file, since nothing new
about `<article>` itself is being taught here — only the real fact that
nothing prevents, or even discourages, more than one existing side by
side.

The real, committed project's own current `--dump-dom` output, run this
session (`verification/5.4/run_output.txt`), already shows all three,
real and complete, structurally identical apart from their own real
text content — confirming directly that the browser's own parser
accepted three sibling `<article>` elements with no real complaint, the
same way it already accepted three sibling `<data>` elements inside a
single article back in Lesson 5.2.

### Discard the Throwaway Example

There is no separate throwaway file to discard here, for the same real
reason as this unit's own previous step.

### Mechanical Walkthrough

- **`<article>`** (second) — per Lesson 5.2's own Terms entry, restated
  here in full per the Repetition Rule: a second, real, independently
  self-contained composition, structurally identical to the first,
  holding "Team Meeting," a real, different `Data` record.
- **`<h3>Team Meeting</h3>`** — per this lesson's own Terms entry for
  **Heading element**: this second article's own real title, at the
  identical real heading level as the first, per this lesson's own
  first Concept Unit — a real, deliberate consequence of repeated
  structure: every repeated item shares not just its own element shape,
  but its own real position in the document's outline too.
- **`<data value="work">Work</data>`, `<data value="45">45 minutes
  </data>`** — per Lesson 5.2's own Terms entries for **`<data>`
  element** and **`value` attribute**, restated here in full: two more
  real, machine-readable values, structurally identical in shape to
  Lesson 5.2's own first pair, differing only in their own real content.
- **`<article>`** (third), **`<h3>Grocery Shopping</h3>`**, and its own
  two `<data>`-wrapped values — structurally and explanatorily identical
  to the second article just walked through, differing only in real
  content, per the Repetition Rule applied to this lesson's own third
  and final repetition of the identical real shape.

### CS Lens

**The same structural template, instantiated more than once with only
its own real data changing between instances**, is a real, foundational
idea this whole curriculum's own next phase depends on directly.

```
Also recognized in: Phase 6's own real subject, still to come — a
single JavaScript function rendering one `Data` record's own real HTML,
called once per item in a real array, producing exactly this lesson's
own real structure automatically instead of by hand; a database query's
own real result set, one row shaped identically to every other row,
differing only in its own real column values; a class definition
(Lesson 1.2, C#'s own `Service`) instantiated many times, each real
object sharing the identical real shape while holding its own,
different real state
```

### SE Lens

The principle is **the DRY principle — Don't Repeat Yourself** — stated
here about *shape*, not yet about *code*: this lesson's own real markup
still, honestly, repeats the same six lines of HTML three separate
times by hand, which is real, genuine repetition CSS cannot remove and
this phase's own real subject (static HTML/CSS, no programming
language) has no real tool to eliminate. The real, honest tradeoff this
lesson's own static approach makes: writing markup by hand, three real
times, for three real records, is completely manageable — but Phase 6's
own real JavaScript, not yet written, exists specifically because a
`Data` collection with hundreds or thousands of real records, not three,
would make hand-writing every single `<article>` a real, unsustainable
cost this lesson's own approach was never meant to survive past a
small, fixed, illustrative example.

### Commands Needed

Same `chrome --headless --disable-gpu --dump-dom` command already
established in this phase, run against the real, current, complete
project file.

### Run It

Verified this session — the real, complete `--dump-dom` output,
`verification/5.4/run_output.txt`, and the real screenshot already
shown in this lesson's own previous unit,
`verification/5.4/three_entries.png`.

### Connect the Pieces (this unit → next)

Three real, structurally identical `Data` records now exist inside
`<section>` — but, with no CSS added yet for this new structure, nothing
visually separates one from the next beyond whatever line-wrapping
happens to fall between them. The final unit adds real, deliberate
spacing and a real, visible boundary around each one.

---

## Concept Unit: Spacing and Alignment

### The Problem

Three real `<article>` elements now sit directly inside `<section>`,
with no CSS written anywhere for either one yet. A browser's own
default stacking, per every earlier lesson in this phase, places one
block-level element directly after another with no deliberate space
between them — real, but visually indistinguishable from one
continuous block of text, exactly the same real problem this lesson's
own previous unit's real heading-level renumbering solved for *outline*
structure but not yet for *visual* structure. Given Lesson 5.3's own
real, established fact that `display: flex` and `flex-direction:
column` already arrange `<body>`'s own children into a real vertical
stack, what would you predict happens if the identical real pair of
declarations were applied to `<section>` instead?

> **Stop and try it yourself first:** Lesson 5.3's own real screenshot
> already proved `<body>`'s own three children (`<header>`, `<main>`,
> `<footer>`) fill real, available space along their own shared main
> axis, using `flex: 1` on just one of them. This lesson's own three
> `<article>` elements have no real need to *grow* the way `<main>`
> did — each one should stay exactly as tall as its own real content
> needs. Given that, what real CSS property — distinct from `flex: 1` —
> would you predict adds consistent, real space *between* three flex
> items that all keep their own natural size?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's own
  opening note.
- **Files affected** — `code/05-html-css/Data/styles.css`.
- **Change type** — add.
- **Location** — two new rules, after the `main` rule Lesson 5.3 already
  built.
- **Dependencies** — the previous two units.

### The New Code

```css
section {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

article {
  border: 1px solid #ccc;
  border-radius: 4px;
  padding: 1rem;
}
```

### The Updated Project

The complete, real, committed file:

```css
1   body {
2     margin: 0;
3     font-family: sans-serif;
4     display: flex;
5     flex-direction: column;
6     min-height: 100vh;
7   }
8
9   main {
10    flex: 1;
11    padding: 1rem;
12  }
13
14  section {
15    display: flex;
16    flex-direction: column;
17    gap: 1rem;
18  }
19
20  article {
21    border: 1px solid #ccc;
22    border-radius: 4px;
23    padding: 1rem;
24  }
```

Two real, new rules (lines 14–18 and 20–24) now exist, targeting
`section` and `article` respectively. This is the complete, real,
committed state of `code/05-html-css/Data/styles.css` at the end of
this lesson.

### Introduce the Concept in Isolation

This is exactly what the real project's own two new rules, just shown
above, are doing — isolated here by real, direct comparison against a
throwaway lab with the identical real markup but neither rule applied.

With this unit's own real CSS (the real, committed project):

![Three real, bordered, rounded-corner cards, each clearly separated from the next by real, visible space](../../verification/5.4/three_entries.png)

Without it (`verification/5.4/lab_no_spacing.html`, a real, throwaway
lab, identical markup, only Lesson 5.3's own `body`/`main` rules
present):

![The same three real records with no border and no gap between them — visually, one continuous, hard-to-scan block of text](../../verification/5.4/no_spacing.png)

This is the real, concrete answer to this unit's own Socratic prompt:
`gap`, not `flex: 1`, is the real property that separates repeated
items without making any of them grow — the real screenshot above
proves all three articles stay their own natural height, with real,
consistent space between them rather than any one of them expanding.
The real, visible border and rounded corner each article now has
further reinforces, visually, the exact real grouping `<article>`
already declared structurally back in this lesson's own second unit —
a real, deliberate case of CSS *confirming*, on screen, a distinction
the markup had already made real underneath.

### Discard the Throwaway Example

`lab_no_spacing.html` is discarded now; the real, committed project
keeps both new rules.

### Mechanical Walkthrough

- **`section`** — this rule's own real selector, per Lesson 5.3's own
  Terms entry for **Selector**, restated here in full: matches the one
  real `<section>` element this lesson's own first Concept Unit already
  created.
- **`display: flex;`, `flex-direction: column;`** — per Lesson 5.3's own
  Terms entries for **`display` property** and **Main axis /
  `flex-direction` property**, restated here in full because this
  lesson's own real code applies both to a second real container: turns
  `<section>` into a second real flex container, its own three
  `<article>` children becoming real flex items, arranged vertically —
  structurally the identical real mechanism Lesson 5.3 already used for
  `<body>`, applied one level deeper.
- **`gap: 1rem;`** — per this lesson's own Terms entry for **`gap`
  property**: adds one real, consistent unit of space between each of
  `<section>`'s own three real flex items, without adding any extra
  space above the first article or below the last one — the real,
  concrete difference the two screenshots above already proved.
- **`article`** — this second rule's own real selector: matches all
  three real `<article>` elements at once, applying the identical real
  declarations to each.
- **`border: 1px solid #ccc;`** — per this lesson's own Terms entry for
  **`border` property**: draws a real, thin, light-gray line around
  each article's own real edge, the real, visible boundary the "without"
  screenshot above proved is genuinely absent otherwise.
- **`border-radius: 4px;`** — per this lesson's own Terms entry: rounds
  each article's own four real corners by four real pixels, a real,
  small, deliberate softening of the border just added, rather than a
  sharp, right-angled box.
- **`padding: 1rem;`** — per Lesson 5.3's own Terms entry (restated
  here in full): real, visible space *inside* each article's own new
  border, between the border itself and the heading/paragraphs inside
  it — without it, this unit's own new border would sit flush against
  "Morning Run" and the two paragraphs beneath it, cramped rather than
  legible.

### CS Lens

Not applicable as its own separate "recognized elsewhere" list here —
`gap`'s own real idea (consistent spacing between repeated items,
without repeating a margin declaration on every individual item) is a
specific instance of the identical resource-allocation idea Lesson
5.3's own final Concept Unit already gave a full, real "recognized
elsewhere" list for (`flex-grow`); repeating that same list here for a
closely related property would violate this schema's own Stopping Rule
against decomposing an already-taught idea further than the point
where it stops teaching something new.

### SE Lens

The principle is **spacing as a property of the container, not of each
individual item** — `gap`, set once on `<section>`, replaces what would
otherwise be a real `margin` declaration repeated on `article` itself
(a `margin-bottom`, for a real example, applied to every article
including the last one, whose own trailing margin would then need a
real, separate override to avoid unwanted space below it). The real
alternative not chosen — margins on the repeated items instead of `gap`
on their shared container — is a real, historically common CSS pattern,
still used where `gap` isn't available (older layout systems, before
flexbox's own real support for it), but carries a real, well-known cost
this lesson's own choice avoids entirely: an item's own margin doesn't
know whether it's first, last, or in the middle of a real group, so
getting spacing exactly even at both ends without any exception
logic requires real, extra CSS `gap` was specifically designed to make
unnecessary.

### Commands Needed

Same `chrome --headless --disable-gpu --screenshot` command already
established in this phase, run against both the real, committed project
and the real, throwaway no-spacing lab.

### Run It

Verified this session — both real screenshots already shown above,
`verification/5.4/three_entries.png` and
`verification/5.4/no_spacing.png`.

### Connect the Pieces (this unit → next)

`code/05-html-css/Data/styles.css` now has four real, complete rules;
`index.html` holds three real, clearly separated `Data` records, each
correctly nested one heading level below the group that contains them.

---

## Connect the Pieces

Tracing this lesson's own real project change, start to finish: one
real `<article>` from Lesson 5.2 first gained a real, named group around
it — `<section>`, chosen specifically, per MDN's own quoted rule,
because it carries thematic meaning a `<div>` never could — which
required demoting that article's own heading from `<h2>` to `<h3>`,
proven by real, direct screenshot comparison to keep this project's own
real outline correctly nested rather than colliding two unrelated
things at the same level. Two more real, structurally identical
articles then joined the first, proving directly, by real
`--dump-dom` inspection, that nothing about `<article>`'s own real
definition (Lesson 5.2) prevents more than one existing side by side —
the same real, repeatable shape this whole curriculum's own next phase
depends on for genuinely dynamic rendering. Finally, `display: flex` and
`flex-direction: column`, the identical real mechanism Lesson 5.3
already used for `<body>`, turned `<section>` into a second real flex
container, `gap: 1rem` spacing its own three real children evenly
without making any of them grow, while a new, real border and rounded
corner on `article` itself gave each one a real, visible boundary —
proven, by direct, real screenshot contrast, to turn three previously
run-together records into three clearly separate ones.

**Next lesson (5.5):** one of these three real records needs a second,
different real visual representation — a detail view, distinct from the
compact form each one already has here, built by reusing this lesson's
own real `<article>` shape rather than inventing a new one from scratch.
