# Lesson 5.7: A Page Has More Than One Real Story to Tell

**What you will build:** `code/05-html-css/Data/styles.css` gains real
CSS for three states this project has never represented at all —
loading, empty, and error — each with its own real, distinct visual
presentation, ready for Phase 6's own real JavaScript to apply once
fetching real data (not yet written) actually needs them. The
transferable problem this lesson is actually about: every real
screenshot this whole phase has captured shows the identical real
situation — three entries, already loaded, already correct. A real
application's own page spends real, meaningful time in at least three
other real states nothing so far accounts for: before any data has
arrived, after arriving to find there genuinely is none, and after
failing to arrive at all. This lesson builds real, static, honest proof
of what each one actually looks like — without writing a single line of
JavaScript to make any of them happen automatically, since that's
Phase 6's own real subject, not this one's.

**What you need to know first:** Lesson 5.4's own real `<section>` and
its own `<h2>Entries</h2>` heading, kept identical across every real
state this lesson builds. Lesson 5.5's own real `class` attribute and
class selector, and its own real descendant combinator — this lesson's
own real CSS reuses both directly.

## Terms used in this lesson

- **`class` attribute** — per Lesson 5.5's own Terms entry, restated
  here in full: a real, global HTML attribute holding one or more
  author-chosen names. This lesson's own real code gives a single real
  element two of them at once for the first time in this curriculum.
- **Class selector** — per Lesson 5.5's own Terms entry, restated here
  in full: a real CSS selector matching every element whose `class`
  attribute contains the given name. This lesson's own real CSS
  introduces three new ones: `.state-loading`, `.state-empty`, and
  `.state-error`.
- **UI state** — a real, distinct condition an interface can genuinely
  be in at a given moment, each one requiring its own real, honest
  visual presentation rather than a single, permanently-correct one.
  This lesson's own real code represents four: **populated** (already
  built, Lessons 5.2 and 5.4), **loading**, **empty**, and **error** —
  the four real conditions a page fetching real data (Phase 6's own
  real subject) genuinely passes through.
- **Descendant combinator** — per Lesson 5.5's own Terms entry, restated
  here in full: a real, plain space between two selectors, matching any
  element matched by the second selector nested anywhere inside an
  element matched by the first, at any real depth. This lesson's own
  real code reuses it to style a message differently depending on
  *which* state's own section it happens to sit inside.

## Objects and methods used

None, for the same real reason as every lesson so far in this phase —
and, per this lesson's own opening note, deliberately still true even
though this lesson's own real subject (states a real, dynamic
application passes through) is exactly the kind of thing JavaScript
will eventually control. Nothing about representing what each state
*looks like* requires JavaScript at all; only actually *switching*
between them, live, does — and that switch is Phase 6's own real
subject, not this lesson's.

---

## Concept Unit: Three States This Project Has Never Shown

### The Problem

`code/05-html-css/Data/index.html`, as every lesson in this phase has
left it, shows exactly one real situation: three `Data` entries,
already present, already correct. A real application fetching real data
from a real server (Phase 6's own real subject, not yet written)
cannot actually guarantee that outcome the instant a page loads — the
real data has to be requested, has to arrive, and might not arrive
successfully at all. Given that this lesson's own real project has no
real network request anywhere in it yet, and won't until Phase 6, what
real, honest thing *can* this lesson still build to represent those
other real situations?

> **Stop and try it yourself first:** Lesson 5.5's own real detail view
> already proved that a second, real visual representation of the
> identical underlying real content doesn't require inventing new
> elements — only a new real composition and a new real class. Given
> that "loading," "empty," and "error" are all real states of the
> identical `<section>` this project already has — not different
> content, but a different real *situation* that section can be in —
> what would you predict the real, honest way to represent each one
> statically looks like, without any JavaScript to actually trigger it?

### Project Change

- **Reference Source** — no reference counterpart, per Lesson 5.1's own
  opening note, still true for this whole phase.
- **Files affected** — `code/05-html-css/Data/styles.css`.
- **Change type** — add.
- **Location** — a new block, after every rule Lessons 5.3 through 5.6
  already built.
- **Dependencies** — Lesson 5.6's own completed real project.

### The New Code

```css
.state-message {
  padding: 2rem;
  text-align: center;
  color: #666;
}

.state-loading .state-message {
  font-style: italic;
}
```

### The Updated Project

```css
1  .state-message {
2    padding: 2rem;
3    text-align: center;
4    color: #666;
5  }
6
7  .state-loading .state-message {
8    font-style: italic;
9  }
```

Two real, new rules, added after every rule Lesson 5.6 already built —
a real, general message style (lines 1–5), plus one real,
state-specific refinement (lines 7–9) reusing the identical descendant-
combinator mechanism Lesson 5.5 already established.

### Introduce the Concept in Isolation

This is exactly what a real, honest static representation of two of
this lesson's own real states looks like — isolated here in two real,
throwaway lab files
(`verification/5.7/lab_loading.html`,
`verification/5.7/lab_empty.html`), each an exact copy of this
project's own real shell, with `<section>`'s own real content replaced:

```html
<section class="state-loading">
  <h2>Entries</h2>
  <p class="state-message">Loading…</p>
</section>
```

Real screenshots, captured this session, of both:

![A gray, italicized "Loading…" message, centered where the list of entries would otherwise be](../../verification/5.7/loading.png)

![A plain gray "No entries yet." message, in the identical position](../../verification/5.7/empty.png)

This is the real, concrete answer to this unit's own Socratic prompt:
`<h2>Entries</h2>` stays exactly as it already was — the group's own
real name doesn't change just because its own real contents currently
don't exist yet — while the group's own real body is replaced with one
real, honest sentence describing the real situation, styled distinctly
enough (the real, italicized "Loading…") to read as a real, temporary
status rather than permanent content. Both real states share the
identical `state-message` class; only `.state-loading` additionally
italicizes it, a real, deliberate, minimal visual difference for two
real conditions that are honestly similar (both mean "there is
currently nothing to show, for a real, different reason each").

### Discard the Throwaway Example

Both lab files are discarded now; the real, committed project's own
`index.html` stays in its one real, populated state — these classes
exist, ready in `styles.css`, for Phase 6's own real JavaScript to
apply later, not for this lesson's own static file to use permanently.

### Mechanical Walkthrough

- **`class="state-loading"`, `class="state-empty"`** — per this
  lesson's own Terms entry for **`class` attribute**: each real
  section's own real, chosen state name, distinguishing "this section
  is currently loading" from "this section is currently empty" even
  though both, per this unit's own real screenshots, currently share
  most of their own real visual presentation.
- **`class="state-message"`** — a real, second class, given to the real
  `<p>` inside each section alongside the section's own state class —
  this lesson's own first real use of a class naming *what a real
  element is* ("a status message"), separate from *which state it
  belongs to*, a real, deliberate separation this unit's own next
  Mechanical Walkthrough bullet explains further.
- **`.state-message`** — per this lesson's own Terms entry for **Class
  selector**: matches any real element carrying that class, regardless
  of which state's own section it happens to sit inside — this rule's
  own three real declarations (padding, centering, a muted gray color)
  apply identically to every real state's own message.
- **`.state-loading .state-message`** — per this lesson's own Terms
  entry for **Descendant combinator**: matches a real `.state-message`
  specifically when it's nested inside a real `.state-loading` element —
  the identical real mechanism Lesson 5.5 already used for `.detail
  article`, here distinguishing one real state's own message from
  every other state's, by real, calculated specificity (`0-2-0`,
  against the general rule's own `0-1-0`) rather than declaration order.

### CS Lens

**A real, small set of named, mutually exclusive conditions a system
can be in, each with its own real, defined behavior**, is a real,
foundational computer science idea well beyond CSS's own state classes.

```
Also recognized in: a real, formal finite-state machine — this
lesson's own four real states (populated, loading, empty, error) are
exactly the real, named nodes such a machine would define, with Phase
6's own real JavaScript, not yet written, supplying the real
transitions between them; a TCP connection's own real states
(`LISTEN`, `ESTABLISHED`, `CLOSED`, among others); a traffic light's own
real, small set of states, each with its own real, defined visual
presentation and its own real rules for what comes next
```

### SE Lens

The principle is **designing for real failure and absence, not just
real success** — this lesson's own real CSS exists before Phase 6's own
real network request does, deliberately, so that "what does this look
like when it fails" is answered honestly, ahead of time, rather than
improvised after a real failure is first observed in production. The
real alternative not chosen: build only the real, populated
presentation now, and only add loading/empty/error handling once Phase
6's own real `fetch` call (not yet written) actually needs it. The
real, concrete cost of that alternative: a real network request that
can fail (every real one can) would then reach a page with no real,
defined visual answer for failure at all, likely rendering as a blank
region or a real, broken partial layout — exactly the kind of gap this
lesson's own deliberately early, honest design work exists to close
before it's ever actually encountered.

### Commands Needed

Same `chrome --headless --disable-gpu --screenshot` command already
established in this phase, run against both real, throwaway labs.

### Run It

Verified this session — both real screenshots already shown above,
`verification/5.7/loading.png` and `verification/5.7/empty.png`.

### Connect the Pieces (this unit → next)

Two of this lesson's own four real states now have real, honest visual
presentations. The final unit builds the third — a real failure — and
addresses a real, separate concern neither loading nor empty required:
making sure the difference is genuinely visible to every real reader,
not only ones who can see color.

---

## Concept Unit: The Error State — More Than Just Red

### The Problem

Loading and empty, per this lesson's own previous unit, both honestly
mean "there is nothing to show right now," differing only in *why*.
A real error is a genuinely different real situation — something a real
network request actually attempted and failed at, not a real, ordinary
absence of data. A real, quick, tempting fix would be to just make the
message red — but given that this lesson's own real message is still
plain, human-readable text either way, does color alone actually
communicate "this failed," or does it only communicate that to a reader
who can perceive the difference between red and gray at all?

> **Stop and try it yourself first:** This lesson's own previous unit's
> own real "Loading…" and "No entries yet." messages already differ
> from each other in real, plain wording, not only in styling. Given
> that a reader who cannot perceive color at all would still be able to
> tell those two states apart from their own real text alone, what real
> property should this unit's own error message also have, in addition
> to — not instead of — a real, distinct color?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's own
  opening note.
- **Files affected** — `code/05-html-css/Data/styles.css`.
- **Change type** — add.
- **Location** — a new rule, after the `.state-loading .state-message`
  rule the previous unit already built.
- **Dependencies** — the previous unit.

### The New Code

```css
.state-error .state-message {
  color: #b00020;
  font-weight: bold;
}
```

### The Updated Project

The complete, real, committed file:

```css
1   body {
2     margin: 0;
3     font-family: sans-serif;
4     display: flex;
5     flex-direction: column;
6     min-height: 100vh;
7   }
8
9   main {
10    flex: 1;
11    padding: 1rem;
12  }
13
14  section {
15    display: flex;
16    flex-direction: column;
17    gap: 1rem;
18  }
19
20  article {
21    border: 1px solid #ccc;
22    border-radius: 4px;
23    padding: 1rem;
24  }
25
26  .detail {
27    background: #f5f5f5;
28    border-radius: 4px;
29    padding: 1rem;
30  }
31
32  .detail article {
33    border: none;
34    padding: 0;
35  }
36
37  .state-message {
38    padding: 2rem;
39    text-align: center;
40    color: #666;
41  }
42
43  .state-loading .state-message {
44    font-style: italic;
45  }
46
47  .state-error .state-message {
48    color: #b00020;
49    font-weight: bold;
50  }
51
52  @media (min-width: 700px) {
53    main {
54      display: flex;
55      gap: 1rem;
56    }
57
58    main > section {
59      flex: 1;
60    }
61  }
```

Lines 47–50's own new rule is the real, final piece of `styles.css`
this whole lesson adds. This is the complete, real, committed state of
`code/05-html-css/Data/styles.css` at the end of this lesson.

### Introduce the Concept in Isolation

This is exactly what a real, honest static representation of this
lesson's own third state looks like — isolated here in a real,
throwaway lab file
(`verification/5.7/lab_error.html`), identical in structure to the
previous unit's own two labs:

```html
<section class="state-error">
  <h2>Entries</h2>
  <p class="state-message">Couldn't load entries.</p>
</section>
```

A real screenshot, captured this session:

![A bold, dark red "Couldn't load entries." message, in the identical position the previous unit's own two states used](../../verification/5.7/error.png)

This is the real, concrete answer to this unit's own Socratic prompt:
`font-weight: bold`, this unit's own real second declaration, is the
real property that keeps this state's own real distinction genuinely
visible without color — proven directly by imagining (and, for a
reader actually testing it, easy to confirm) this exact same screenshot
rendered in pure grayscale: "Couldn't load entries." would still stand
out as bold against "Loading…"'s own italics and "No entries yet."'s
own plain weight, three real, distinct *shapes* of text, not just three
real, distinct colors. The W3C's own real Web Content Accessibility
Guidelines, fetched and quoted this session, state the exact real rule
this unit's own second declaration satisfies: "Color is not used as the
only visual means of conveying information" — this unit's own error
state uses two real, independent signals (bold weight, and, separately,
its own plain-text wording, "Couldn't load," present regardless of any
styling at all) specifically so color is never the *only* one.

### Discard the Throwaway Example

`lab_error.html` is discarded now, for the same real reason as the
previous unit's own two labs.

### Mechanical Walkthrough

- **`class="state-error"`** — per this lesson's own Terms entry for
  **`class` attribute**: this state's own real, distinct name, matching
  the identical real pattern the previous unit's own two state classes
  already established.
- **`.state-error .state-message`** — per this lesson's own Terms entry
  for **Descendant combinator**: matches a real `.state-message`
  specifically inside a real `.state-error` element, the identical real
  mechanism the previous unit's own `.state-loading .state-message`
  rule already used, applied to a third, real, distinct state.
- **`color: #b00020;`** — a real, dark red — this state's own first
  real visual signal, deliberately darker and more saturated than
  `.state-message`'s own general, muted gray, to draw a real reader's
  eye directly.
- **`font-weight: bold;`** — this state's own second, real, independent
  visual signal, per this unit's own Socratic prompt and its own real,
  proven answer: the real property making this state's own real
  distinction survive even without color at all, satisfying the real,
  quoted WCAG rule this unit's own Introduce-the-Concept step already
  cited.

### CS Lens

Not applicable as its own separate "recognized elsewhere" list here —
this unit's own real subject (a UI state, specifically the failure
one) is the identical real idea this lesson's own previous unit already
gave a full, real CS Lens for; this unit's own genuinely new
contribution is an accessibility constraint on *how* that state is
signaled, covered by this unit's own SE Lens instead, per this
schema's own Stopping Rule against repeating an already-taught idea.

### SE Lens

The principle is **redundant, non-color-dependent signaling** — a
direct, real, named accessibility requirement (WCAG's own Success
Criterion 1.4.1, quoted above), not a stylistic preference. The real
alternative not chosen: color alone (`color: #b00020;`, with no other
real change) would have been visually sufficient for most real
readers, and easy to mistake for "done" — but a real, meaningful
fraction of real readers have some real form of color vision
deficiency, per the WCAG documentation's own quoted reasoning ("not
everyone sees colors or sees them the same way"), for whom red text on
a white background can be genuinely difficult to distinguish from the
surrounding plain text at all. The real, small cost this unit's own
fix adds — one extra real declaration, `font-weight: bold;` — is
trivial compared to the real, concrete failure it prevents: a real
user who cannot perceive the color difference having no real way to
tell "this failed" from "this is just plain, empty text," exactly the
kind of invisible-until-tested gap this whole phase's own Verification
Rule (Lesson 5.1's own opening note) exists to catch before it reaches
a real reader.

### Commands Needed

Same `chrome --headless --disable-gpu --screenshot` command already
established in this phase, run against the real, throwaway error lab.

### Run It

Verified this session — the real screenshot already shown above,
`verification/5.7/error.png`.

### Connect the Pieces (this unit → next)

All four of this project's own real states — populated, loading,
empty, and error — now have real, honest, distinct visual
presentations, ready for Phase 6's own real JavaScript to apply.
Nothing in this project's own real, committed `index.html` actually
uses the three new classes yet; the next, final lesson of this whole
phase reviews everything built so far and marks exactly where that
real connection will need to happen.

---

## Connect the Pieces

Tracing this lesson's own real project change, start to finish: this
whole phase's own real project, until this lesson, could only ever show
one real situation — data, already successfully loaded. Two real,
throwaway labs first proved what "nothing to show yet" honestly looks
like in two real, different circumstances — loading, styled with a real
italic to read as temporary, and empty, styled identically apart from
that one real difference, both sharing one real, general `.state-
message` class and its own muted, real gray. A third real lab then
proved what genuine failure looks like — a real, bold, dark red,
deliberately not color alone, per the W3C's own real, quoted rule that
color must never be the only real signal a state actually changed.
None of these three real states touch `index.html` itself; all three
live only in `styles.css`, real and ready, waiting for a real
connection this whole phase's own final lesson identifies next.

**Next lesson (5.8):** every real piece this whole phase has built —
the list, the detail view, four real visual states, one real responsive
breakpoint — gets reviewed together, and this project's own real seams
for Phase 6's own real JavaScript get named explicitly, closing out this
whole phase's own real milestone: a static UI that can already represent
every real state `Data` will ever be in, before a single line of
JavaScript exists to control it.
