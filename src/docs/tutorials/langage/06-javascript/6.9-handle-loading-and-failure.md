# Lesson 6.9: Three Ways This Page Can Honestly Look

**What you will build:** `code/05-html-css/Data/app.js`'s own
`loadEntries()` finally puts Lesson 5.7's own real `.state-loading`,
`.state-empty`, and `.state-error` CSS classes to work — real, unused
CSS since the session that wrote it, five lessons ago, specifically
built for this exact moment. A real, five-attempt verification, run
this session, actually caught the page mid-flight, still loading. Two
separate real labs proved two genuinely different real failures: a
missing file, which does *not* throw the way it might be assumed to,
and a genuinely unreachable server, which does. The transferable problem
this lesson is actually about: `loadEntries()`, since Lesson 6.8, has
exactly one real story to tell — data arrives, and `renderEntries` runs.
A real fetch can also still be pending, can succeed with genuinely
nothing in it, or can fail outright — three real situations this
project has never actually represented, only prepared for.

**What you need to know first:** Lesson 5.7's own real, four-state UI
design (`populated`, `loading`, `empty`, `error`) and its own real CSS,
committed but never applied by any script until now. Lesson 6.8's own
real `async function loadEntries()`. Lesson 1.10's and Lesson 2.7's own
real `try`/`catch` pattern, from C# and Java — reappearing here in
JavaScript for the first time.

## Terms used in this lesson

- **`try`/`catch` statement** (reappearing, first JavaScript appearance)
  — per MDN's own documentation, fetched and quoted this session, "the
  code in the `try` block is executed first, and if it throws an
  exception, the code in the `catch` block will be executed." The
  identical real shape Lesson 1.10's own C# `try`/`catch` and Lesson
  2.7's own Java `try`/`catch` already used, for the identical real
  reason: separating "the real work that might fail" from "what happens
  if it does."
- **`throw` statement** — a real JavaScript statement that immediately
  stops normal execution and signals a failure, handing a real value
  (conventionally a real `Error` object) to the nearest enclosing
  `catch` block.
- **`Error`** — per MDN's own documentation, fetched and quoted this
  session, a real, built-in object type: "`Error` objects are thrown
  when runtime errors occur," and "can also be used as a base object for
  user-defined exceptions" — this lesson's own real code does exactly
  that, constructing one by hand to represent a real HTTP failure
  `fetch` itself doesn't treat as one.

## Objects and methods used

- **`Response.ok`**
  *What it is:* a real, read-only property on a `Response` object
  stating whether a real HTTP request actually succeeded.
  *Implementation:* per MDN's own documentation, fetched and quoted this
  session, the property "contains a Boolean stating whether the
  response was successful (status in the range 200-299) or not."
  *Its use:* this lesson's own real code checks it immediately after
  `fetch` resolves, since — real, verified this session — a `Response`
  whose own status is `404` still resolves normally; `fetch` itself
  never treats an HTTP-level failure as a reason to reject.
  - *Type:* a read-only instance property, defined on `Response`.
  - *Responsibility:* report, as one plain boolean, whether the real
    HTTP status code this response carries falls in the real, defined
    successful range.
  - *Depends on:* being read on a real `Response` object — here, the one
    `await fetch(...)` already resolved to.
  - *Connects to:* read directly on `response`, Lesson 6.8's own already-
    named constant; its own real value, when `false`, is what this
    lesson's own new `throw` statement, immediately below, actually
    checks for.
  - *Shape:* the real, honest gap this lesson exists to close — proof,
    verified this session, that "the request completed" and "the
    request succeeded" are two genuinely different real facts, and only
    this property distinguishes them.

- **`Element.remove()`**
  *What it is:* a real method that removes the element it's called on
  from wherever it currently sits in the DOM.
  *Implementation:* per MDN's own documentation, fetched and quoted this
  session, the method "removes the element from its parent node," and,
  per that same documentation, "if it has no parent node, calling
  `remove()` does nothing."
  *Its use:* this lesson's own real code calls it on whatever `.state-
  message` paragraph currently exists, clearing a previous state's own
  real message before showing a new one.
  - *Type:* an instance method, defined on `Element`.
  - *Responsibility:* detach the element it's called on from the DOM,
    safely doing nothing if it's already detached or was never
    attached.
  - *Depends on:* being called on a real element.
  - *Connects to:* called here on whatever `entryList.querySelector(".
    state-message")` finds, per Lesson 6.6's own already-explained real
    `querySelector` method, restated here in full: searches only inside
    the element it's called on.
  - *Shape:* the real, simpler cousin of Lesson 6.6's own `.replaceWith`
    — used here because this lesson only ever needs to take something
    *away*, never swap it for something else in the same real step.

---

## Concept Unit: Naming the Wait

### The Problem

`loadEntries()`, since Lesson 6.8, does nothing at all while `await
fetch(...)` is still pending — `#entry-list` simply shows Lesson 6.3's
own now-empty shell (just `<h2>Entries</h2>`) for however long the real
request actually takes. Lesson 5.7 already built a real, complete
visual answer for exactly this situation — `.state-loading` and
`.state-message`, styled, committed, and never once applied by any real
script since. Given `loadEntries`'s own real, current first line
(`showListState`, this lesson's own new function, not yet written), what
would the smallest real change be that shows Lesson 5.7's own real
loading message the instant `loadEntries` starts, before `fetch` has
even been called?

> **Stop and try it yourself first:** Lesson 5.7's own real markup
> shape, from that lesson's own throwaway lab, was `<section class=
> "state-loading"><h2>Entries</h2><p class="state-message">Loading…
> </p></section>`. Given that `#entry-list` already has a real `<h2>`
> Lesson 6.3 never removed, and that this lesson's own real `<article>`
> elements are siblings of that `<h2>`, not children of it, what real,
> two-part change — one to the section's own `class`, one adding a new
> child — would reproduce that exact real shape?

### Project Change

- **Reference Source** — no reference counterpart, per Lesson 5.1's own
  opening note, still true for this whole phase.
- **Files affected** — `code/05-html-css/Data/app.js`.
- **Change type** — add.
- **Location** — two new functions before `loadEntries`, and one new
  line at the start of `loadEntries`'s own body.
- **Dependencies** — Lesson 6.8's completed real project, and Lesson
  5.7's own real, already-committed CSS.

### The New Code

```js
function clearListMessage() {
  const existing = entryList.querySelector(".state-message");
  if (existing) {
    existing.remove();
  }
}

function showListState(state, text) {
  clearListMessage();
  entryList.classList.remove("state-loading", "state-empty", "state-error");
  entryList.classList.add(state);

  const message = document.createElement("p");
  message.classList.add("state-message");
  message.textContent = text;
  entryList.appendChild(message);
}
```

### The Updated Project

`loadEntries`'s own real, updated form:

```js
1  async function loadEntries() {
2    showListState("state-loading", "Loading…");  ← new
3
4    const response = await fetch("data.json");
5    const data = await response.json();
6    renderEntries(data);
7  }
```

Line 2 is this unit's own entire real addition to `loadEntries` itself;
lines 4–7, unchanged since Lesson 6.8, still follow directly after it.
This lesson's own next unit changes the rest of this function's own
body.

### Introduce the Concept in Isolation

This is exactly what this unit's own new functions do, isolated here by
direct inspection of a real, five-attempt verification against the
actual, committed project, run this session and saved in full in
`verification/6.9/loading_capture.txt`: five separate, real `--dump-dom`
runs, back to back, against the real page served over a real local
server, with no `--virtual-time-budget` flag at all this time —
deliberately, so each run genuinely races the real fetch instead of
waiting for it. Three of the five real attempts caught the page after
the real fetch had already resolved; the fourth and fifth genuinely
caught it mid-flight:

```html
<section id="entry-list" class="state-loading">
  <h2>Entries</h2>
  <p class="state-message">Loading…</p>
</section>
```

This is the real, concrete answer to this unit's own Socratic prompt:
`showListState("state-loading", "Loading…")` sets `entryList`'s own
`class` directly (matching Lesson 5.7's own first real part), then
builds and appends a real `<p class="state-message">` (matching Lesson
5.7's own second real part) — and this real, five-attempt capture proves
the resulting shape is genuinely reachable, not merely theoretically
correct: two of five real runs actually observed it.

### Discard the Throwaway Example

Not applicable — this unit's own real proof is a direct, real
verification against the committed project, not a separate, discardable
lab.

### Mechanical Walkthrough

- **`entryList.querySelector(".state-message")`** — per Lesson 6.6's own
  real Objects and methods entry for `querySelector`, restated here in
  full: searches inside `entryList` for the first element carrying
  `class="state-message"`, returning `null` if none currently exists —
  correctly the real case the very first time `loadEntries` ever runs.
- **`if (existing) { existing.remove(); }`** — a real, plain
  conditional, skipped correctly when nothing was found, and, per this
  lesson's own Objects and methods entry for `.remove()`, above,
  detaching whatever message was previously showing when something was.
- **`entryList.classList.remove("state-loading", "state-empty",
  "state-error")`** — per Lesson 6.6's own real Objects and methods
  entry for `.remove`, restated here in full, with one real, new detail:
  called here with *three* real arguments at once — per MDN's own
  already-quoted documentation for this method's own real signature,
  every one is removed if present, none of them raising any real error
  if they weren't there to begin with.
- **`entryList.classList.add(state)`** — per Lesson 6.3's own real
  Objects and methods entry for `.add`, restated here in full: adds
  whichever one real state name was actually passed in.
- **`document.createElement("p")`, `.classList.add("category")`
  → `.classList.add("state-message")`, `.textContent = text`,
  `entryList.appendChild(message)`** — the identical real DOM-building
  methods Lesson 6.3 already gave full treatment to, applied here to a
  message paragraph instead of a `Data` record's own markup.
- **`function clearListMessage() { ... }`, `function showListState
  (state, text) { ... }`** — per Lesson 6.4's own real Terms entry for
  **function declaration**, restated here in full: two new, real, named
  functions, `showListState` taking two real parameters (which state,
  what text) so this lesson's own next two units can reuse it for
  genuinely different real states without repeating this unit's own
  real construction logic.

### CS Lens

Not applicable as a full, separate "recognized elsewhere" list here —
naming a small, fixed set of mutually exclusive conditions is the
identical real idea Lesson 5.7's own CS Lens already gave a full, real
treatment for (a finite-state machine's own real states); repeating
that list here for the identical underlying idea, now finally wired to
real script instead of only styled, would violate this schema's own
Stopping Rule.

### SE Lens

The principle is **a UI state made real and testable, not just styled
and assumed** — this unit's own real, five-attempt capture is the
honest proof: a state that only ever exists in CSS, never actually
triggered and observed by a real test, is a real, unverified claim, no
matter how correct the styling looks in isolation. The real alternative
not chosen: trust Lesson 5.7's own real screenshots (a static `<section
class="state-loading">` typed by hand into a throwaway lab) as
sufficient proof this state would ever actually appear once real script
existed to trigger it. The real, concrete cost that alternative would
introduce: nothing would have actually confirmed `loadEntries`'s own
real timing ever produces a window where that class is genuinely
applied — this unit's own five real, back-to-back attempts, two of
which caught it, are the difference between "this should work" and "this
was actually observed happening."

### Commands Needed

Same `python -m http.server <port>` and `chrome --headless --disable-gpu
--dump-dom` commands already established — this unit's own real, five
attempts specifically omit `--virtual-time-budget`, unlike every prior
use of this same command in Phase 6, precisely because catching the
race is the whole real point here.

### Run It

Verified this session — all five real attempts already described above,
saved in full in `verification/6.9/loading_capture.txt`.

### Connect the Pieces (this unit → next)

`loadEntries` now honestly names the wait before anything else happens.
It still treats every real outcome after that wait as identical
success — the next unit proves that isn't true.

---

## Concept Unit: Two Kinds of Real Failure

### The Problem

`await fetch("data.json")`, since Lesson 6.7, has always been treated as
either "it works" or "something throws." A real, deliberate test this
session — requesting a file that genuinely doesn't exist — proves that
assumption wrong in a specific, important way: does a real HTTP `404`
actually throw at all, the way a reader might reasonably expect a
"failed" request to?

> **Stop and try it yourself first:** Given `fetch`'s own real job (per
> Lesson 6.7: start a request, resolve once *a response* — any
> response — comes back), and given that a real web server answering
> with "404, not found" is still genuinely *answering*, not failing to
> connect at all, what would you predict: does `fetch`'s own Promise
> reject for a real `404`, the same way it would for a genuinely
> unreachable server — or does it resolve normally, leaving something
> else to notice the failure?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's own
  opening note.
- **Files affected** — `code/05-html-css/Data/app.js`.
- **Change type** — add.
- **Location** — inside `loadEntries`'s own `try` block, immediately
  after `await fetch(...)`; the whole request now wrapped in a real
  `try`/`catch`.
- **Dependencies** — the previous unit.

### The New Code

```js
try {
  const response = await fetch("data.json");
  if (!response.ok) {
    throw new Error(`Request failed: ${response.status}`);
  }
  const data = await response.json();
  // ...
} catch (error) {
  showListState("state-error", "Couldn't load entries.");
}
```

### The Updated Project

The complete, real, committed `code/05-html-css/Data/app.js`:

```js
1   const entryList = document.getElementById("entry-list");
2   const entryDetail = document.getElementById("entry-detail");
3   let selectedArticle = null;
4
5   function renderDetail(entry) {
6     const currentArticle = entryDetail.querySelector("article");
7     currentArticle.replaceWith(createEntryElement(entry));
8   }
9
10  function createEntryElement(entry) {
11    const article = document.createElement("article");
12    article.setAttribute("data-entry-id", entry.id);
13
14    const heading = document.createElement("h3");
15    heading.textContent = entry.title;
16    article.appendChild(heading);
17
18    const categoryValue = document.createElement("data");
19    categoryValue.setAttribute("value", entry.category);
20    categoryValue.classList.add("category");
21    categoryValue.textContent = entry.category;
22    const categoryLine = document.createElement("p");
23    categoryLine.append("Category: ", categoryValue);
24    article.appendChild(categoryLine);
25
26    const durationValue = document.createElement("data");
27    durationValue.setAttribute("value", entry.duration);
28    durationValue.textContent = `${entry.duration} minutes`;
29    const durationLine = document.createElement("p");
30    durationLine.append("Duration: ", durationValue);
31    article.appendChild(durationLine);
32
33    article.addEventListener("click", () => {
34      document.title = `Data — ${entry.title}`;
35
36      if (selectedArticle) {
37        selectedArticle.classList.remove("selected");
38      }
39      selectedArticle = article;
40      selectedArticle.classList.add("selected");
41
42      renderDetail(entry);
43    });
44
45    return article;
46  }
47
48  function renderEntries(list) {
49    for (const entry of list) {
50      entryList.appendChild(createEntryElement(entry));
51    }
52  }
53
54  function clearListMessage() {
55    const existing = entryList.querySelector(".state-message");
56    if (existing) {
57      existing.remove();
58    }
59  }
60
61  function showListState(state, text) {
62    clearListMessage();
63    entryList.classList.remove("state-loading", "state-empty", "state-error");
64    entryList.classList.add(state);
65
66    const message = document.createElement("p");
67    message.classList.add("state-message");
68    message.textContent = text;
69    entryList.appendChild(message);
70  }
71
72  async function loadEntries() {
73    showListState("state-loading", "Loading…");
74
75    try {  ← new
76      const response = await fetch("data.json");
77      if (!response.ok) {  ← new
78        throw new Error(`Request failed: ${response.status}`);  ← new
79      }  ← new
80      const data = await response.json();
81      renderEntries(data);
82    } catch (error) {  ← new
83      showListState("state-error", "Couldn't load entries.");  ← new
84    }  ← new
85  }
86
87  loadEntries();
```

Lines 1–70, unchanged since this lesson's own previous unit, hold every
real function this project already had, plus this lesson's own new
`showListState`/`clearListMessage`. Lines 75, 77–79, and 82–84 are this
unit's own entire real addition: a `try` block now wraps the whole real
request, a real `if`/`throw` catches an HTTP-level failure `fetch`
itself wouldn't, and a real `catch` block shows this lesson's own error
state for either kind of real failure. Line 81's own `renderEntries
(data)`, unchanged, still only runs once both real checks — the
network succeeded, *and* the server said it succeeded — have actually
passed.

### Introduce the Concept in Isolation

This is exactly the real distinction this unit's own code depends on,
isolated here in a real, throwaway lab
(`verification/6.9/lab_two_failures.js`), run this session against the
real local server already established:

```js
async function checkNotFound() {
  try {
    const response = await fetch("http://localhost:8125/nonexistent.json");
    console.log("did NOT throw. response.ok =", response.ok, "status =", response.status);
  } catch (error) {
    console.log("threw:", error.message);
  }
}

async function checkUnreachable() {
  try {
    const response = await fetch("http://localhost:1/data.json");
    console.log("did NOT throw. response.ok =", response.ok, "status =", response.status);
  } catch (error) {
    console.log("threw:", error.message);
  }
}

await checkNotFound();
await checkUnreachable();
```

Real output, from a real `node lab_two_failures.js` run this session,
saved in `verification/6.9/run_output.txt`:

```
did NOT throw. response.ok = false status = 404
threw: fetch failed
```

This is the real, concrete answer to this unit's own Socratic prompt: a
real `404` does **not** reject `fetch`'s own Promise — `checkNotFound`'s
own real `try` block runs to completion, `response.ok` correctly
reporting `false` and `response.status` correctly reporting `404`,
exactly per this lesson's own Objects and methods entry for `Response
.ok`, quoted directly from MDN. A genuinely unreachable server, `http:
//localhost:1` (a real port nothing is listening on), *does* reject —
`checkUnreachable`'s own real `catch` block runs instead, `error
.message` reading `"fetch failed"`. Two real, different failures,
requiring two real, different mechanisms to actually catch: `response
.ok` plus a manual `throw` for the first, `try`/`catch` alone for the
second — exactly the real, combined shape this lesson's own real code,
in this unit's own Updated Project step above, actually uses.

### Discard the Throwaway Example

`lab_two_failures.js` is discarded now; the real, committed project's
own combined error handling, shown in this unit's own Updated Project
step above, is what actually ships.

### Mechanical Walkthrough

- **`try { ... } catch (error) { ... }`** — per this lesson's own Terms
  entry for **`try`/`catch` statement**: runs everything inside the
  `try` block normally, and, per MDN's own quoted documentation, jumps
  straight to the `catch` block the instant anything inside `try` throws
  — here, either this unit's own new `throw` statement, or a genuine
  rejection from `await fetch(...)` itself.
- **`if (!response.ok)`** — per this lesson's own Objects and methods
  entry for `Response.ok`, above: reads this real, already-explained
  property directly; `!`, JavaScript's own logical-not operator, negates
  it, so this real branch runs specifically when the request did *not*
  succeed.
- **`throw new Error(\`Request failed: ${response.status}\`)`** — per
  this lesson's own Terms entries for **`throw`** and **`Error`**,
  above: constructs a real, new `Error` object — `new`, the identical
  real construction keyword this whole curriculum's own Phase 1 through
  Phase 4 already used for building an object from a class — holding a
  real, descriptive message built with Lesson 6.3's own real template
  literal, then immediately throws it, jumping straight to this unit's
  own `catch` block.
- **`catch (error)`** — per this lesson's own Terms entry for
  **`try`/`catch` statement**, restated here in full: names the thrown
  real value, whichever real `Error` it happened to be — this unit's own
  manually-constructed one, or one `fetch` itself constructed and threw
  for a genuine network failure.
- **`showListState("state-error", "Couldn't load entries.")`** — per
  this lesson's own previous unit's own real function, reused here
  unchanged, applying Lesson 5.7's own real error state — its own bold,
  dark red text, per that lesson's own quoted WCAG citation, not color
  alone.

### CS Lens

**Distinguishing "the operation completed" from "the operation
succeeded," rather than conflating them into one binary outcome**, is a
real, recurring idea beyond this one `fetch` call.

```
Also recognized in: an HTTP status code itself, real and standardized,
where a server can complete a request and still honestly report failure
(4xx, 5xx) without the underlying TCP connection ever failing at all; a
process's own real exit code in any operating system, where the process
genuinely ran to completion but can still report non-zero to signal
failure; a database transaction (this whole curriculum's own Phase 9)
that completes and returns a real result set, which might still be
empty or represent a business-logic failure the database itself has no
way to detect
```

### SE Lens

The principle is **failing loudly and specifically, not silently or
generically** — this unit's own real `Error` message includes
`response.status`, the actual real HTTP code, rather than a generic
"something went wrong." The real alternative not chosen: skip the
`response.ok` check entirely, and let a real `404` flow straight into
`response.json()` — which, given a server's own real `404` page is
typically HTML, not JSON, would itself throw, but with a real, unrelated
parsing error (`Unexpected token '<'`, in most real JavaScript engines)
instead of an honest, direct statement that the *request* failed. The
real, concrete cost that alternative would introduce: a real developer
debugging this project later would see a confusing, low-level parsing
error and have to work backward to realize the actual real cause was an
HTTP failure several steps earlier — exactly the kind of real,
avoidable confusion this unit's own explicit `response.ok` check and
descriptive `throw` prevent by construction.

### Commands Needed

Same `node <file>.js` command already established in Phase 6, run
against the real local server already started for this lesson.

### Run It

Verified this session — the real output already shown above, saved in
`verification/6.9/run_output.txt`.

### Connect the Pieces (this unit → next)

Failure, in both of its real, distinct forms, now shows Lesson 5.7's own
real error state correctly. One real situation remains unaccounted for:
a request that succeeds, with a real, valid, empty response.

---

## Concept Unit: Empty Is Not the Same as Broken

### The Problem

`renderEntries(data)`, per this lesson's own previous unit, still runs
unconditionally the moment `data.json`'s own real content is
successfully parsed — even if that real content is a genuinely empty
array, `[]`, a real, valid, honest response meaning "the request
succeeded, and there is currently nothing to show," not "something
failed." Given Lesson 5.7's own real, separate `.state-empty` class,
distinct from `.state-error`, and given `data`'s own real shape (a
JavaScript array, per Lesson 6.2's own already-established real shape),
what real, minimal check would distinguish "genuinely nothing to
render" from "something to actually pass to `renderEntries`"?

> **Stop and try it yourself first:** JavaScript arrays already carry
> a real, built-in `.length` property, reporting how many real elements
> they hold — a real, already-familiar idea from this whole curriculum's
> own earlier phases' own collection types (Lesson 2.8's own Java
> `List`). Given that, what real, single comparison would you predict
> distinguishes an empty real array from one genuinely holding at least
> one record?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's own
  opening note.
- **Files affected** — `code/05-html-css/Data/app.js`.
- **Change type** — add.
- **Location** — inside `loadEntries`'s own `try` block, replacing the
  unconditional `renderEntries(data);` call.
- **Dependencies** — both of this lesson's own previous units.

### The New Code

```js
if (data.length === 0) {
  showListState("state-empty", "No entries yet.");
} else {
  renderEntries(data);
}
```

### The Updated Project

The complete, real, committed `code/05-html-css/Data/app.js`:

```js
1   const entryList = document.getElementById("entry-list");
2   const entryDetail = document.getElementById("entry-detail");
3   let selectedArticle = null;
4
5   function renderDetail(entry) {
6     const currentArticle = entryDetail.querySelector("article");
7     currentArticle.replaceWith(createEntryElement(entry));
8   }
9
10  function createEntryElement(entry) {
11    const article = document.createElement("article");
12    article.setAttribute("data-entry-id", entry.id);
13
14    const heading = document.createElement("h3");
15    heading.textContent = entry.title;
16    article.appendChild(heading);
17
18    const categoryValue = document.createElement("data");
19    categoryValue.setAttribute("value", entry.category);
20    categoryValue.classList.add("category");
21    categoryValue.textContent = entry.category;
22    const categoryLine = document.createElement("p");
23    categoryLine.append("Category: ", categoryValue);
24    article.appendChild(categoryLine);
25
26    const durationValue = document.createElement("data");
27    durationValue.setAttribute("value", entry.duration);
28    durationValue.textContent = `${entry.duration} minutes`;
29    const durationLine = document.createElement("p");
30    durationLine.append("Duration: ", durationValue);
31    article.appendChild(durationLine);
32
33    article.addEventListener("click", () => {
34      document.title = `Data — ${entry.title}`;
35
36      if (selectedArticle) {
37        selectedArticle.classList.remove("selected");
38      }
39      selectedArticle = article;
40      selectedArticle.classList.add("selected");
41
42      renderDetail(entry);
43    });
44
45    return article;
46  }
47
48  function renderEntries(list) {
49    for (const entry of list) {
50      entryList.appendChild(createEntryElement(entry));
51    }
52  }
53
54  function clearListMessage() {
55    const existing = entryList.querySelector(".state-message");
56    if (existing) {
57      existing.remove();
58    }
59  }
60
61  function showListState(state, text) {
62    clearListMessage();
63    entryList.classList.remove("state-loading", "state-empty", "state-error");
64    entryList.classList.add(state);
65
66    const message = document.createElement("p");
67    message.classList.add("state-message");
68    message.textContent = text;
69    entryList.appendChild(message);
70  }
71
72  async function loadEntries() {
73    showListState("state-loading", "Loading…");
74
75    try {
76      const response = await fetch("data.json");
77      if (!response.ok) {
78        throw new Error(`Request failed: ${response.status}`);
79      }
80      const data = await response.json();
81
82      clearListMessage();  ← new
83      entryList.classList.remove("state-loading");  ← new
84
85      if (data.length === 0) {  ← new
86        showListState("state-empty", "No entries yet.");  ← new
87      } else {  ← new
88        renderEntries(data);  ← new
89      }  ← new
90    } catch (error) {
91      showListState("state-error", "Couldn't load entries.");
92    }
93  }
94
95  loadEntries();
```

Lines 1–79 and 90–93, unchanged since this lesson's own previous unit,
still handle every real state this project already accounted for. Lines
82–89 are this unit's own entire real addition: clearing the loading
message and class directly (not via `showListState`, since the real,
populated case needs neither a new message nor a new state class at
all), then branching on `data.length`.

### Introduce the Concept in Isolation

This is exactly what this unit's own new check does, isolated here by a
real, direct comparison of two genuinely different real server
responses, both served this session from the real local server already
established:

Real `--dump-dom` output, `data.json` holding three real records:

```html
<section id="entry-list" class="">
  <h2>Entries</h2>
<article data-entry-id="1"><h3>Morning Run</h3><p>Category: <data value="cardio" class="category">cardio</data></p><p>Duration: <data value="32">32 minutes</data></p></article><article data-entry-id="2"><h3>Team Meeting</h3><p>Category: <data value="work" class="category">work</data></p><p>Duration: <data value="45">45 minutes</data></p></article><article data-entry-id="3"><h3>Grocery Shopping</h3><p>Category: <data value="errand" class="category">errand</data></p><p>Duration: <data value="20">20 minutes</data></p></article></section>
```

Real `--dump-dom` output, `data.json` holding a real, valid, empty
array, `[]`:

```html
<section id="entry-list" class="state-empty">
  <h2>Entries</h2>
<p class="state-message">No entries yet.</p></section>
```

Both real outputs, saved in full in `verification/6.9/run_output.txt`,
confirm this unit's own real Socratic prompt directly: `data.length ===
0` — a real, strict equality comparison against the array's own real,
built-in `.length` property — is exactly the check that separates these
two real, valid outcomes, neither one ever reaching `.state-error` at
all.

### Discard the Throwaway Example

Not applicable — this unit's own real proof is a direct, real comparison
between two genuine server responses, not a separate, discardable lab.

### Mechanical Walkthrough

- **`data.length`** — a real, built-in property every JavaScript array
  carries, reporting how many real elements it holds — `3` for the real,
  committed `data.json`; `0` for this unit's own second real test case.
- **`=== 0`** — a real, strict equality comparison; `===`, unlike the
  looser `==` this project has never used, checks both value *and*
  type, without JavaScript's own real, sometimes-surprising implicit
  type conversion — the correct real choice any time comparing against
  a known, specific real value like this.
- **`showListState("state-empty", "No entries yet.")`** — per this
  lesson's own first unit's own real function, reused here, applying
  Lesson 5.7's own real, distinct empty-state styling.
- **`renderEntries(data)`** — per Lesson 6.4's own real Terms entry for
  **function call**, restated here in full: now runs only in the real
  branch where `data` genuinely holds at least one record, correctly
  guarded for the first time since Lesson 6.7 first introduced it.

### CS Lens

Not applicable as a full, separate "recognized elsewhere" list here —
distinguishing a genuinely empty, successful result from a failure is
the identical real idea this unit's own previous unit already gave a
full, real CS Lens treatment for ("the operation completed" versus "the
operation succeeded," extended here to a third real, honest outcome:
"succeeded, with nothing in it"); repeating that list here would violate
this schema's own Stopping Rule.

### SE Lens

The principle is **every real, valid outcome gets its own real,
distinct representation** — three genuinely different real conditions
(populated, empty, failed) now each produce a visibly, honestly
different real result, rather than two of the three being silently
collapsed into one. The real alternative not chosen: let `renderEntries`
run unconditionally, regardless of `data.length`, and let an empty real
list simply render as a `<section>` holding nothing but its own `<h2>`
— visually indistinguishable from a real bug (a script that silently
failed to attach anything) rather than an honest, deliberate "there is
currently nothing here." The real, concrete cost that alternative would
introduce is exactly Lesson 5.7's own original real reasoning, restated
here because it's now finally true instead of only planned: a real
reader has no way to tell "correctly empty" from "silently broken"
without this lesson's own explicit, distinct real state to tell them
apart.

### Commands Needed

Same `python -m http.server <port>` and `chrome --headless --disable-gpu
--dump-dom` / `--screenshot` commands already established in Phase 6.

### Run It

Verified this session — both real outputs already shown above, saved in
full in `verification/6.9/run_output.txt`. Real screenshots of all three
now-reachable non-default states — `verification/6.9/loading_state.png`,
`verification/6.9/empty_state.png`, `verification/6.9/error_state.png`
— all captured this session, visually confirm Lesson 5.7's own italic
loading text, muted empty message, and bold red error message, each
exactly as that lesson originally designed and screenshotted, five
lessons before any of them had a real script to trigger them.

### Connect the Pieces (this unit → next)

All four of `Data`'s own real states — populated, loading, empty, and
error — are now genuinely reachable, each one proven this session by an
actual, real, observed run, not merely a styled, hypothetical CSS rule.

---

## Connect the Pieces

Tracing this lesson's own real project change, start to finish:
`showListState` and `clearListMessage`, two new real functions, first
gave `loadEntries` a real, visible "loading" moment — proven not just
plausible but *actually observed*, twice, across five real, back-to-back
attempts specifically run without the timing safety net every earlier
Phase 6 lesson used. A real, throwaway comparison of two genuinely
different real failures then proved `fetch` itself only rejects for a
real network-level failure, never for an HTTP-level one like a `404` —
requiring this lesson's own explicit `response.ok` check and a real,
manually-constructed `Error`, alongside an ordinary `try`/`catch`
reappearing from this whole curriculum's own Phase 1 and Phase 2. And a
real, final check — `data.length === 0` — correctly separated "nothing
to show, and that's fine" from both of this lesson's own other real
outcomes. `code/05-html-css/Data/styles.css`, unchanged since Lesson
5.7, is exactly the same file it was five lessons ago — every real
class this lesson needed was already there, waiting.

**Next lesson (6.10):** `app.js`, now well past ninety real lines and
several genuinely distinct real jobs (rendering, selection, fetching,
state), gets split across more than one real file for the first time —
`import` and `export`, JavaScript's own real module system, replacing a
single growing script with several smaller, connected ones.
