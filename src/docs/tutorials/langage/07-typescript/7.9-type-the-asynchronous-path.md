# Lesson 7.9: The Type Underneath `await`

**What you will build:** a real, isolated proof of what `await`
actually does to a `Promise<T>`, generically — tying together Lesson
7.1's own real `Promise<any>` finding, Lesson 7.4's own real
`Promise<Entry[]>` boundary type, and Lesson 7.8's own real generics,
all three now shown to be the identical real mechanism. TypeScript's
own actual, fetched `Promise<T>` interface and its own real `Awaited
<T>` utility type — genuine, current source, not reconstructed from
memory — back the real claim directly. `loadEntries`'s own real,
awaited `data` then receives one small, deliberate, explicit
annotation, `Entry[]`, completing a real, traceable type all the way
from `fetchEntries`'s own outer signature to `renderEntries`'s own
parameter — the last real seam in this project's async path that
hadn't been named out loud.

**What you need to know first:** Lesson 6.8's own real `async`/`await`
rewrite. Lesson 7.1's own real, captured `Promise<any>` finding. Lesson
7.4's own real `Promise<Entry[]>` boundary type on `fetchEntries`.
Lesson 7.8's own real generic functions and type parameters — this
lesson's own central real claim is that `Promise<T>` is exactly one of
those, not a special case.

## Terms used in this lesson

- **`Promise<T>`, as a generic type** — reappearing from Lesson 7.1's
  own real introduction, given its full, formal treatment for the
  first time: `Promise` is not a fixed, single real type — it's a real,
  generic interface, `Promise<T>`, exactly like Lesson 7.8's own
  `firstOf<T>` or `renderList<T>`, parameterized by whatever real type
  it will eventually resolve with. `Promise<any>` (Lesson 7.1) and
  `Promise<Entry[]>` (Lesson 7.4) are the identical real interface,
  with two genuinely different real type arguments.
- **`Awaited<T>`** — a real, built-in TypeScript utility type, quoted
  directly from its own real, current source this session (below),
  computing exactly what type an `await` expression produces from a
  given real `T`. It exists because `await`'s own real behavior —
  unwrap a `Promise<T>` down to `T`, but only if the value actually is
  one — needed a real, named, inspectable answer, not an unwritten
  assumption.

## Objects and methods used

- **`Promise<T>`** — *What it is:* JavaScript's own real, built-in,
  generic type representing a value that doesn't exist yet but will.
  *Implementation:* per TypeScript's own real, current source, fetched
  this session from
  `node_modules/@typescript/typescript-win32-x64/lib/lib.es5.d.ts`,
  lines 1548–1563:
    ```ts
    interface Promise<T> {
      then<TResult1 = T, TResult2 = never>(onfulfilled?: ((value: T) => TResult1 | PromiseLike<TResult1>) | undefined | null, onrejected?: ((reason: any) => TResult2 | PromiseLike<TResult2>) | undefined | null): Promise<TResult1 | TResult2>;
      catch<TResult = never>(onrejected?: ((reason: any) => TResult | PromiseLike<TResult>) | undefined | null): Promise<T | TResult>;
    }
    ```
    `.then` and `.catch` are `Promise<T>`'s own two real, declared
    instance methods — the same real API this project's own Lesson
    6.7 first wrote, `fetch(...).then(...)`, before Lesson 6.8's own
    real rewrite replaced every call site with `await` instead; neither
    method has been called directly anywhere in this project's own
    real, committed code since.
  *Its use:* the real, generic return type of every real `async
  function` this project has ever written — `fetchEntries`, `loadEntries`
  — whether or not either one's own source ever explicitly names it.
  - *Type:* a real, built-in, generic TypeScript interface.
  - *Responsibility:* represents a real, eventual result — success,
    carrying a real value of type `T`, or failure, carrying a real
    reason — and exposes `.then`/`.catch` for real, explicit,
    callback-based handling of either outcome.
  - *Depends on:* nothing to exist; a real, language-level built-in.
  - *Connects to:* returned by `fetch` (Lesson 6.7), `Response.json()`
    (Lesson 7.1), and every real `async function` in this project;
    consumed, throughout this whole phase, exclusively via `await`
    rather than `.then`/`.catch` directly.
  - *Shape:* this project's own real, central asynchronous type — named
    explicitly, in full, for the first time this lesson, despite
    having been used since Lesson 6.7.
- **`Promise.resolve`** — *What it is:* a real, static method on
  `Promise` itself (not an instance method), building an
  already-resolved real `Promise`. *Implementation:* per TypeScript's
  own real, fetched declaration,
  `resolve<T>(value: T): Promise<Awaited<T>>`. *Its use:* this lesson's
  own isolated lab's own `delay<T>` function uses it once, purely to
  produce a real `Promise<T>` to test `await` against, without needing
  a real, genuine asynchronous source like `fetch`.
  - *Type:* a real, static, generic method on the built-in
    `Promise` constructor.
  - *Responsibility:* wraps an already-known real value in an
    already-resolved real `Promise`.
  - *Depends on:* one real value, of any type.
  - *Connects to:* called once, inside this lesson's own isolated lab's
    `delay<T>`.
  - *Shape:* a real, throwaway testing convenience — not used anywhere
    in this project's own committed code.
- **`fetchEntries`** — reappearing from Lesson 6.7's own real
  introduction, restated in full, now fully typed since Lesson 7.4.
  *What it is:* this project's own real function fetching and parsing
  `Data`'s own entries. *Implementation:* `async function
  fetchEntries(): Promise<Entry[]>` (Lesson 7.4), body unchanged since
  Lesson 6.8. *Its use:* the one real, outer source of `loadEntries`'s
  own `data`, this lesson's own real project change.
  - *Type:* an exported, `async` function.
  - *Responsibility:* fetches, checks, and parses this project's own
    entry data, returning a real `Promise<Entry[]>`.
  - *Depends on:* a real, reachable `data.json` over HTTP.
  - *Connects to:* called once, inside `loadEntries`, its own real
    result now explicitly annotated this lesson.
  - *Shape:* unchanged since Lesson 7.4.
- **`tsc`** — reappearing from Lesson 7.1's own real, full introduction,
  restated in full. *What it is:* the real TypeScript compiler.
  *Its use:* this lesson's own real, captured proof that `await`
  genuinely unwraps a generic `Promise<T>` to plain `T`, and that this
  project's own real, final async seam now compiles clean with an
  explicit type at every real step.
  - *Type:* a real, external command-line program.
  - *Responsibility:* type-checks every real `.ts` file it's pointed
    at.
  - *Depends on:* `tsconfig.json` and this project's own real `.ts`
    files.
  - *Connects to:* reads this lesson's own isolated lab and the real,
    committed `app.ts`.
  - *Shape:* unchanged since Lesson 7.1.

---

## Concept Unit: The Type Behind `await`

### The Problem

`fetchEntries`'s own real, outer type, `Promise<Entry[]>`, has been
explicit since Lesson 7.4. `loadEntries`'s own real `const data = await
fetchEntries();` has, this whole time, silently received a plain
`Entry[]` — proven correct only by trusting TypeScript's own real
inference, never directly shown *why* awaiting a `Promise<Entry[]>`
produces exactly `Entry[]`, and nothing else.

> **Stop and try it yourself first:** Given Lesson 7.8's own real
> generic functions, and given `Promise<T>`'s own real, generic shape,
> what would you predict `await` actually does to a value of type
> `Promise<T>`, for *any* real `T` — not just `Entry[]`? Would you
> expect the real result to still be wrapped in `Promise`, or unwrapped
> down to plain `T`?

### Project Change

- **Reference Source** — no reference counterpart, per Lesson 7.1's own
  opening note, still true this lesson.
- **Files affected** — none in this unit; this unit proves a real,
  general fact about `await` in isolation before this lesson's own
  next unit applies it, by name, to the real, committed project.
- **Change type** — none (isolation only).
- **Location** — not applicable.
- **Dependencies** — Lesson 7.8's own real generic functions.

### The New Code

```ts
function delay<T>(value: T): Promise<T> {
  return Promise.resolve(value);
}

async function run() {
  const unwrapped: number = await delay(42);
  const wrong: string = await delay(42);
}
```

### The Updated Project

Not applicable — this unit's own code is a standalone, real
illustration; no project file changes.

### Introduce the Concept in Isolation

Real, captured output, `tsc --noEmit --strict --lib ES2022,DOM
lab_await_unwrap.ts`, this session, saved in
`verification/7.9/lab_await_unwrap.ts` and
`verification/7.9/run_output.txt`:

```
lab_await_unwrap.ts(7,9): error TS2322: Type 'number' is not assignable to type 'string'.
```

This is the real, concrete answer to this unit's own Socratic prompt:
`await delay(42)` — `delay<T>` inferring `T` as `number` (Lesson 7.8's
own real contextual inference) — produces plain `number`, not
`Promise<number>`; `unwrapped: number` is accepted, and only line 7's
own real, mismatched `wrong: string` is rejected. `await` genuinely
unwraps `Promise<T>` down to `T`, for whatever real `T` a specific
`Promise` was actually built with — not a special case for this
project's own `Entry[]`, but the identical real mechanism this whole
phase has relied on, unnamed, since Lesson 6.8.

TypeScript's own real, fetched source names this mechanism directly —
`Awaited<T>`, quoted from its own real, current, human-written
documentation comment, `lib.es5.d.ts`, this session:

```
/**
 * Recursively unwraps the "awaited type" of a type. Non-promise
 * "thenables" should resolve to `never`. This emulates the behavior
 * of `await`.
 */
```

This is real, genuine, inspectable code, not an unwritten assumption —
its own real implementation (a conditional type, using real syntax
this curriculum hasn't taught) is deliberately not decomposed here;
what matters for this lesson is that a real, named, official answer
exists at all, and that `await`'s own real behavior is documented,
literal TypeScript source a reader's own IDE can navigate to directly,
not framework magic.

### Discard the Throwaway Example

`lab_await_unwrap.ts` is discarded now; `delay<T>` never becomes part
of this project — its own real purpose was proving `await`'s own
general behavior, already fully relied on by `fetchEntries`.

### Mechanical Walkthrough

- **`function delay<T>(value: T): Promise<T>`** — per Lesson 7.8's own
  real, restated **Generic function**: a real type parameter, `T`,
  used for both the parameter and the return type.
- **`return Promise.resolve(value);`** — per this lesson's own Objects
  and methods entry for `Promise.resolve`: wraps `value` (typed `T`) in
  an already-resolved real `Promise<T>`.
- **`const unwrapped: number = await delay(42);`** — `delay(42)` infers
  `T` as `number`, returning `Promise<number>`; `await`, per this
  lesson's own Terms entry for `Awaited<T>`, unwraps it to plain
  `number`.
- **`const wrong: string = await delay(42);`** — the identical real
  unwrap, `number`, now forced into a `string`-typed variable — the
  exact real line this unit's own captured error rejects.

### CS Lens

**A generic wrapper type, and a separate, real operation that
mechanically unwraps it back to the value inside** is a real,
recurring idea beyond `Promise<T>`.

```
Also recognized in: C#'s own `Task<T>` and `await` (this whole
curriculum's own Phase 1) — the identical real relationship, `Task<T>`
unwrapped to `T`; Kotlin's own coroutines and `Deferred<T>` (Phase 3);
Rust's own `Option<T>`/`Result<T, E>`, unwrapped via real pattern
matching rather than `await` — a different real trigger, the identical
real "generic wrapper, explicit unwrap" shape
```

### SE Lens

The principle is **naming a mechanism a project already depends on is
worth doing even after it already works** — every real line of this
project's own `async`/`await` code has behaved correctly since Lesson
6.8, entirely without anyone naming `Awaited<T>` or `Promise<T>`'s own
generic shape out loud. The real, honest value of this unit's own work
isn't a bug fixed — nothing was broken — it's a real, concrete
understanding upgrade: a reader who has only ever seen `await` "just
work" can now point to the exact real, built-in type computing what it
produces, the same real payoff Lesson 7.3's own return-type inference
proof and Lesson 7.1's own `.d.ts` proof already delivered for other
mechanisms this project quietly relied on before explaining them.

### Commands Needed

`tsc --noEmit --strict --lib ES2022,DOM <file>.ts`, identical to every
DOM-touching isolated lab this phase.

### Run It

Verified this session — real, captured output already shown above,
saved in `verification/7.9/run_output.txt`.

### Connect the Pieces (this unit → next)

`await` genuinely unwraps any real `Promise<T>` to `T` — proven in
isolation, for a real, generic `T`. The next unit names this project's
own real `T`, explicitly, at the one place it had only ever been
inferred.

---

## Concept Unit: Tracing the Type Across the Whole Real Path

### The Problem

`fetchEntries`'s own real, outer type is `Promise<Entry[]>` (Lesson
7.4). This unit's own previous unit just proved, generically, that
`await` unwraps that down to `Entry[]` — but `loadEntries`'s own real
`const data = await fetchEntries();` has never said so explicitly; it
has only ever been correctly inferred, silently, since Lesson 7.4.

> **Stop and try it yourself first:** Given this unit's own previous
> unit's real, general proof, what real type would you predict `data`
> already, correctly holds, right now, in the real, committed project —
> before this unit changes a single character?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's
  own opening note, above.
- **Files affected** — `code/05-html-css/Data/app.ts`.
- **Change type** — add (one explicit annotation).
- **Location** — inside `loadEntries`, on `data`'s own declaration.
- **Dependencies** — Lesson 7.4's own real, committed `fetchEntries`.

### The New Code

```ts
const data: Entry[] = await fetchEntries();
```

### The Updated Project

The complete, real, committed `code/05-html-css/Data/app.ts`'s own
`loadEntries`:

```ts
83 async function loadEntries(): Promise<void> {
84   showListState("state-loading", "Loading…");
85
86   try {
87     const data: Entry[] = await fetchEntries();  ← changed
88
89     clearListMessage();
90     entryList.classList.remove("state-loading");
91
92     if (data.length === 0) {
93       showListState("state-empty", "No entries yet.");
94     } else {
95       renderEntries(data);
96     }
97   } catch (error) {
98     showListState("state-error", "Couldn't load entries.");
99   }
100 }
```

Line 87's own new `: Entry[]` is this unit's own only real change —
every other real line, including line 95's own already-typed
`renderEntries(data)` call (Lesson 7.3), unchanged.

### Introduce the Concept in Isolation

Not applicable in the usual sense — this unit's own previous unit
already proved the general real mechanism in full isolation; this unit
applies it directly to the real, committed project, with no separate
lab needed. One further, real, related fact, proven directly against
this project's own actual code rather than a separate lab: `loadEntries
`'s own real `catch (error)` block — unchanged by this lesson — already
carries a real, specific type for `error`, confirmed this session:

```
try {
  throw new Error("x");
} catch (error) {
  const probe: string = error;
}
```

Real, captured output: `error TS2322: Type 'unknown' is not assignable
to type 'string'.` Under `"strict": true`, a real `catch` clause
variable's own inferred type is `unknown`, not `any` — genuinely
different from every other untyped value this whole phase has
encountered, since `unknown` can't be used for *anything* without
first narrowing it (Lesson 7.7), unlike `any`. This project's own real
`catch (error)` already carries this real type; its own real body never
reads `error` at all, so no annotation was necessary here — a real,
honest fact worth naming, not a gap this lesson needed to close.

### Discard the Throwaway Example

Not applicable — this unit's own real work lands directly in the
committed project; nothing here was throwaway.

### Mechanical Walkthrough

- **`const data: Entry[] = await fetchEntries();`** — per this
  lesson's own previous unit's real proof: `fetchEntries()` returns
  `Promise<Entry[]>`; `await`, per `Awaited<T>`, unwraps it to plain
  `Entry[]`; the explicit `: Entry[]` states, deliberately, what was
  already true — the identical real communicative choice Lesson 7.1's
  own `dataUrl: string` and Lesson 7.3's own explicit return types
  already made, restated here at this project's own final, real,
  untyped seam.

### CS Lens

Not applicable as a full, separate "recognized elsewhere" list here —
this unit's own real subject is the direct, applied conclusion of this
lesson's own previous unit's already-given CS Lens; repeating that
same real list here would violate this schema's own Stopping Rule.

### SE Lens

The principle is **a fully typed path is worth tracing end to end at
least once, even when every individual step already worked** —
`Entry`, defined in `types.ts` (7.2), now demonstrably governs every
real step of this project's own data flow: fetched and boundary-typed
in `data.ts` (7.4), unwrapped through `await` (this lesson), passed to
`renderEntries` (7.3), and rendered by `createEntryElement` (7.3, 7.7).
No real step in that whole chain was ever actually broken — the real
value of this unit's own small, explicit annotation is confirming,
directly, that the chain holds all the way through, rather than
trusting each real link individually and never checking the whole
real path at once.

### Commands Needed

`npx tsc --noEmit`, identical to every lesson since 7.1.

### Run It

Verified this session — real, captured output already shown above,
saved in `verification/7.9/run_output.txt`. A real, end-to-end check —
`python -m http.server` (already running, port `8792`) plus a real
headless-Chrome `--dump-dom` — confirms all three real entries still
render correctly (full output in
`verification/7.9/dump_after_7.9.html`).

### Connect the Pieces (this unit → next)

Every real seam this project's own data crosses — network, JSON
boundary, async unwrap, function parameter, DOM render — now carries an
explicit or provably-inferred real type, traced once, end to end.

---

## Connect the Pieces

Tracing this lesson's own real project change, start to finish: a
real, isolated lab first proved `await` genuinely unwraps any real
`Promise<T>` down to plain `T`, generically — the identical real
mechanism, TypeScript's own real, fetched `Awaited<T>` names directly,
that this project's own `fetchEntries`/`loadEntries` pair has silently
relied on since Lesson 6.8. `Promise<T>` itself, shown with its own
real, fetched `.then`/`.catch` interface, was named, for the first
time this phase, as exactly the same kind of real, generic type Lesson
7.8's own `firstOf<T>` and `renderList<T>` already were.
`loadEntries`'s own real `data`, awaited from `fetchEntries()` since
Lesson 6.7, then received one small, deliberate `: Entry[]`
annotation — restating what was already, correctly, true — completing
a real, traceable type from this project's own network boundary all the
way to its own rendered DOM.

**Next lesson (7.10), the last of this whole phase:** TypeScript, now
threaded through every real seam of this project, gets compared
directly against the plain JavaScript Lesson 6.12 already measured it
against — closing this whole phase's own real milestone, the same real
question Lesson 6.12 asked of JavaScript, now asked of its own typed
successor: what does a type system actually let you know before the
program runs?
