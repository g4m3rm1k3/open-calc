# Lesson 7.6: The Typo `string` Never Catches

**What you will build:** `app.ts`'s own `showListState`, whose real
`state` parameter has stayed plain `string` since Lesson 7.3 — flagged,
every lesson since, as a real, still-open gap — gets a real, precise
type: `ListState`, a real, named union of exactly the three literal
strings this project's own CSS (Lesson 5.7) and real code have always
used, `"state-loading" | "state-empty" | "state-error"`. Two real,
directly contrasted labs prove why this matters: the identical
misspelled real call, `"state-succes"`, is caught immediately once
`state` is typed `ListState`, and passes through completely unnoticed
when it's plain `string`.

**What you need to know first:** Lesson 7.3's own real, flagged gap —
`showListState(state: string, text: string)`, any real string
accepted, not just the three legitimate ones. Lesson 7.4's own real
`type` keyword, introduced but not yet committed to this project.
Lesson 7.2's own real, twice-deferred union sightings —
`number | undefined` (`Entry.duration`) and `HTMLElement | null`
(`selection.article`) — both named honestly as "not yet explained,"
until now.

## Terms used in this lesson

- **Union type** — a real type built from two or more other real
  types with the real `|` operator, meaning "a value that is *any one*
  of these" — `number | string`, this lesson's own first real example,
  accepts a real number or a real string, and rejects everything else.
  It exists because a real value's own legitimate range sometimes
  genuinely spans more than one type, or, this lesson's own real,
  central case, more than one specific literal value — and a single
  type name can't express "one of these several options" any other
  way.
- **Literal type** — a real type that names one *specific* value, not
  a whole category of them — `"state-loading"` as a type means "the
  string `state-loading`, and nothing else," not "any string." It
  exists so a union like `ListState` can name a small, closed, real set
  of legitimate values instead of the entire, unbounded space every
  plain `string` allows.
- **Type alias** — reappearing from Lesson 7.4's own real
  introduction: the real `type` keyword, naming a type so it can be
  reused by that name — this lesson's own real, first genuine,
  committed use of it in this project.

## Objects and methods used

- **`tsc`** — reappearing from Lesson 7.1's own real, full introduction,
  restated in full. *What it is:* the real TypeScript compiler.
  *Implementation:* run here the identical real way as every lesson
  since 7.1. *Its use:* this lesson's own real, central, directly
  contrasted proof — one real, identical typo, caught by one real
  parameter type and silently accepted by another.
  - *Type:* a real, external command-line program.
  - *Responsibility:* type-checks every real `.ts` file it's pointed
    at, reporting a real, specific error for a value outside a real
    union's own named set, same as any other real type mismatch.
  - *Depends on:* `tsconfig.json` and this project's own real `.ts`
    files.
  - *Connects to:* reads `app.ts`; this lesson's own real change is
    the smallest yet — three real characters added to one line.
  - *Shape:* unchanged since Lesson 7.1.
- **`console.log`** — reappearing from this whole curriculum's own
  earliest JavaScript lessons, restated in full. *What it is:* a real,
  built-in function printing its own argument to the real console.
  *Its use:* this lesson's own two comparison labs each use it once,
  purely to give their own real, throwaway `showListState` a real body.
  - *Type:* a real, global function.
  - *Responsibility:* prints its own given value as real text.
  - *Depends on:* nothing beyond a real, running JavaScript
    environment.
  - *Connects to:* called once in each of this lesson's own two
    isolated labs, below.
  - *Shape:* unchanged since Lesson 6.12.

---

## Concept Unit: A Value That Could Be Either

### The Problem

Lesson 7.2's own real `duration?: number` produced a real error
message naming `number | undefined` — never explained, flagged
explicitly as Lesson 7.6's own job. Lesson 7.3's own real `selection`
annotation named `HTMLElement | null` the identical real way. Both real
values genuinely can be more than one type; nothing so far has named
what that `|` syntax actually *is*.

> **Stop and try it yourself first:** Given `number | undefined`'s own
> real, already-seen shape, what would you predict a real, standalone
> variable declared `let id: number | string;` actually accepts? Just
> real numbers? Just real strings? Both? And what real, third value —
> neither a number nor a string — would you expect it to reject?

### Project Change

- **Reference Source** — no reference counterpart, per Lesson 7.1's own
  opening note, still true this lesson.
- **Files affected** — none in this unit; this unit names, in
  isolation, syntax this project's own compiler error messages have
  already shown twice, before this lesson's own next unit applies it
  for real.
- **Change type** — none (isolation only).
- **Location** — not applicable.
- **Dependencies** — none beyond this lesson's own already-installed
  TypeScript toolchain.

### The New Code

```ts
let id: number | string;

id = 1;
id = "a1";
id = true;
```

### The Updated Project

Not applicable — this unit's own code is a standalone, real
illustration; no project file changes.

### Introduce the Concept in Isolation

Real, captured output, `tsc --noEmit --strict lab_basic_union.ts`, this
session, saved in `verification/7.6/lab_basic_union.ts` and
`verification/7.6/run_output.txt`:

```
lab_basic_union.ts(5,1): error TS2322: Type 'boolean' is not assignable to type 'string | number'.
```

This is the real, concrete answer to this unit's own Socratic prompt:
`id = 1;` and `id = "a1";` both compile cleanly — a real number, and a
real string, exactly the two real types named. `id = true;`, a real
`boolean`, is rejected — this is called a **union type**, and this real
error message states it plainly: `id`'s own real type is `string |
number` (TypeScript's own real, internal ordering, not necessarily the
order a union was written in), and `boolean` was never one of the real
options. This is the identical real `|` syntax Lesson 7.2's own
`number | undefined` and Lesson 7.3's own `HTMLElement | null` both
already used, unexplained, until now.

### Discard the Throwaway Example

`lab_basic_union.ts` is discarded now; this lesson's own next unit
applies the identical real concept to a genuinely different real shape
— not "one of several types," but "one of several specific values."

### Mechanical Walkthrough

- **`let id: number | string;`** — per this lesson's own Terms entry
  for **Union type**: a real type annotation (Lesson 7.1) naming two
  real, alternative types, joined by `|`; `id` may hold either, at
  different real times, since it's declared `let` (Lesson 6.2),
  reassignable.
- **`id = 1;`** — a real number literal, satisfying the real `number`
  half of the union.
- **`id = "a1";`** — a real string literal, satisfying the real
  `string` half.
- **`id = true;`** — a real `boolean` literal, satisfying neither real
  half — the exact real value this unit's own captured error rejects.

### CS Lens

**A type representing "one of several alternatives," checked at every
real use** is a real, recurring idea beyond TypeScript's own union
syntax.

```
Also recognized in: C#'s own discriminated unions and pattern matching
(more recent C# versions, not used yet by this whole curriculum's own
Phase 1 Service); Kotlin's own sealed classes (Phase 3), a real,
different mechanism reaching the identical real goal — a closed,
enumerable set of legitimate alternatives; a database column's own
real `CHECK` constraint or `ENUM` type, restricting a real value to a
named, finite set the identical real way, at a completely different
real layer (Phase 9, still ahead)
```

### SE Lens

The principle is **naming the real, legitimate alternatives explicitly,
instead of accepting an entire, unbounded category "just in case"** —
`number | string` accepts exactly two real shapes; a looser real
alternative, `any`, would accept everything, silently discarding the
exact real information this union states. The real, honest cost:
every real union has to be updated by hand if a genuinely new
alternative is ever needed — a real, small, ongoing maintenance
obligation, traded for the real, concrete protection this lesson's own
next unit demonstrates directly.

### Commands Needed

`tsc --noEmit --strict <file>.ts`, identical to every isolated lab this
phase.

### Run It

Verified this session — real, captured output already shown above,
saved in `verification/7.6/run_output.txt`.

### Connect the Pieces (this unit → next)

A union of *types* is now real and named. The next unit narrows the
identical real idea down to a union of *specific values* — exactly what
`showListState`'s own real, still-open gap has needed since Lesson 7.3.

---

## Concept Unit: A Value That's One Specific String

### The Problem

`showListState(state: string, text: string)`'s own real `state`
parameter currently accepts *any* real string — `"state-loading"`,
`"nonsense"`, an empty string, all equally valid, per Lesson 7.3's own
real, flagged gap. This project's own real code only ever calls it
with exactly three real, legitimate values — `"state-loading"`,
`"state-empty"`, `"state-error"` — matching Lesson 5.7's own real,
already-committed CSS classes. Given this unit's own previous unit's
real `number | string`, what would a union built from these three real
strings themselves, instead of two broader types, actually look like?

> **Stop and try it yourself first:** If a real, future edit to
> `loadEntries` ever misspelled one of these three real calls —
> `showListState("state-succes", "Done!")`, missing the second `s` —
> would `state: string`, this project's own real, current type, catch
> it? What real type would need to exist instead for a mistake exactly
> this specific to be caught before the program ever ran?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's
  own opening note, above.
- **Files affected** — `code/05-html-css/Data/app.ts`.
- **Change type** — add (a new type alias) plus refactor (one
  parameter's own type).
- **Location** — a new line directly above `showListState`'s own
  declaration; `state`'s own parameter type.
- **Dependencies** — this lesson's own previous unit's real, isolated
  finding.

### The New Code

```ts
type ListState = "state-loading" | "state-empty" | "state-error";

function showListState(state: ListState, text: string): void {
```

### The Updated Project

The complete, real, committed `code/05-html-css/Data/app.ts`, this
lesson's own real change, in context:

```ts
61 function clearListMessage(): void {
62   const existing = entryList.querySelector(".state-message");
63   if (existing) {
64     existing.remove();
65   }
66 }
67
68 type ListState = "state-loading" | "state-empty" | "state-error";  ← new
69
70 function showListState(state: ListState, text: string): void {  ← changed
71   clearListMessage();
72   entryList.classList.remove("state-loading", "state-empty", "state-error");
73   entryList.classList.add(state);
74
75   const message = document.createElement("p");
76   message.classList.add("state-message");
77   message.textContent = text;
78   entryList.appendChild(message);
79 }
```

Line 68 is this lesson's own new, real type alias; line 70's own only
change is `state`'s own new type, `ListState`, replacing plain
`string`. Every other real line, including line 73's own
`entryList.classList.add(state)` — which still just needs a real
string, and gets one, since every real `ListState` value *is* a real
string — is unchanged.

### Introduce the Concept in Isolation

This is exactly what this unit's own Socratic prompt asked, answered
with two real, directly contrasted `tsc` runs, saved in
`verification/7.6/lab_union.ts`,
`verification/7.6/lab_plain_string.ts`, and
`verification/7.6/run_output.txt`.

With the real union:

```ts
type ListState = "state-loading" | "state-empty" | "state-error";

function showListState(state: ListState, text: string): void {
  console.log(state, text);
}

showListState("state-loading", "Loading…");
showListState("state-succes", "typo");
```

Real, captured output:

```
lab_union.ts(8,15): error TS2345: Argument of type '"state-succes"' is not assignable to parameter of type 'ListState'.
```

The identical real scenario, `state` typed plain `string`:

```ts
function showListState(state: string, text: string): void {
  console.log(state, text);
}

showListState("state-loading", "Loading…");
showListState("state-succes", "typo");
```

Real output: `exit: 0` — the identical real typo, completely unnoticed.

This is the real, concrete, directly contrasted answer to this unit's
own Socratic prompt: `state: string` would let the misspelled real call
through silently, exactly as this unit's own real proof shows; `state:
ListState` catches it immediately, naming the exact real, offending
literal string in its own error message. `"state-loading"`,
`"state-empty"`, and `"state-error"` are each called a **literal
type** here — not "any string," but "exactly this one, specific
string" — and `ListState`, their real union, accepts nothing else.

### Discard the Throwaway Example

`lab_union.ts` and `lab_plain_string.ts` are discarded now; the real,
committed `app.ts`, shown above, carries the identical real
`ListState` type these labs just proved catches what plain `string`
never could.

### Mechanical Walkthrough

- **`type ListState = "state-loading" | "state-empty" | "state-error";`**
  — per this lesson's own Terms entries for **Type alias**, **Union
  type**, and **Literal type**: names a real union of exactly three
  real literal string types — the first genuine, real, committed use
  this whole phase has had for `type` (Lesson 7.4's own second real
  way to name a shape), reached for here specifically because a union
  of literal values, per Lesson 7.4's own real, captured proof, is
  something `interface` cannot express under any real syntax at all.
- **`function showListState(state: ListState, text: string): void`**
  — `state`'s own type annotation now names `ListState` instead of
  plain `string`; `text` is unchanged, since every real call genuinely
  does pass an arbitrary, real, human-readable message, not one of a
  small, closed set.
- **`entryList.classList.add(state)`** — unchanged; per this lesson's
  own Terms entry for **Literal type**, every real value `ListState`
  allows genuinely *is* a real string underneath, so this real,
  already-established call (Lesson 6.9) needs no adjustment at all.

### CS Lens

Not applicable as a full, separate "recognized elsewhere" list here —
this unit's own real subject, a union of literal values, is the direct,
narrower application of this lesson's own previous unit's already-given
CS Lens on union types generally; repeating that same real list here
would violate this schema's own Stopping Rule.

### SE Lens

The principle is **the smallest type that still accepts every real,
legitimate value is the correct one — not the smallest type that
happens to compile** — `state: string` always compiled; it was never
*wrong*, only *loose*, accepting a real, unbounded space of values this
project's own actual behavior never needed. `ListState` is the real,
concrete, provable improvement this unit's own directly contrasted labs
demonstrate: identical real code, identical real mistake, caught by one
type and missed entirely by the other. The real, honest cost, matching
this lesson's own previous unit's SE Lens: a real, future fourth list
state would require a real, deliberate edit to `ListState` itself —
a small, real, worthwhile price for a typo this project's own three
real, already-existing states will never again let through silently.

### Commands Needed

`npx tsc --noEmit`, identical to every lesson since 7.1.

### Run It

Verified this session — real, captured output already shown above,
saved in `verification/7.6/run_output.txt`. A real, end-to-end check —
`python -m http.server` (already running, port `8792`) plus a real
headless-Chrome `--dump-dom` — confirms all three real entries still
render correctly (full output in
`verification/7.6/dump_after_7.6.html`).

### Connect the Pieces (this unit → next)

`showListState`'s own real gap, flagged since Lesson 7.3, is closed —
every real call site this project has ever written already used one of
`ListState`'s own three real, legitimate values, so nothing about this
project's own real, running behavior changed; only what's now
*enforced* did. The next lesson asks what happens once a real function
needs to tell two members of a union apart from *inside* its own body,
not just at the moment a value is first assigned.

---

## Connect the Pieces

Tracing this lesson's own real project change, start to finish: a real,
isolated lab first named the `|` syntax Lesson 7.2 and Lesson 7.3 had
each already produced, unexplained, in a real compiler error message —
`number | string`, a **union type**, accepting either real type and
rejecting a real third. A second real lab narrowed the identical idea
to a union of three specific, real **literal types**,
`"state-loading" | "state-empty" | "state-error"`, directly, provably
catching a real, misspelled call that plain `string` — proven, side by
side, in a second, identical lab — let through with zero complaint.
`ListState`, this project's own real, committed union, then replaced
`showListState`'s own plain `string` parameter, closing a gap this
whole phase has carried, named, since Lesson 7.3.

**Next lesson (7.7):** `Entry.duration`, real and optional since Lesson
7.2, is still read completely unguarded inside `createEntryElement` —
the exact real gap that lesson's own third lab proved still reproduces
Lesson 6.12's own `"undefined minutes"` bug, even now, with every real
type in this project otherwise correct. Lesson 7.7 finally closes it,
using a real technique this lesson's own union types make possible for
the first time: telling two members of a real union apart, safely,
from inside a function's own body.
