# Lesson 7.4: The Type That Was Never Verified

**What you will build:** `data.ts`'s own `fetchEntries` gets a real,
explicit return type, `Promise<Entry[]>`, replacing Lesson 7.1's own
real, captured `Promise<any>` finding. A real, deliberately dishonest
isolated lab then proves the uncomfortable, transferable truth this
lesson is actually about: `tsc` reports **zero errors** for a function
whose own declared return type is a real, provable lie — the real,
compiled program still crashes the instant it actually runs. Along the
way, this lesson also names the real, second way TypeScript can declare
a shape — `type`, alongside Lesson 7.2's own `interface` — and shows,
with two real, captured proofs, exactly where the two genuinely differ.

**What you need to know first:** Lesson 7.1's own real, captured
`.d.ts` proof that `fetchEntries`'s own inferred return type was
`Promise<any>`. Lesson 7.2's own real `Entry` interface. Lesson 7.3's
own real finding that `loadEntries`'s own `renderEntries(data)` call
hands `data` — still `any`, right up until this lesson — straight to a
parameter expecting `Entry[]`, with nothing actually checking it.

## Terms used in this lesson

- **`any`** — reappearing from Lesson 7.1's own real, captured
  `Promise<any>` finding, given its own full treatment for the first
  time here. TypeScript's own real, special type meaning "checking is
  turned off for this value" — unlike every other real type in this
  whole phase, `any` is bidirectionally assignable to and from
  *everything*, generic type parameters included, with zero real
  complaint either direction. It exists as TypeScript's own real,
  deliberate escape hatch for values whose own shape genuinely can't be
  known statically — `response.json()`'s own real, built-in declared
  return type, unchanged since Lesson 7.1, is exactly this: real JSON
  data, parsed from real text, whose own actual shape TypeScript has no
  way to know in advance.
- **Boundary typing** — declaring a real type for data at the exact
  real point it crosses from an untyped source (a real network
  response, real user input, a real file) into this project's own
  otherwise fully-typed code. It exists because every real value has to
  enter a typed program from *somewhere* untyped — the real, honest
  question this lesson exists to ask is what that declared type
  actually *proves*, once it's written.
- **Type alias** — TypeScript's own second real way to name a type,
  using the real `type` keyword: `type Status = "loading" | "empty" |
  "error";`. It exists to name real types `interface` (Lesson 7.2)
  structurally can't — a union of literal strings, this lesson's own
  real example, has no real "shape" of properties at all for an
  `interface` to describe.
- **Declaration merging** — a real, `interface`-only behavior: two
  separate real `interface Shape { ... }` declarations, using the
  identical real name, automatically combine into one real type
  carrying every property from both. It exists to let a real interface
  be extended from a second, separate real location — this lesson's
  own isolated lab proves `type` has no equivalent real behavior at
  all; the identical attempt is a real, hard error.
- **`interface`** — reappearing from Lesson 7.2's own real
  introduction: the real TypeScript keyword declaring an object type.
- **Object type** — reappearing from Lesson 7.2's own real
  introduction: a real type describing an object's own required and
  optional properties.
- **`Promise`** — reappearing from Lesson 7.1's own real introduction:
  a real, built-in object representing a value that doesn't exist yet
  but eventually will.

## Objects and methods used

- **`tsc`** — reappearing from Lesson 7.1's own real, full introduction,
  restated in full. *What it is:* the real TypeScript compiler.
  *Implementation:* run here the identical real way as every lesson
  since 7.1. *Its use:* this lesson's own central, real, uncomfortable
  finding — a completely clean `tsc --noEmit` run, `exit: 0`, against a
  function whose own declared return type this lesson's own lab proves,
  directly, is false.
  - *Type:* a real, external command-line program.
  - *Responsibility:* type-checks every real `.ts` file it's pointed
    at — but, per this lesson's own real, captured proof, only ever
    against other *declared* types, never against real, external data
    it has no way to inspect.
  - *Depends on:* `tsconfig.json` and this project's own real `.ts`
    files.
  - *Connects to:* reads `data.ts`; its own real silence on this
    lesson's own deliberately dishonest lab is the exact, uncomfortable
    proof this lesson exists to deliver.
  - *Shape:* unchanged since Lesson 7.1.
- **`Response.json()`** — reappearing from Lesson 7.1's own real
  introduction, restated in full. *What it is:* a real method on
  `Response` that reads its own real body and parses it as JSON.
  *Implementation:* returns `Promise<any>` — per this lesson's own
  Terms entry for **`any`**, this is the exact real, declared source of
  this lesson's own central finding: nothing about `Response.json()`'s
  own real, built-in declaration has ever promised what shape of data
  actually comes back. *Its use:* `fetchEntries`'s own real, final
  line, still unchanged in logic since Lesson 6.7 — only its own
  surrounding function signature changes, this lesson.
  - *Type:* a real instance method on `Response`, returning
    `Promise<any>`.
  - *Responsibility:* reads the real response body to completion and
    parses it as JSON — nothing about its own real, built-in contract
    validates the parsed result against any particular shape.
  - *Depends on:* a real `Response` object.
  - *Connects to:* its own real return value is what `fetchEntries`
    returns — now, per this lesson's own real project change, silently
    accepted as `Promise<Entry[]>`, whether or not it actually is one.
  - *Shape:* unchanged since Lesson 7.1; this lesson doesn't alter its
    own real behavior at all, only how the value it returns gets
    labeled afterward.
- **`fetch`** — reappearing from Lesson 6.7's own real introduction,
  restated in full. *What it is:* a real, built-in function making an
  HTTP request. *Implementation:* unchanged since every prior lesson
  this phase. *Its use:* this lesson's own isolated labs call it once
  each, purely to obtain a real `Response` object to call `.json()` on.
  - *Type:* a real, global function, part of the DOM lib.
  - *Responsibility:* sends a real HTTP request and resolves with a
    real `Response`.
  - *Depends on:* a real URL.
  - *Connects to:* called once in each of this lesson's own two
    boundary-typing labs, below.
  - *Shape:* unchanged since Lesson 6.7.

---

## Concept Unit: Two Ways to Name the Same Shape

### The Problem

Lesson 7.2's own real `interface Entry { ... }` names an object's own
shape. This lesson's own next unit needs to name something genuinely
different: not a real object's own properties, but one of several
legitimate, literal real string states (echoing `showListState`'s own
real, still-plain-`string` parameter, flagged back in Lesson 7.3 as
Lesson 7.6's own real job to finish). Does `interface` stretch to cover
that too, or does TypeScript need a genuinely different real tool for
it?

> **Stop and try it yourself first:** Given `interface Entry { id:
> number; ... }`'s own real, brace-delimited shape, what would you
> guess happens if you tried to write `interface Status { "loading" |
> "empty" | "error" }` — naming three literal real strings instead of
> properties? Would you expect that to work, or does the real syntax
> itself resist it?

### Project Change

- **Reference Source** — no reference counterpart, per Lesson 7.1's own
  opening note, still true this lesson.
- **Files affected** — none in this unit; this unit compares two real,
  already-available TypeScript syntaxes in isolation before this
  lesson's own next unit applies one of them for a genuine, real
  purpose. Lesson 7.6, later in this phase, is the first lesson to
  actually need a real, project-committed type alias.
- **Change type** — none (isolation only).
- **Location** — not applicable.
- **Dependencies** — none beyond this lesson's own already-installed
  TypeScript toolchain.

### The New Code

```ts
interface Shape {
  sides: number;
}
interface Shape {
  color: string;
}

const square: Shape = { sides: 4, color: "red" };

type Status = "loading" | "empty" | "error";

const current: Status = "loading";
```

### The Updated Project

Not applicable — this unit's own code is a standalone, real
illustration; no project file changes.

### Introduce the Concept in Isolation

Three real, separate `tsc` runs this session, saved in
`verification/7.4/lab_interface_vs_type.ts`,
`verification/7.4/lab_type_no_merge.ts`,
`verification/7.4/lab_interface_no_union.ts`, and
`verification/7.4/run_output.txt`.

The code above — real output: `exit: 0`. Two separate real `interface
Shape` declarations combined cleanly into one real type carrying both
`sides` and `color`; `square`, satisfying both, is accepted. `type
Status = "loading" | "empty" | "error";` is real, valid syntax, and
`current: Status = "loading"` is accepted too.

The identical real attempt with `type` instead of `interface`:

```ts
type Shape = {
  sides: number;
};
type Shape = {
  color: string;
};
```

Real, captured output:

```
lab_type_no_merge.ts(1,6): error TS2300: Duplicate identifier 'Shape'.
lab_type_no_merge.ts(4,6): error TS2300: Duplicate identifier 'Shape'.
```

And the reverse attempt — a union, written with `interface`:

```ts
interface Status = "loading" | "empty" | "error";
```

Real, captured output:

```
lab_interface_no_union.ts(1,18): error TS1005: '{' expected.
```

This is the real, concrete answer to this unit's own Socratic prompt:
it doesn't even parse — `interface`'s own real syntax has no way to
write `=` followed by anything but a brace-delimited shape at all. This
is called a **type alias**, and these three real results, together,
prove the genuine, real asymmetry: two `interface`s sharing a name
combine (**declaration merging**); two `type`s sharing a name collide,
as a real, hard error; and only `type` can name something that isn't a
real object shape at all — a union of literal values, `interface`
cannot express under any real syntax.

### Discard the Throwaway Example

`lab_interface_vs_type.ts`, `lab_type_no_merge.ts`, and
`lab_interface_no_union.ts` are discarded now; neither `Shape` nor
`Status` ever becomes part of this project — Lesson 7.6, later in this
phase, writes this project's own first real, committed type alias, for
`showListState`'s own real state parameter.

### Mechanical Walkthrough

- **`interface Shape { sides: number; }`** / **`interface Shape {
  color: string; }`** — per this lesson's own Terms entry for
  **Declaration merging**: two real, separate `interface` declarations,
  identically named; TypeScript combines them into one real type
  requiring both `sides` and `color`.
- **`const square: Shape = { sides: 4, color: "red" };`** — an ordinary
  real object-type annotation (Lesson 7.2), satisfying the merged real
  shape from both declarations at once.
- **`type Status = "loading" | "empty" | "error";`** — per this
  lesson's own Terms entry for **Type alias**: the real `type` keyword,
  a name (`Status`), `=`, and a real union of three literal strings —
  this lesson doesn't yet name or fully explain the union syntax
  itself; Lesson 7.6, later in this phase, does, the identical real
  deferral Lessons 7.2 and 7.3 already made.
- **`const current: Status = "loading";`** — an ordinary real
  annotation, using the new alias by name; `"loading"` is one of the
  three real literal strings `Status` allows.

### CS Lens

**Structural definitions that can be split across multiple real
declarations and merged automatically** is a real, narrower idea than
most of this lesson's own other content, specific enough to skip a
full, separate "recognized elsewhere" list — its own closest real
curriculum cousin, C#'s own `partial class` (splitting one real class
across multiple real files, explicitly, with a keyword), is different
in kind: C# requires the real `partial` keyword at every real site;
TypeScript's own `interface` merges automatically, with no keyword
announcing it's happening.

### SE Lens

The principle is **choose the tool that matches what's actually being
named, not habit** — `interface`'s own real declaration-merging
behavior is a genuine, useful real feature for describing a shape a
real library might let a consumer *extend* (a real, common pattern in
published `.d.ts` files this project hasn't needed yet); it is a real,
unwanted surprise the one real time an author *doesn't* expect two
same-named declarations to combine instead of colliding. The real,
concrete cost of picking wrong: naming `Status` with `interface`
wouldn't just be a real style mismatch — per this unit's own real,
captured `TS1005` proof, it's not valid syntax at all; the real choice
here isn't stylistic, it's dictated entirely by what's actually being
named.

### Commands Needed

`npx tsc --noEmit` (or, for a standalone file with no `tsconfig.json`
nearby, `tsc --noEmit --strict <file>.ts` directly) — identical to
every lesson since 7.1.

### Run It

Verified this session — real, captured output already shown above,
saved in `verification/7.4/run_output.txt`.

### Connect the Pieces (this unit → next)

Two real, genuinely different tools now exist for naming a type. The
next unit reaches for neither, on purpose — it asks a harder, more
uncomfortable real question about what naming a type at a real network
boundary actually accomplishes.

---

## Concept Unit: A Boundary That Trusts Without Checking

### The Problem

`fetchEntries`'s own real return type, per Lesson 7.1's own captured
proof, is `Promise<any>` — and `loadEntries`'s own real
`renderEntries(data)` call, per Lesson 7.3's own real finding, hands
that untyped `any` straight to a parameter expecting `Entry[]`, with
nothing actually checking it. Adding `: Promise<Entry[]>` to
`fetchEntries`'s own signature seems like the obvious real fix — but
given this lesson's own Terms entry for **`any`**, what does that
annotation actually *verify*, if anything, about the real data flowing
through it?

> **Stop and try it yourself first:** `Response.json()`'s own real,
> declared return type has always been `Promise<any>` — before, and
> after, this unit adds an explicit `Promise<Entry[]>` to
> `fetchEntries` itself. Given `any`'s own real, special, bidirectional
> assignability, what would you predict happens if the real server
> actually sent back something that isn't shaped like `Entry[]` at all
> — would `tsc` catch it at compile time? Would the real, running
> program catch it some other way? Or would nothing catch it at all?

### Project Change

- **Reference Source** — no reference counterpart, per this lesson's
  own opening note, above.
- **Files affected** — `code/05-html-css/Data/data.ts`.
- **Change type** — add (an import and a return-type annotation).
- **Location** — a new `import` line; `fetchEntries`'s own signature.
- **Dependencies** — Lesson 7.2's own real, committed `types.ts`.

### The New Code

```ts
import { Entry } from "./types.js";
```

```ts
export async function fetchEntries(): Promise<Entry[]> {
```

### The Updated Project

The complete, real, committed `code/05-html-css/Data/data.ts`:

```ts
1  import { Entry } from "./types.js";  ← new
2
3  export async function fetchEntries(): Promise<Entry[]> {  ← changed
4    const dataUrl: string = "data.json";
5    const response = await fetch(dataUrl);
6    if (!response.ok) {
7      throw new Error(`Request failed: ${response.status}`);
8    }
9    return response.json();
10 }
```

Line 1 is this unit's own new import; line 3's own new `:
Promise<Entry[]>` is the only other real change. Line 9,
`return response.json();`, is completely unchanged — still returning a
real `Promise<any>`, per this lesson's own restated Objects and methods
entry for `Response.json()`, above.

### Introduce the Concept in Isolation

This is exactly what this unit's own Socratic prompt asked — answered
with two real, separate, captured proofs, saved in
`verification/7.4/lab_no_assertion_needed.ts`,
`verification/7.4/lab_boundary_lies.ts`, and
`verification/7.4/run_output.txt`.

**Part one — no assertion is even needed:**

```ts
interface Entry {
  id: number;
}

async function withReturnType(): Promise<Entry[]> {
  const response = await fetch("x");
  return response.json();
}
```

Real output: `exit: 0` — `response.json()`'s own real `Promise<any>`
satisfies the declared `Promise<Entry[]>` with zero cast, zero
assertion, anywhere. This confirms this lesson's own Terms entry for
**`any`** directly: `any` is real, bidirectionally compatible with
every type, so a mere return-type annotation is already enough to make
`tsc` accept it — no `as` needed, because nothing is actually being
checked in the first place.

**Part two — a real, deliberate lie, and what actually happens:**

```ts
interface Entry {
  id: number;
  title: string;
  category: string;
  duration?: number;
}

async function fakeFetchJson(): Promise<any> {
  return { totally: "not an Entry" };
}

async function fetchEntries(): Promise<Entry[]> {
  return fakeFetchJson();
}

async function run() {
  const entries = await fetchEntries();
  console.log(entries[0].title.toUpperCase());
}

run();
```

Real, captured output — `tsc --noEmit --strict`:

```
exit: 0
```

**Zero real errors** — even though `fakeFetchJson`'s own real, actual
value, `{ totally: "not an Entry" }`, isn't shaped anything like an
`Entry[]`; it isn't even an array. Compiled and actually run for real
(`tsc lab_boundary_lies.ts && node lab_boundary_lies.js`):

```
TypeError: Cannot read properties of undefined (reading 'title')
    at run (...\lab_boundary_lies.js:10:28)
```

This is the real, concrete, uncomfortable answer to this unit's own
Socratic prompt: nothing catches it at compile time — `tsc`'s own real
silence, `exit: 0`, is not proof the boundary is safe, only proof
nothing at that boundary was ever actually checked. The real program
crashes the instant it runs, `entries[0]` turning out to be `undefined`
because `entries` was never really an array at all. This is called
**boundary typing**, and this real, deliberately dishonest lab proves
its own genuine, honest limit: `: Promise<Entry[]>`, written on
`fetchEntries`, is a real, human assertion about what the network will
send back — the identical real *kind* of claim Lesson 7.3's own
non-null assertion made about `entryList`, trusted, not verified,
because `any`, flowing through `response.json()`, defeats real checking
entirely the instant it's assigned to anything.

### Discard the Throwaway Example

`lab_no_assertion_needed.ts` and `lab_boundary_lies.ts` are discarded
now; the real, committed `data.ts`, shown above, carries the identical
real *kind* of unverified trust this lab just proved — honestly, this
project's own real `data.json` genuinely does match `Entry[]`, so
nothing is actually broken; this unit's own real point is that nothing
in the type system itself is what's keeping that true.

### Mechanical Walkthrough

- **`import { Entry } from "./types.js";`** — an ordinary real ES
  module import (Lesson 6.10), identical in form to Lesson 7.3's own
  `import { Entry } from "./types.js";` inside `app.ts`.
- **`export async function fetchEntries(): Promise<Entry[]> {`** — per
  this lesson's own Terms entry for **Boundary typing**: the real `:
  Promise<Entry[]>` annotation states, explicitly, what this function's
  own real, eventual value is asserted to be — checked, per this
  unit's own real, captured proof, against nothing but itself; nothing
  about the real network response this function actually awaits is
  ever inspected against it.

### CS Lens

**A type system's own real guarantees hold only as far as its own
checked boundary reaches — past that boundary, a declared type is
assertion, not proof** is a real, recurring idea well beyond this one
function.

```
Also recognized in: deserializing JSON in C# or Java (this whole
curriculum's own Phase 1/Phase 2) via a generic deserializer — the
declared target type is trusted, not verified, the identical real way,
unless a library specifically validates the real data's own actual
shape at runtime; a SQL query's own declared result-row type in almost
any real ORM, this whole curriculum's own Phase 9 still ahead; any
real API contract (a REST endpoint's own documented response shape)
that nothing on the receiving end actually enforces at runtime
```

### SE Lens

The principle is **a type annotation at an external boundary is a
claim, not a guarantee, and the difference only shows up the moment
reality disagrees with the claim** — the real alternative to this
unit's own real, trusting annotation is genuine runtime validation:
real code that actually inspects the parsed JSON's own real shape
before trusting it, rejecting or repairing anything that doesn't
match. This project's own real, honest choice, this lesson, is to skip
that — the real, concrete cost is exactly this unit's own real,
captured crash, waiting to happen the instant `data.json`'s own real
content ever drifts from what `Entry` describes, with `tsc` offering
no real warning beforehand, ever, at this exact real boundary.

### Commands Needed

`npx tsc --noEmit` to check, plain `npx tsc` to compile — identical to
every unit since Lesson 7.1.

### Run It

Verified this session — real, captured output already shown above,
saved in `verification/7.4/run_output.txt`. A real, end-to-end check —
`python -m http.server` (already running, port `8792`) plus a real
headless-Chrome `--dump-dom` — confirms all three real entries still
render correctly (full output in
`verification/7.4/dump_after_7.4.html`); this project's own real
`data.json` genuinely does match `Entry[]`, so this lesson's own real,
unverified trust happens to hold, today.

### Connect the Pieces (this unit → next)

`fetchEntries` now carries a real, explicit, honest-sounding return
type — proven, by this unit's own real, deliberately dishonest lab, to
be trusted rather than verified. Nothing about this project's own real
data has actually changed; only what's now *claimed* about it has.

---

## Connect the Pieces

Tracing this lesson's own real project change, start to finish: a real,
isolated comparison first established that `interface` and `type` are
genuinely different real tools — `interface` merges across repeated
real declarations and can only ever describe an object's own shape;
`type` never merges, and can name real things `interface` structurally
cannot, a union of literal strings among them — neither committed to
this project yet, both waiting for Lesson 7.6's own real, first genuine
need. `fetchEntries` then received this lesson's own real, central
addition: an explicit `Promise<Entry[]>` return type, replacing Lesson
7.1's own real, captured `Promise<any>`. Two real, captured proofs
followed: first, that no assertion was even needed to make `any`
satisfy that declared type — proving nothing was ever really checked;
second, a real, deliberately dishonest function, declared to return
`Promise<Entry[]>` while actually returning something else entirely,
compiled with **zero real errors** and then genuinely crashed the
instant it ran. This project's own real `data.json` still matches
`Entry`, today — but nothing in the type system itself is what makes
that true.

**Next lesson (7.5):** this lesson's own real, uncomfortable finding
was that TypeScript's own checking stops cold at an untyped boundary.
Lesson 7.5 shows the real, honest opposite case directly — a mismatch
introduced entirely *within* this project's own already, statically
typed code, with no network boundary anywhere near it, and asks whether
`tsc` catches that one instead.
