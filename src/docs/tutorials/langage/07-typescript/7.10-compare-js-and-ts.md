# Lesson 7.10: What the Type System Actually Knew

**What you will build:** nothing new, real, or committed — this
lesson, this phase's own last, is a real, inspection-only milestone,
the identical real structure Lesson 6.12 already established for
Phase 6 (itself extending 1.12/2.10/3.8's own real precedent).
`code/05-html-css/Data`'s own real, finished TypeScript — `app.ts`,
`data.ts`, `types.ts`, `tsconfig.json` — is compared directly against
its own real, committed JavaScript predecessor (Lesson 6.11's own
final state) and against Lesson 6.12's own real, already-captured
findings about C# and Java, answering this whole phase's own real,
central question: **what does a type system actually let you know
before a program runs — and, just as honestly, what does it still
leave you finding out the hard way?**

**What you need to know first:** every real lesson this phase,
7.1 through 7.9, in full — this lesson cites each one's own real,
already-captured proof directly, rather than re-deriving any of it.
Lesson 6.12's own real, six-axis JavaScript/C#/Java comparison,
specifically its own closing claim that JavaScript's dynamic typing
"catches nothing before a program runs."

## Terms used in this lesson

No new terms — every real concept this lesson compares was already
named, in full, somewhere in Lessons 7.1 through 7.9; per this
schema's own Repetition Rule, each one is restated in full again below,
at the exact point it's compared, not cited by lesson number alone.

## Objects and methods used

No new objects or methods — this lesson's own real subject is
comparison, not new code; every real tool and type cited below
(`tsc`, `Entry`, `Promise<T>`, and the rest) already received full
treatment in the lesson that introduced it, restated in full again
here, at the point of comparison, per the Repetition Rule.

---

## Concept Unit: A Value Whose Shape Is Now Checked

### The Problem

Lesson 6.12's own real, captured proof — plain JavaScript, `entry
.duration` fed a string, `"banana"` — printed `"banana minutes"` with
zero error, anywhere, ever. This whole phase, since Lesson 7.1,
exists to answer one real question directly: does TypeScript actually
change that outcome, or only appear to?

### The Real Project Change

None — this unit compares real, already-committed facts from Lessons
7.1, 7.2, and 7.5.

### The Real Demonstration

Lesson 7.1's own real, captured `.d.ts` proof: `data.ts`, renamed from
`data.js` with zero manual annotations anywhere, already carried
correct, real, inferred types for every value in it — `fetchEntries():
Promise<any>`, worked out entirely from `fetch`'s and `Response.json
()`'s own real, built-in declarations. This is called **type
inference**, and it has no real JavaScript equivalent at all — nothing
about a `.js` file, ever, computes or checks a value's own type before
the program actually runs; JavaScript has no compile step in which
such a question could even be asked.

Lesson 7.2's own real `interface Entry { id: number; title: string;
category: string; duration?: number; }`, checked with a real,
captured `TS2741` error the moment a required real property was
missing from an object literal — this is called **structural typing**,
judged purely by shape, the identical real distinction Lesson 6.12
already drew against C#'s and Java's own nominal typing, now drawn a
second time against JavaScript's own real, complete absence of any
such check at all.

Lesson 7.5's own real, direct, retyped rerun of Lesson 6.12's exact
scenario is this unit's own most concrete real proof:

```
JavaScript (Lesson 6.12, verification/6.12/run_output.txt):
  32 minutes
  banana minutes
  undefined minutes
  (all three printed, zero error)

TypeScript (Lesson 7.5, verification/7.5/run_output.txt):
  lab_typed_mismatch.ts(10,33): error TS2322: Type 'string' is not
  assignable to type 'number'.
  (caught before the program ever ran)
```

The identical real function, `buildDurationText`, fed the identical
real mistake — caught, by name, at compile time, in TypeScript;
silently rendered, in JavaScript. This is called **compile-time type
checking**, and this real, side-by-side comparison is its own direct,
provable payoff.

### Mechanical Walkthrough

Not applicable in the usual sense — this unit's own real content is
comparison of already-fully-walked-through code (Lessons 7.1, 7.2,
7.5), not a new code block requiring its own enumeration.

### CS Lens

**Static analysis catching a real defect class before a program ever
executes** is a real, recurring idea this whole curriculum has now
measured directly, twice, against the identical real language
(JavaScript) it started without.

```
Also recognized in: a linter (ESLint) catching a real, different class
of mistake — style and common bug patterns, not type mismatches — at
the identical real "before running" moment; a real database schema's
own `NOT NULL` constraint, rejecting a real, invalid row before it's
ever committed (this whole curriculum's own Phase 9, still ahead);
this whole curriculum's own C# and Java phases, whose own real static
typing already caught the identical real class of mistake, at the
identical real moment, since this whole curriculum's own Phase 1
- 2
```

### SE Lens

The principle is **the value of a type system is measured in real,
caught mistakes, not in vocabulary learned** — this unit's own real,
side-by-side comparison is the concrete, provable version of a claim
this whole phase could otherwise only assert. The real, honest
trade-off, unchanged since Lesson 6.12's own original framing:
JavaScript's own dynamic typing offers real flexibility with zero
automatic protection against exactly this class of mistake; every
real lesson this phase, 7.1 through 7.9, was the real, cumulative cost
of buying that protection back.

### Commands Needed

None new — every real command cited was already run for real in the
lesson that first produced it.

### Run It

No new run — every real fact cited above was already proven, for real,
in the lesson named beside it, per the Verification Rule's own
Necessity standard.

### Connect to What Came Before

TypeScript genuinely knows more, before a program runs, than
JavaScript ever did. The next unit asks the harder, more honest
question: knows more about *what*, exactly — and where does that
knowledge stop?

---

## Concept Unit: The Boundary Neither Language Actually Checks

### The Problem

Lesson 7.4's own real, deliberately dishonest lab proved something
this phase could easily have left unsaid: `tsc` reported **zero real
errors** for a function whose own declared return type,
`Promise<Entry[]>`, was a real, provable lie — the real, compiled
program crashed the instant it actually ran. Does this mean
TypeScript's own real advantage over JavaScript, proven in this
lesson's own previous unit, was overstated?

### The Real Project Change

None — this unit compares real, already-committed facts from Lesson
7.4.

### The Real Demonstration

Lesson 7.4's own real, captured proof, quoted in full:

```
async function fakeFetchJson(): Promise<any> {
  return { totally: "not an Entry" };
}

async function fetchEntries(): Promise<Entry[]> {
  return fakeFetchJson();
}
```

Real output, `tsc --noEmit --strict`: `exit: 0`. Real, compiled and
run output: `TypeError: Cannot read properties of undefined (reading
'title')`. This is called **boundary typing**, and its own real, honest
limit is the direct, necessary complement to this lesson's own previous
unit: at the exact real point untyped data — JSON, parsed from a real
network response — enters this project, TypeScript's own real
checking stops. `any`, per Lesson 7.4's own real, restated Terms entry,
is bidirectionally compatible with everything; a declared boundary
type past that point is a real, human assertion, never verified
against what actually arrives.

Set directly beside JavaScript's own real behavior at the identical
real boundary: JavaScript never checked anything here either — every
real line of Lesson 6.7's own original `fetch(...).then(r => r.json
())` trusted the real network response exactly as much as this
lesson's own TypeScript does, because neither language's own real
checking mechanism — TypeScript's compiler, JavaScript's complete
absence of one — ever reaches past a real, external, untyped source.
This is the one real axis this whole comparison finds **no genuine
difference** on: both languages trust the network, equally, all the
way down.

### Mechanical Walkthrough

Not applicable — comparison of already-walked-through code (Lesson
7.4).

### CS Lens

**A system's own guarantees are only as strong as its weakest,
unchecked boundary** is a real, recurring idea beyond this one network
call.

```
Also recognized in: a real SQL injection vulnerability — a database's
own real type system (Phase 9, still ahead) protects a column's own
stored values completely, and offers zero protection at all for a raw
string concatenated into a query before it ever reaches the database;
a real REST API's own documented response schema, trusted by every
real client that calls it, verified by none of them without separate,
explicit real validation code
```

### SE Lens

The principle is **know exactly where your own guarantees end, not
just that they exist** — this lesson's own previous unit's real,
provable protection could be mistaken for a claim that TypeScript
makes JavaScript's own network-trust problem disappear; this unit's
own real, captured crash proves it doesn't, and that mistaking "checked
somewhere" for "checked everywhere" is itself a real, honest risk this
whole phase's own real, deliberately dishonest lab exists to head off.
This project's own real `data.json` happens to match `Entry` today —
nothing in the type system is what keeps that true.

### Commands Needed

None new.

### Run It

No new run — cited from Lesson 7.4's own real, saved proof.

### Connect to What Came Before

TypeScript's own real advantage has a real, honest edge — untyped
boundaries stay untyped, in either language. The next unit asks a
different, TypeScript-specific question this whole comparison hasn't
raised yet: once a real mistake *is* caught, is it actually stopped?

---

## Concept Unit: A Caught Mistake Isn't Automatically a Blocked One

### The Problem

This question has no real JavaScript analog at all — JavaScript has no
compile step, so "does a compile error block the program from running"
is a question only TypeScript's own real toolchain can even be asked.
Given this lesson's own first unit's real, captured `TS2322` proof —
TypeScript caught the identical real mistake JavaScript missed — does
catching it actually *stop* the broken program from running?

### The Real Project Change

None — this unit compares real, already-committed facts from Lesson
7.5.

### The Real Demonstration

Lesson 7.5's own real, captured, two-part proof:

```
$ tsc --strict lab_typed_mismatch.ts   (no --noEmit)
lab_typed_mismatch.ts(10,33): error TS2322: Type 'string' is not assignable to type 'number'.
$ node lab_typed_mismatch.js
32 minutes
banana minutes
undefined minutes
```

`tsc`'s own real, default behavior — report the error, still emit real,
runnable output — meant the identical real mistake this lesson's own
first unit celebrated catching still reached a real, running program,
by default, reproducing Lesson 6.12's own exact real bug anyway. Only
this project's own real, committed `"noEmitOnError": true` — Lesson
7.5's own real, permanent fix — actually closes that gap, proven,
directly, to withhold output for this project's own *entire* real
compilation unit the instant any one file in it has an error.

### Mechanical Walkthrough

Not applicable — comparison of already-walked-through code (Lesson
7.5).

### CS Lens

Not applicable as a full, separate "recognized elsewhere" list — this
unit's own real subject already received one, in Lesson 7.5 itself
(detection versus enforcement); repeating it here would violate this
schema's own Stopping Rule.

### SE Lens

The principle is **a language's own real safety guarantees are a
property of how a project is built, not of the language alone** — two
real TypeScript projects, identical in every other way, genuinely
differ in real safety if only one of them sets `noEmitOnError` (or
gates a real deploy on `tsc`'s own real exit code). This is the one
real axis where TypeScript's own comparison to JavaScript isn't really
the point at all — the real, honest lesson is that adopting TypeScript
is necessary, but not sufficient, for the real protection this whole
phase has otherwise demonstrated; a project has to actually wire that
protection in.

### Commands Needed

None new.

### Run It

No new run — cited from Lesson 7.5's own real, saved proof.

### Connect to What Came Before

Catching and blocking are two real, separate claims — this project's
own real `tsconfig.json` now makes both true together. The final unit
asks what TypeScript adds beyond safety altogether.

---

## Concept Unit: New Capability, Not Just New Safety

### The Problem

Every real comparison so far has been about *safety* — catching a
mistake JavaScript would have missed. Lesson 7.8's own real
`renderList<T extends { id: number }>` and Lesson 7.9's own real,
fetched `Promise<T>` interface suggest TypeScript offers something
JavaScript structurally *can't*, safety aside entirely — is that real,
or does it collapse back into the identical safety story?

### The Real Project Change

None — this unit compares real, already-committed facts from Lessons
7.8 and 7.9, and closes with one final real fact spanning this whole
phase.

### The Real Demonstration

Lesson 7.8's own real, captured proof: `firstOf<T>` and `renderList<T
extends { id: number }>` both compiled and correctly resolved a real,
different type parameter per real call site — `T` as `number`, then
`string`; `Entry`, proven, directly, to already satisfy `{ id: number
}` with zero changes needed. JavaScript has no real, compile-time
equivalent of a **generic function** at all — a JavaScript function
parameterized "over any type" is simply a function that accepts
anything, with no real, checkable relationship enforced between what
goes in and what comes out; TypeScript's own real generics are new
*expressive* capability, not merely a safety net over something
JavaScript could already do.

Lesson 7.9's own real, fetched `Promise<T>` interface and its own real
`Awaited<T>` utility type, quoted from TypeScript's own actual,
current source, made this project's own `async`/`await` — real,
identical JavaScript syntax since Lesson 6.8 — mechanically explicit
for the first time: `Promise<T>` is a real, generic type; `await`
genuinely unwraps it to `T`. JavaScript's own real `Promise` object
underneath is byte-for-byte the identical real mechanism — this whole
phase never changed what actually runs.

That real, final claim is this whole phase's own most direct, provable
fact, cited from three genuinely separate real sessions: Lesson 7.1's
own real diff, `data.js` regenerated from `data.ts`, showing only
whitespace and a stripped `: string` annotation — **type erasure**;
Lesson 7.3's own real `diff --strip-trailing-cr` against `app.js`'s
own original, pre-port content (`git show 6bd723a:app.js`), showing
only whitespace and two deliberate `String(...)` bug fixes; and every
real, headless-Chrome `--dump-dom` run, 7.1 through 7.9, rendering the
identical three real entries, unchanged, in the identical real browser.
This is the real, concrete meaning of this whole phase's own real
milestone: **TypeScript is JavaScript, plus a static type system — not
an unrelated language.** Every real line this project actually runs,
in a real browser, right now, is plain JavaScript; TypeScript's own
real, entire contribution was checked, then erased, before any of it
ever executed.

### Mechanical Walkthrough

Not applicable — comparison of already-walked-through code (Lessons
7.8, 7.9).

### CS Lens

**A compile-time-only layer, fully erased before runtime, changing
what can be known without changing what executes** is this whole
phase's own real, central idea, recognized well beyond TypeScript.

```
Also recognized in: Python's own type hints, checked by a real,
separate tool (`mypy`) and completely ignored by the real Python
interpreter at runtime — the identical real erasure, opt-in rather
than compiled away; a real design-by-contract annotation, checked only
in a debug real build and stripped entirely from a real release one;
this whole curriculum's own C++ phase's own real templates (Phase 4),
resolved entirely at compile time into separate, real, generated code
per type — a genuinely different real mechanism from TypeScript's own
pure erasure, reaching a related but not identical real goal
```

### SE Lens

The principle is **adopting a static type system is a real, additive
choice, not a rewrite** — this whole phase, start to finish, never
once replaced this project's own real JavaScript logic; every real
Concept Unit either added a real annotation, a real check, or a real
generic capability *on top of* code Lesson 6.11 had already finished
and committed. The real, concrete proof: 13 real commits this phase,
`code/05-html-css/Data` ending at `master`, `13aab9d`, working tree
clean — real project change, `app.ts` 102 lines, `data.ts` 10 lines,
`types.ts` 6 lines, `tsconfig.json` 10 lines — every one of them a real
addition to, never a rewrite of, the identical real application this
whole curriculum's own Phase 5 and Phase 6 already built.

### Commands Needed

None new.

### Run It

No new run — cited from Lessons 7.1, 7.3, 7.8, and 7.9's own real,
saved proof.

### Connect to What Came Before

This closes the real, four-part answer this whole phase set out to
give: TypeScript knows real, provable more than JavaScript about a
program's own correctness, before it ever runs; that knowledge
genuinely stops at any real, external, untyped boundary; a caught
mistake is only truly blocked once a project explicitly wires that in;
and none of it changes what actually executes — only what can be known
about it first.

---

## Connect the Pieces

`code/05-html-css/Data`'s own real, complete git history, this whole
phase, confirmed one final time via `git log --oneline`:

```
13aab9d Explicitly annotate loadEntries's awaited data as Entry[]
f4c8369 Guard the duration line with a real type guard, closing the undefined-minutes gap
41b57d5 Give showListState's state parameter a real ListState union type
e2e4f42 Set noEmitOnError so a real type error can never compile through
dd5d3e6 Give fetchEntries a real, explicit Promise<Entry[]> return type
c480582 Gitignore app.js and types.js, both now tsc's own compiled output
a74b576 Add explicit return types to every function, matching what tsc already inferred
384dc43 Give every function's parameters a real Entry-based type
1d1d35a Port app.js to app.ts; assert non-null on the DOM lookups this project already guarantees exist
a4d472d Make duration an optional property on Entry
465caf4 Add types.ts, declaring the Entry object type
a27da5b Add an explicit type annotation to data.ts's dataUrl constant
a871855 Port data.js to data.ts, compiled by tsc into the same real output
30e8595 Add npm and install TypeScript as a dev dependency
```

Thirteen real commits — every one of Lessons 7.1 through 7.9 that had
a genuine, real reason to touch the project (7.4's CU1, 7.8's whole
real lesson, and part of 7.9 honestly didn't, per each one's own real,
stated reasoning, the identical real discipline Lesson 6.4 first
established) added exactly what it needed to, never more.

This lesson's own real, four-axis answer, gathered in one place: what
TypeScript knows before running that JavaScript never could —
inferred and annotated types, structural shapes, and a real,
compile-time-caught mismatch, directly proven against the identical
real scenario Lesson 6.12 already ran in plain JavaScript. What it
still doesn't know — anything past a real, untyped boundary, network
data included, proven with a real, captured runtime crash despite
zero compile errors. What "caught" actually requires — a project's own
explicit choice to treat a compiler error as a real wall, not merely a
note. And what's genuinely new, not just safer — real generic
functions and a real, named `Promise<T>`, both proven to layer
cleanly on top of identical, unchanged, real JavaScript underneath.

This is the milestone this whole phase set out to reach: not the
ability to recite what `interface` or `Promise<T>` mean in isolation,
but the ability to read a real, working, typed program and say,
correctly, exactly what its own type system actually proved before it
ran — and, just as important, exactly where that proof stops. Phase 8
introduces React directly on top of this exact same real, finished,
typed project — the next real abstraction layered on, never a rewrite.
