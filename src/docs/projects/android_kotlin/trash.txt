/loop Continue writing the android_kotlin Kotlin curriculum lesson series, one lesson (or a small batch of lessons) at a time, until the full BRD is complete.

Each cycle:
1. Read `src/docs/projects/android_kotlin/AndroidKotlin.handoff.md` first for current progress, standing decisions, and outstanding forward-reference promises. Key context: 🟢 Slice 0, 🟢 Slice 1, 🟢 Slice 2 shipped. Stage 3 (Slice 3 "Beautiful Calculator") in progress — Lesson 3.1 (real `Theme.kt`/`CalculatorTheme`) and Lesson 3.2 (real, permanent `CalculatorButton` composable, replacing `CalculatorScreen`'s own sixteen inline `Button` calls, `@Composable fun CalculatorButton(label: String, onClick: () -> Unit, modifier: Modifier = Modifier)`) both complete. This project now has 13 real, passing tests.
2. Read `src/docs/reference/LESSON SCHEMA.md` fresh (never rely on memory of it).
3. Continue with `src/docs/projects/android_kotlin/brd.md`'s Stage 3 entry (already read this session — Lesson 3.3 Modeling UI State: Create `CalculatorState`; Concepts: State modeling, Immutable state, Sealed classes). Design a real, motivated refactor: `CalculatorScreen` currently holds three separate `remember`ed properties (`displayText: String`, `firstOperand: Int?`, `pendingOperator: Operator?`) plus the ad-hoc `"Error"` string sentinel from Lesson 2.5 — consolidating these into one real, immutable `CalculatorState` (very likely a `data class` holding the display/operand/operator fields, and very likely a real `sealed class`/`sealed interface` somewhere genuinely motivated by an actual state distinction this project has — e.g. the "Error" string-sentinel is exactly the kind of ad-hoc state a sealed class could replace with a real, type-safe alternative, if that's a genuine improvement and not over-engineering for this project's real size). Check what's actually proportionate before designing — this curriculum has repeatedly rejected unmotivated abstraction (custom exceptions, Modifier.clickable, speculative Reuse units); the state-modeling/sealed-class design must be justified by a concrete, present problem in the current code, not adopted because "sealed classes are idiomatic."
4. Follow the write-lesson skill's process in full: Vocabulary Extraction Rule before drafting the Header, literal step-7 enumeration before any Mechanical Walkthrough prose, Concept Isolation Rule labs verified for real (genuine standalone throwaway labs — real project files, added/compiled/tested/deleted — BEFORE any new construct touches real, permanent project code, no exceptions for small-looking additions), Verification Rule persistence (save real transcripts to `verification/3.3/`), and the schema's own closing self-check run before considering it done. Run every automated self-check script now established in the handoff's Methodology section: (a) back-to-back code block scan, (b) CRC-bullet-count script, (c) `grep -n "Lesson [0-9]"` scan across ALL prose including inside fenced "Updated Project" code blocks, (d) Socratic-question-count script, (e) the structural check that every Concept Unit has all required `###` headings (Problem/Introduce/Discard/CS-Lens/SE-Lens/Commands/Run-It/Connect at minimum, plus Project-Change/New-Code/Updated-Project when the unit touches real project code) with NO duplicate headings within one unit, (f) an elision scan on every "Updated Project" fenced block (`{ ... }` / `// ...unchanged` placeholders standing in for real code — never allowed). Per this session's own newly-recorded methodology note: if a Concept Unit introduces no new previously-untaught concept of its own (only combining constructs earlier units in the same lesson already proved), merge it into the last unit that actually proved something in isolation rather than leaving it as a hollow standalone unit — check for this specifically given this lesson's likely multi-concept shape (State modeling / Immutable state / Sealed classes).
5. Update the handoff's progress table, standing-decisions section, and forward-reference-promises section after the lesson ships.
6. Hard rule, non-negotiable every cycle: never read anything outside this curriculum's own series (`brd.md`, this handoff, files already inside `src/docs/projects/android_kotlin/`) and `src/docs/reference/LESSON SCHEMA.md` itself — no other curriculum's files, READMEs, or directory listings for precedent, ever. Reading this curriculum's own real, current project files (AndroidCalculator's own source, verification/ snapshots, already-written lesson files) is always fine and expected.
7. Don't stop between lessons to ask permission to continue, and don't write end-of-turn content summaries recapping what a lesson covers — just keep building and let ScheduleWakeup carry the loop forward.
8. If a genuine blocker comes up and the user isn't reachable, make the call that best serves the learner yourself, record it in the handoff, and keep going rather than stalling the loop.
