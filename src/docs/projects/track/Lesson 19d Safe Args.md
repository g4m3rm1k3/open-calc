# Lesson 19d: Safe Args

**What you will build:** No new code to compile — this reads real
Android code directly.

**What you need to know first:** Lesson 19c's Navigation Graph.

**Terms introduced in this lesson:**

- **Safe Args** — a build-time plugin generating typed "Directions"/
  "Args" classes per navigation destination from the graph's declared
  arguments, replacing string-keyed Intent extras with compiler-checked
  method calls.

---

## Concept Unit: Safe Args

### The Problem

Every hand-built `Intent`/`putExtra` pair between screens is Lesson 7c's
own "parallel state" risk relocated to Activity/Fragment boundaries — a
missing or wrong-typed argument becomes a runtime `null` or
`ClassCastException` the moment a mistyped extra key goes unnoticed,
rather than a genuine compile error.

### Introduce the Concept in Isolation

This is not a throwaway lab — it's real Android code, verified against
the actual framework source:

```java
// Generated by Safe Args, from the navigation graph's own action:
ItemListFragmentDirections.ActionToDetail action =
    ItemListFragmentDirections.actionToDetail(itemId);
Navigation.findNavController(view).navigate(action);
```

This is `Safe Args` — **first appearance**: a build-time plugin
generating typed "Directions"/"Args" classes per navigation destination
from the graph's declared arguments, replacing string-keyed Intent
extras with compiler-checked method calls.
`ItemListFragmentDirections.actionToDetail(itemId)` is a real, generated
method — its parameter list is generated directly from the navigation
graph's own declared arguments (Lesson 19c), so a missing or
wrong-typed argument fails to *compile* at all, rather than surfacing as
a runtime crash.

### Discard the Throwaway Example

Nothing here is a throwaway lab to discard — this is real, verified
Android code.

### Mechanical Walkthrough

1. `ItemListFragmentDirections` — **(a) first appearance**: a real class
   generated at build time, one per destination declared in the
   navigation graph.
2. `.actionToDetail(itemId)` — **(a) first appearance**: a generated
   method whose parameter list matches exactly whatever arguments the
   navigation graph's own `<action>` declares — a wrong type or a missing
   argument fails to compile.
3. `Navigation.findNavController(view).navigate(action);` — **(a) first
   appearance**: performs the actual navigation using the generated,
   already-validated `action` object.

### CS Lens

Safe Args generates real, inspectable code from the navigation graph's
own declared data — directly connecting Lesson 19c's navigation graph to
a compile-checked API, the same way any build-time code generator turns
declared data into typed source files.

Also recognized in: typed routing libraries generating compile-checked
navigation APIs from a declared route table, across virtually every
modern web and mobile framework with a similar code-generation step.

### SE Lens

The alternative — hand-built `Intent().putExtra("EXTRA_ITEM_ID",
itemId)` pairs — was not chosen going forward because nothing then
checks that the sending and receiving sides agree on the key's name or
the value's type; Safe Args generates both sides from the same single
source (the navigation graph), making a mismatch a compile error instead
of a runtime bug.

---

## Connect the Pieces

Lesson 19a's directed graph is the precise shape of an app's own screen
topology. Lesson 19b's Single-Activity Architecture collapses every
screen into Fragments hosted by one Activity. Lesson 19c's navigation
graph declares that shape as real, XML data. And Safe Args reads that
same data at build time and generates a compile-checked API, replacing
hand-built `Intent`/`putExtra` pairs with typed method calls.

## What Breaks Without This

A missing or wrong-typed navigation argument, without Safe Args,
surfaces as a runtime `null` or crash, discovered only when that specific
path is actually exercised, rather than as a compile error caught
immediately.

## Exercises

1. Add a second `<action>` to the navigation graph's own `itemListFragment`
   node, and explain, in your own words, what new Safe Args class or
   method you'd expect to be generated for it.
2. Explain, in your own words, why a missing Safe Args argument is a
   compile error, while a missing hand-built `Intent` extra is only a
   runtime problem.
3. Explain, in your own words, why `ItemListFragmentDirections` is
   generated per destination, rather than one single class for the
   entire graph.

## Definition of Done

- [ ] You read the real Safe Args example and can explain why a
      mismatched argument fails to compile.
- [ ] You completed Exercise 2.
- [ ] You can state, without looking back at this lesson, what Safe Args
      generates its typed classes from.
