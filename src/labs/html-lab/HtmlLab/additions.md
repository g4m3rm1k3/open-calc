linear gradient default options
body resets no padding no margin its called css reset
nav when I click on it it take me away and I lose everything
need an export so students can save thier work etc
container has not color themes to match the rest of the styled comopnents
background images etc for everything with the tint and stuff like that
when element is selected such as div or contiainer that becomes the target for the new element/component added
