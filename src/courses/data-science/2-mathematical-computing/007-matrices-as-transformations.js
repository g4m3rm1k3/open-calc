export default {
  id:'b-07',slug:'matrices-as-transformations',track:'B',order:7,
  title:'Matrices as Transformations',subtitle:'What Matrix Multiplication Actually Does',
  tags:['matrix', 'transformation', 'determinant', 'composition', 'numpy', 'linear-algebra'],prereqs:['b-05', 'b-06'],unlocks:['b-08', 'd-02'],
  hook:{question:'Why is a matrix multiplication a transformation of space?',realWorldContext:'Matrix multiplication is not just bookkeeping. It is applying a transformation to a vector — rotating it, scaling it, shearing it, projecting it. Understanding this visually makes every linear algebra theorem obvious instead of opaque.'},
  intuition:{
    prose:['A 2×2 matrix [[a,b],[c,d]] encodes a transformation: it shows where î=[1,0] and ĵ=[0,1] land. î → [a,b]. ĵ → [c,d]. Every other vector follows from linearity.', 'Matrix-vector multiplication Av: the result is a[column 1] + v[0] * column_1 + v[1] * column_2. It is a **linear combination of the matrix columns** with the vector components as coefficients.', 'The **determinant** is the area scaling factor. det=2 means the transformation doubles every area. det=0 means space is squashed to a lower dimension — the transformation is not invertible.'],
    callouts:[{type:'important',title:'The Column View of Matrix Multiplication',body:'A = [[2, 0], [0, 3]], v = [x, y]\n\nAv = x * [2,0] + y * [0,3]  (linear combination of columns)\n   = [2x, 3y]\n\nColumn 1 tells you where x-component goes.\nColumn 2 tells you where y-component goes.'}],
    visualizations:[{id:'PythonNotebook',title:'Matrices as Transformations',
      props:{initialCells:[{id:1,cellTitle:'Stage 1 — Matrix-Vector Multiplication',prose:'A @ v applies the transformation to vector v.',instructions:'Run. Then try different matrices and observe the result.',code:'import numpy as np\nA = np.array([[2,0],[0,3]])\nv = np.array([1.0, 1.0])\nresult = A @ v\nprint(f"A @ v = {result}")',output:'',status:'idle'},
        {id:2,cellTitle:'Stage 2 — Visualizing Transformations',prose:'The matrix transforms the grid. The columns show where î and ĵ land.',instructions:'Run. Try the rotation matrix [[0,-1],[1,0]].',code:'from opencalc import Figure\nimport numpy as np\n\nA = np.array([[1.5, 0.5], [0.0, 1.5]])\nfig = Figure(square=True, xmin=-5, xmax=5, ymin=-5, ymax=5)\nfig.grid()\nfig.transformed_grid(A.tolist())\nfig.vector(A[:,0].tolist(), color="red", label="î→")\nfig.vector(A[:,1].tolist(), color="green", label="ĵ→")\nfig.show()',output:'',status:'idle'},
        {id:3,cellTitle:'Stage 3 — Matrix Multiplication as Composition',prose:'A @ B applies B first, then A. Order matters: AB ≠ BA in general.',instructions:'Run. Swap the order and verify the result changes.',code:'import numpy as np\nrotate = np.array([[0,-1],[1,0]])\nscale = np.array([[2,0],[0,2]])\nprint("Rotate then scale:")\nprint(scale @ rotate)\nprint("Scale then rotate:")\nprint(rotate @ scale)',output:'',status:'idle'},
        {id:4,cellTitle:'Stage 4 — Determinant',prose:'det(A) = area scaling. det=0 means the transformation collapses space.',instructions:'Run. Try the singular matrix [[1,2],[2,4]] — its det is 0.',code:'import numpy as np\nA = np.array([[2.0,1],[0,3]])\nprint(f"det(A) = {np.linalg.det(A):.4f}")\nprint("A scales area by this factor")\n# Singular matrix:\nB = np.array([[1.0,2],[2,4]])\nprint(f"det(B) = {np.linalg.det(B):.4f}")',output:'',status:'idle'},
        {id:11,challengeType:'write',challengeNumber:1,challengeTitle:'Challenge 1 — Rotation Verification',difficulty:'medium',prompt:'The 45° rotation matrix is [[cos45,-sin45],[sin45,cos45]]. Apply it to [1,0] and verify the result equals [cos45, sin45]. Store the result in rotated.',instructions:'1. import math. 2. Build the rotation matrix. 3. Apply to [1,0]. 4. Verify.',code:'import numpy as np, math\nangle = math.pi / 4  # 45 degrees\nR = np.array([[math.cos(angle), -math.sin(angle)],\n              [math.sin(angle),  math.cos(angle)]])\nv = np.array([1.0, 0.0])\nrotated = ',output:'',status:'idle',testCode:'\nimport numpy as np,math\nexpected=np.array([math.cos(math.pi/4),math.sin(math.pi/4)])\nif not np.allclose(rotated,expected,atol=1e-6): raise ValueError(f"Expected {expected}, got {rotated}")\nres=f"SUCCESS: R @ [1,0] = [{rotated[0]:.4f}, {rotated[1]:.4f}] = [cos45°, sin45°]."\nres\n',hint:'rotated = R @ v'}]}
    }],
  },
  mentalModel:['Matrix columns show where î and ĵ land under the transformation.', 'A @ v = linear combination of columns of A, with v as coefficients.', 'A @ B: apply B first, then A. AB ≠ BA in general.', 'det(A): area scaling factor. det=0: transformation collapses space (not invertible).', 'np.linalg.det(), np.linalg.solve(), @ for matrix multiply.'],
}