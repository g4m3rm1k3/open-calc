const sim2_006 = {
  id: 'sim2-006',
  slug: 'unit-circle',
  chapter: 'sim2',
  order: 6,
  title: 'Unit Circle',
  subtitle: 'Use requestAnimationFrame to animate a point tracing the unit circle, and watch how cosine and sine emerge as the x and y coordinates of that circular motion.',
  tags: ['unit circle', 'trigonometry', 'parametric', 'animation', 'canvas', 'coordinate transform'],
  timeToComplete: 30,
  coreConcept: 'cos(θ) and sin(θ) are the x and y coordinates of a point on the unit circle at angle θ. The "wave" shapes of the sine and cosine graphs are the projections of circular motion onto each axis — watch the animated dot\'s shadow on the y-axis and you have literally drawn a sine wave.',

  hook: {
    question: 'You have seen the sine wave on oscilloscopes and audio visualisers. Where does that shape actually come from — why is it curved like that, and not some other wave?',
    realWorldContext: 'Every rotating motor, every AC power line, every speaker cone, and every GPS satellite orbit uses the unit circle. The sine wave on your oscilloscope is not a coincidence — it is the direct mathematical shadow of circular motion. AC electricity is generated by spinning a coil in a magnetic field; the voltage at any instant is sin(ωt), the y-projection of that spin. This lesson gives you the geometric foundation that makes trigonometry physical, not just symbolic.',
  },

  intuition: {
    prose: [
      '**The unit circle is a coordinate machine.** Place a circle of radius 1 at the origin. Every point on that circle satisfies x² + y² = 1. Pick any angle θ (measured counter-clockwise from the positive x-axis) and there is exactly one point on the circle at that angle: (cos θ, sin θ). This is not a definition to memorise — it is *how* cos and sin are defined geometrically.',
      '**Mapping math coordinates to canvas pixels.** The canvas counts pixels from the top-left; math coordinates have the origin at the centre with y increasing upward. The bridge is a linear map: pick a canvas pixel as your origin (cx, cy) and a scale R (pixels per math unit). Any math point (x, y) maps to `px = cx + x * R` and `py = cy − y * R`. The minus sign on y is the only non-obvious step — math y grows up, canvas y grows down.',
      '**Animation by incrementing θ each frame.** The rAF loop from Lesson 5 delivers a delta-time `dt` each frame. Adding `dt` to θ moves the point counter-clockwise at one radian per second. Clear the canvas, recompute cos(θ) and sin(θ), redraw. Nothing else changes — the point appears to move because each frame paints it at a slightly different angle.',
      '**Projections reveal the wave.** Draw a dashed vertical line from the moving point straight down to the x-axis: the landing spot is cos(θ). Draw a dashed horizontal line left to the y-axis: the landing spot is sin(θ). Watch those spots oscillate as the point rotates. A sine wave recorded over time is literally the y-projection of this circular motion — nothing more.',
      '**Angular frequency and period.** If θ increases at 1 radian/second the point takes 2π ≈ 6.28 s for one full revolution. Double the speed (θ += 2·dt) and it takes 3.14 s — frequency doubles, period halves. The parameter ω in sin(ωt) is angular frequency in radians/second. Frequency in Hz is ω / (2π).',
    ],
    callouts: [
      {
        type: 'sequencing',
        title: 'Lesson 6 of 10 — Unit Circle',
        body: '**Previous:** Lesson 5 — Animation Loop. The `requestAnimationFrame` and delta-time `dt` patterns are used here unchanged.\n**This lesson:** Parametric curves — one parameter `t` drives both x and y.\n**Next:** Lesson 7 — Multiple Functions. Multiple traces with distinct colors and a live legend.',
      },
      {
        type: 'definition',
        title: 'Unit Circle',
        body: 'The unit circle is the set of all points (x, y) with x² + y² = 1. For any angle θ measured counter-clockwise from the positive x-axis, the point on the unit circle is **(cos θ, sin θ)**. This is the geometric definition of cosine and sine — not a ratio of triangle sides, but a coordinate on a circle.',
      },
      {
        type: 'warning',
        title: 'Canvas Y Is Flipped',
        body: 'Canvas y = 0 is at the top; math y = 0 is at the bottom.\n\n`py = cy − sin(θ) × R`  ← correct\n`py = cy + sin(θ) × R`  ← draws the circle upside-down\n\nAlways subtract the math-y value when converting to canvas coordinates.',
      },
      {
        type: 'procedure',
        title: 'Coordinate Transform (Math → Canvas)',
        body: 'Choose origin pixel `(cx, cy)` and scale `R` (pixels per unit):\n\n```\npx = cx + mathX * R\npy = cy − mathY * R\n```\n\nGood default: `R = Math.min(W, H) * 0.38` — leaves margin around a radius-1 circle.',
      },
      {
        type: 'insight',
        title: 'Pythagorean Identity from the Picture',
        body: 'Because the point always stays on the unit circle, cos²θ + sin²θ = 1 for all θ. Verify numerically: `Math.cos(theta)**2 + Math.sin(theta)**2` is always exactly 1 (floating-point permitting). The identity is not a theorem to prove — it is the definition of the unit circle, stated algebraically.',
      },
      {
        type: 'strategy',
        title: 'Constants Outside the Loop',
        body: 'Compute `cx`, `cy`, and `R` once before starting `requestAnimationFrame`. They depend only on canvas size, which does not change frame-to-frame. Only `getColors()` belongs inside the loop — the user might toggle dark/light mode while the animation runs.',
      },
    ],
    visualizations: [
      {
        id: 'SimNotebook',
        title: 'Unit Circle — Four Cells',
        mathBridge: 'Work through the four cells in order. Cell 1 establishes the coordinate mapping with a static picture. Cell 2 animates the point. Cell 3 adds the projection lines showing where sin and cos come from. Cell 4 builds the full interactive widget with an angle slider.',
        initialProps: {
          initialCells: [
            {
              id: 1,
              mode: 'html',
              cellTitle: 'Coordinate Mapping',
              prose: [
                'Before animating, nail the static picture. Place the origin at the canvas centre, choose scale R, and confirm that the point at θ = π/4 (45°) lands at (0.707, 0.707).',
                'The dashed lines from the point to each axis are the "shadows": cos(θ) is the x-landing and sin(θ) is the y-landing. All four cells build on this diagram.',
              ],
              code: `app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:8px;overflow:hidden}
    h3{font-size:13px;font-weight:600;color:var(--text);margin:0;flex-shrink:0}
    canvas{flex:1;min-height:0;width:100%;display:block;border-radius:4px;background:var(--surface)}
    .note{font-size:12px;color:var(--muted);flex-shrink:0}
  </style>
  <div class="wrap">
    <h3>Unit Circle — coordinate mapping</h3>
    <canvas id="cv"></canvas>
    <p class="note">θ = π/4 = 0.785 rad = 45°   point: (cos θ, sin θ) = (0.707, 0.707)</p>
  </div>
\`
const cv  = app.querySelector('#cv')
cv.width  = cv.clientWidth  || 440
cv.height = cv.clientHeight || 280
const ctx = cv.getContext('2d')
const W = cv.width, H = cv.height
const cx = W / 2, cy = H / 2
const R  = Math.min(W, H) * 0.38

function getC() {
  const s = getComputedStyle(document.documentElement)
  const g = v => s.getPropertyValue(v).trim() || '#888'
  return { text:g('--text'), muted:g('--muted'), accent:g('--accent'), border:g('--border') }
}
const C = getC()

const theta = Math.PI / 4
const cosT  = Math.cos(theta), sinT = Math.sin(theta)
const px    = cx + cosT * R,   py   = cy - sinT * R

ctx.strokeStyle = C.muted; ctx.lineWidth = 1
ctx.beginPath(); ctx.moveTo(0,cy); ctx.lineTo(W,cy); ctx.stroke()
ctx.beginPath(); ctx.moveTo(cx,0); ctx.lineTo(cx,H); ctx.stroke()

ctx.fillStyle = C.muted; ctx.font = '11px system-ui'; ctx.textAlign = 'center'
ctx.fillText('1', cx+R, cy+16); ctx.fillText('−1', cx-R, cy+16)
ctx.textAlign = 'right'
ctx.fillText('1', cx-5, cy-R+4); ctx.fillText('−1', cx-5, cy+R+4)

ctx.strokeStyle = C.border; ctx.lineWidth = 2
ctx.beginPath(); ctx.arc(cx, cy, R, 0, 2*Math.PI); ctx.stroke()

ctx.setLineDash([5,4]); ctx.strokeStyle = C.muted; ctx.lineWidth = 1.5
ctx.beginPath(); ctx.moveTo(px,py); ctx.lineTo(px,cy); ctx.stroke()
ctx.beginPath(); ctx.moveTo(px,py); ctx.lineTo(cx,py); ctx.stroke()
ctx.setLineDash([])

ctx.fillStyle = C.accent; ctx.font = 'bold 11px system-ui'
ctx.textAlign = 'center'; ctx.fillText('cos θ', px, cy+16)
ctx.textAlign = 'right';  ctx.fillText('sin θ', cx-6, py+4)

ctx.strokeStyle = C.accent; ctx.lineWidth = 2
ctx.beginPath(); ctx.moveTo(cx,cy); ctx.lineTo(px,py); ctx.stroke()
ctx.fillStyle = C.accent; ctx.beginPath(); ctx.arc(px,py,6,0,2*Math.PI); ctx.fill()

ctx.fillStyle = C.text; ctx.font = '12px system-ui'; ctx.textAlign = 'left'
ctx.fillText('('+cosT.toFixed(3)+', '+sinT.toFixed(3)+')', px+10, py-8)`,
            },
            {
              id: 2,
              mode: 'html',
              cellTitle: 'Animating the Point',
              prose: [
                'Add `requestAnimationFrame` from Lesson 5. Each frame, increment `theta` by `dt`. At 1 radian/second the point takes ~6.28 s per revolution.',
                'The loop reads current state, clears the canvas, draws, and schedules the next frame. Nothing carries over between frames — it is always a complete redraw.',
              ],
              code: `app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:8px;overflow:hidden}
    .bar{display:flex;gap:8px;align-items:center;flex-shrink:0}
    button{padding:5px 14px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);color:var(--text);cursor:pointer;font-size:13px}
    button:hover{background:var(--accent-bg)}
    span{font-size:12px;color:var(--muted)}
    canvas{flex:1;min-height:0;width:100%;display:block;background:var(--surface);border-radius:4px}
  </style>
  <div class="wrap">
    <div class="bar">
      <button id="btn">▶ Play</button>
      <span id="out">θ = 0.000 rad</span>
    </div>
    <canvas id="cv"></canvas>
  </div>
\`
const cv  = app.querySelector('#cv')
cv.width  = cv.clientWidth  || 440
cv.height = cv.clientHeight || 280
const ctx = cv.getContext('2d')
const W = cv.width, H = cv.height
const cx = W/2, cy = H/2
const R  = Math.min(W,H) * 0.38

function getC() {
  const s = getComputedStyle(document.documentElement)
  const g = v => s.getPropertyValue(v).trim() || '#888'
  return { text:g('--text'), muted:g('--muted'), accent:g('--accent'), border:g('--border') }
}

let animId=null, running=false, theta=0, lastTs=null

function draw(th) {
  const C = getC()
  const cosT=Math.cos(th), sinT=Math.sin(th)
  const px=cx+cosT*R, py=cy-sinT*R
  ctx.clearRect(0,0,W,H)
  ctx.strokeStyle=C.muted; ctx.lineWidth=1
  ctx.beginPath(); ctx.moveTo(0,cy); ctx.lineTo(W,cy); ctx.stroke()
  ctx.beginPath(); ctx.moveTo(cx,0); ctx.lineTo(cx,H); ctx.stroke()
  ctx.strokeStyle=C.border; ctx.lineWidth=2
  ctx.beginPath(); ctx.arc(cx,cy,R,0,2*Math.PI); ctx.stroke()
  ctx.strokeStyle=C.accent; ctx.lineWidth=2
  ctx.beginPath(); ctx.moveTo(cx,cy); ctx.lineTo(px,py); ctx.stroke()
  ctx.fillStyle=C.accent; ctx.beginPath(); ctx.arc(px,py,6,0,2*Math.PI); ctx.fill()
  ctx.fillStyle=C.text; ctx.font='12px system-ui'; ctx.textAlign='left'
  ctx.fillText('('+cosT.toFixed(3)+', '+sinT.toFixed(3)+')', px+10, py-8)
  app.querySelector('#out').textContent = 'θ = '+th.toFixed(3)+' rad'
}

function loop(ts) {
  const dt = lastTs ? Math.min((ts-lastTs)/1000, 0.05) : 0
  lastTs=ts; theta=(theta+dt)%(2*Math.PI)
  draw(theta); animId=requestAnimationFrame(loop)
}
draw(0)

app.querySelector('#btn').addEventListener('click', () => {
  if (running) {
    cancelAnimationFrame(animId); animId=null; running=false; lastTs=null
    app.querySelector('#btn').textContent='▶ Play'
  } else {
    running=true; app.querySelector('#btn').textContent='⏸ Pause'
    animId=requestAnimationFrame(loop)
  }
})`,
            },
            {
              id: 3,
              mode: 'html',
              cellTitle: 'Projections — Where Sin and Cos Live',
              prose: [
                'Add dashed projection lines: the vertical drop lands at x = cos(θ) on the x-axis; the horizontal line lands at y = sin(θ) on the y-axis. Those landing spots oscillate — that oscillation IS the sine and cosine function.',
                'Toggle dark mode while the animation runs. Because `getColors()` is called inside the loop, the projection lines update instantly.',
              ],
              code: `app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:6px;overflow:hidden}
    .bar{display:flex;gap:10px;align-items:center;flex-shrink:0;flex-wrap:wrap}
    button{padding:5px 14px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);color:var(--text);cursor:pointer;font-size:13px}
    button:hover{background:var(--accent-bg)}
    .kv{font-size:13px;color:var(--text);font-variant-numeric:tabular-nums}
    .lbl{color:var(--muted);font-size:12px}
    canvas{flex:1;min-height:0;width:100%;display:block;background:var(--surface);border-radius:4px}
  </style>
  <div class="wrap">
    <div class="bar">
      <button id="btn">▶ Play</button>
      <span><span class="lbl">cos θ = </span><span class="kv" id="cv2">1.000</span></span>
      <span><span class="lbl">sin θ = </span><span class="kv" id="sv">0.000</span></span>
      <span><span class="lbl">θ = </span><span class="kv" id="tv">0.000</span><span class="lbl"> rad</span></span>
    </div>
    <canvas id="cn"></canvas>
  </div>
\`
const cn = app.querySelector('#cn')
cn.width  = cn.clientWidth  || 440
cn.height = cn.clientHeight || 280
const ctx = cn.getContext('2d')
const W=cn.width, H=cn.height, cx=W/2, cy=H/2
const R = Math.min(W,H)*0.38
const CC='#0ea5e9', SC='#f59e0b'   // cos color, sin color

function getC() {
  const s=getComputedStyle(document.documentElement), g=v=>s.getPropertyValue(v).trim()||'#888'
  return { text:g('--text'), muted:g('--muted'), accent:g('--accent'), border:g('--border') }
}

let animId=null, running=false, theta=0, lastTs=null

function drawFrame(th) {
  const C=getC(), cosT=Math.cos(th), sinT=Math.sin(th)
  const px=cx+cosT*R, py=cy-sinT*R
  ctx.clearRect(0,0,W,H)
  ctx.strokeStyle=C.muted; ctx.lineWidth=1
  ctx.beginPath(); ctx.moveTo(0,cy); ctx.lineTo(W,cy); ctx.stroke()
  ctx.beginPath(); ctx.moveTo(cx,0); ctx.lineTo(cx,H); ctx.stroke()
  ctx.strokeStyle=C.border; ctx.lineWidth=2
  ctx.beginPath(); ctx.arc(cx,cy,R,0,2*Math.PI); ctx.stroke()
  ctx.setLineDash([5,4]); ctx.lineWidth=1.5
  ctx.strokeStyle=CC; ctx.beginPath(); ctx.moveTo(px,py); ctx.lineTo(px,cy); ctx.stroke()
  ctx.strokeStyle=SC; ctx.beginPath(); ctx.moveTo(px,py); ctx.lineTo(cx,py); ctx.stroke()
  ctx.setLineDash([])
  ctx.fillStyle=CC; ctx.beginPath(); ctx.arc(px,cy,5,0,2*Math.PI); ctx.fill()
  ctx.fillStyle=SC; ctx.beginPath(); ctx.arc(cx,py,5,0,2*Math.PI); ctx.fill()
  ctx.strokeStyle=C.accent; ctx.lineWidth=2
  ctx.beginPath(); ctx.moveTo(cx,cy); ctx.lineTo(px,py); ctx.stroke()
  ctx.fillStyle=C.accent; ctx.beginPath(); ctx.arc(px,py,6,0,2*Math.PI); ctx.fill()
  ctx.font='bold 11px system-ui'; ctx.fillStyle=CC; ctx.textAlign='center'
  ctx.fillText('cos θ', px, cy+16)
  ctx.fillStyle=SC; ctx.textAlign='right'; ctx.fillText('sin θ', cx-6, py+4)
  app.querySelector('#cv2').textContent=cosT.toFixed(3)
  app.querySelector('#sv').textContent=sinT.toFixed(3)
  app.querySelector('#tv').textContent=th.toFixed(3)
}

function loop(ts) {
  const dt=lastTs?Math.min((ts-lastTs)/1000,0.05):0
  lastTs=ts; theta=(theta+dt)%(2*Math.PI)
  drawFrame(theta); animId=requestAnimationFrame(loop)
}
drawFrame(0)

app.querySelector('#btn').addEventListener('click', ()=>{
  if (running) {
    cancelAnimationFrame(animId); animId=null; running=false; lastTs=null
    app.querySelector('#btn').textContent='▶ Play'
  } else {
    running=true; app.querySelector('#btn').textContent='⏸ Pause'
    animId=requestAnimationFrame(loop)
  }
})`,
            },
            {
              id: 4,
              mode: 'html',
              cellTitle: 'Interactive Angle Slider',
              prose: [
                'Add a 0°–360° slider for manual control. Dragging the slider pauses the animation; pressing Play resumes from wherever the slider is.',
                'Try dragging to 90° (sin = 1), 180° (cos = −1), 270° (sin = −1). Every quadrant identity becomes a visual fact.',
              ],
              code: `app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:6px;overflow:hidden}
    .row{display:flex;gap:8px;align-items:center;flex-shrink:0;flex-wrap:wrap}
    button{padding:5px 14px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);color:var(--text);cursor:pointer;font-size:13px}
    button:hover{background:var(--accent-bg)}
    input[type=range]{flex:1;min-width:100px;max-width:180px;accent-color:var(--accent)}
    .val{font-size:13px;color:var(--text);font-variant-numeric:tabular-nums}
    canvas{flex:1;min-height:0;width:100%;display:block;background:var(--surface);border-radius:4px}
  </style>
  <div class="wrap">
    <div class="row">
      <button id="btn">▶ Play</button>
      <input type="range" id="sl" min="0" max="360" value="0" step="1">
      <span class="val" id="out">0°  cos=1.000  sin=0.000</span>
    </div>
    <canvas id="cn"></canvas>
  </div>
\`
const cn=app.querySelector('#cn')
cn.width=cn.clientWidth||440; cn.height=cn.clientHeight||280
const ctx=cn.getContext('2d')
const W=cn.width, H=cn.height, cx=W/2, cy=H/2
const R=Math.min(W,H)*0.38, sl=app.querySelector('#sl')
const CC='#0ea5e9', SC='#f59e0b'

function getC() {
  const s=getComputedStyle(document.documentElement),g=v=>s.getPropertyValue(v).trim()||'#888'
  return {text:g('--text'),muted:g('--muted'),accent:g('--accent'),border:g('--border')}
}

let animId=null, running=false, deg=0, lastTs=null

function draw(d) {
  const C=getC(), rad=d*Math.PI/180
  const cosT=Math.cos(rad), sinT=Math.sin(rad)
  const px=cx+cosT*R, py=cy-sinT*R
  ctx.clearRect(0,0,W,H)
  ctx.strokeStyle=C.muted; ctx.lineWidth=1
  ctx.beginPath(); ctx.moveTo(0,cy); ctx.lineTo(W,cy); ctx.stroke()
  ctx.beginPath(); ctx.moveTo(cx,0); ctx.lineTo(cx,H); ctx.stroke()
  ctx.strokeStyle=C.border; ctx.lineWidth=2
  ctx.beginPath(); ctx.arc(cx,cy,R,0,2*Math.PI); ctx.stroke()
  ctx.strokeStyle=C.accent+'66'; ctx.lineWidth=2
  ctx.beginPath(); ctx.arc(cx,cy,R*0.22,0,-rad,rad<0); ctx.stroke()
  ctx.setLineDash([5,4]); ctx.lineWidth=1.5
  ctx.strokeStyle=CC; ctx.beginPath(); ctx.moveTo(px,py); ctx.lineTo(px,cy); ctx.stroke()
  ctx.strokeStyle=SC; ctx.beginPath(); ctx.moveTo(px,py); ctx.lineTo(cx,py); ctx.stroke()
  ctx.setLineDash([])
  ctx.fillStyle=CC; ctx.beginPath(); ctx.arc(px,cy,5,0,2*Math.PI); ctx.fill()
  ctx.fillStyle=SC; ctx.beginPath(); ctx.arc(cx,py,5,0,2*Math.PI); ctx.fill()
  ctx.strokeStyle=C.accent; ctx.lineWidth=2
  ctx.beginPath(); ctx.moveTo(cx,cy); ctx.lineTo(px,py); ctx.stroke()
  ctx.fillStyle=C.accent; ctx.beginPath(); ctx.arc(px,py,6,0,2*Math.PI); ctx.fill()
  app.querySelector('#out').textContent=
    Math.round(d)+'°   cos='+cosT.toFixed(3)+'   sin='+sinT.toFixed(3)
}

function loop(ts) {
  const dt=lastTs?Math.min((ts-lastTs)/1000,0.05):0
  lastTs=ts; deg=(deg+dt*57.296)%360
  sl.value=deg; draw(deg); animId=requestAnimationFrame(loop)
}
draw(0)

sl.addEventListener('input', ()=>{
  if(running){cancelAnimationFrame(animId);animId=null;running=false;lastTs=null}
  deg=parseFloat(sl.value); draw(deg)
  app.querySelector('#btn').textContent='▶ Play'
})
app.querySelector('#btn').addEventListener('click', ()=>{
  if(running){
    cancelAnimationFrame(animId);animId=null;running=false;lastTs=null
    app.querySelector('#btn').textContent='▶ Play'
  } else {
    running=true; app.querySelector('#btn').textContent='⏸ Pause'
    animId=requestAnimationFrame(loop)
  }
})`,
            },

            // ── Challenges ──────────────────────────────────────────────────────
            {
              id: 'c1',
              isChallenge: true,
              mode: 'html',
              challengeTitle: 'Double-Speed Orbit',
              difficulty: 'easy',
              prompt: 'Change the angular frequency so the point completes two full revolutions per second instead of one. Only one constant needs to change.',
              hint: 'Angular frequency ω multiplies dt. Currently ω = 1 rad/s ≈ one revolution per 6.28 s. One revolution per second = ω = 2π ≈ 6.283. Two revolutions per second = ω = 4π ≈ 12.566.',
              code: `const OMEGA = 1   // TODO: change this. 1 rad/s ≈ 1 rev per 6.28 s. 2π ≈ 1 rev/s. 4π ≈ 2 rev/s.

app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:8px;overflow:hidden}
    .bar{display:flex;gap:8px;align-items:center;flex-shrink:0}
    button{padding:5px 14px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);color:var(--text);cursor:pointer;font-size:13px}
    button:hover{background:var(--accent-bg)}
    span{font-size:12px;color:var(--muted)}
    canvas{flex:1;min-height:0;width:100%;display:block;background:var(--surface);border-radius:4px}
  </style>
  <div class="wrap">
    <div class="bar"><button id="btn">▶ Play</button><span id="out">ω = \${OMEGA} rad/s</span></div>
    <canvas id="cn"></canvas>
  </div>
\`
const cn=app.querySelector('#cn')
cn.width=cn.clientWidth||440; cn.height=cn.clientHeight||280
const ctx=cn.getContext('2d')
const W=cn.width,H=cn.height,cx=W/2,cy=H/2,R=Math.min(W,H)*0.38
function getC(){const s=getComputedStyle(document.documentElement),g=v=>s.getPropertyValue(v).trim()||'#888';return{muted:g('--muted'),accent:g('--accent'),border:g('--border')}}
let animId=null,running=false,theta=0,lastTs=null
function draw(){
  const C=getC(),cosT=Math.cos(theta),sinT=Math.sin(theta),px=cx+cosT*R,py=cy-sinT*R
  ctx.clearRect(0,0,W,H)
  ctx.strokeStyle=C.muted;ctx.lineWidth=1
  ctx.beginPath();ctx.moveTo(0,cy);ctx.lineTo(W,cy);ctx.stroke()
  ctx.beginPath();ctx.moveTo(cx,0);ctx.lineTo(cx,H);ctx.stroke()
  ctx.strokeStyle=C.border;ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,R,0,2*Math.PI);ctx.stroke()
  ctx.strokeStyle=C.accent;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,py);ctx.stroke()
  ctx.fillStyle=C.accent;ctx.beginPath();ctx.arc(px,py,6,0,2*Math.PI);ctx.fill()
}
function loop(ts){
  const dt=lastTs?Math.min((ts-lastTs)/1000,0.05):0
  lastTs=ts; theta=(theta+dt*OMEGA)%(2*Math.PI)   // ← OMEGA scales the speed
  draw(); animId=requestAnimationFrame(loop)
}
draw()
app.querySelector('#btn').addEventListener('click',()=>{
  if(running){cancelAnimationFrame(animId);animId=null;running=false;lastTs=null;app.querySelector('#btn').textContent='▶ Play'}
  else{running=true;app.querySelector('#btn').textContent='⏸ Pause';animId=requestAnimationFrame(loop)}
})`,
            },
            {
              id: 'c2',
              isChallenge: true,
              mode: 'html',
              challengeTitle: 'Orbital Trail',
              difficulty: 'medium',
              prompt: 'Store the last 60 canvas positions of the moving point in a ring buffer. Draw each stored position as a fading dot — older positions are more transparent — creating a comet-like trail.',
              hint: 'Push `{px, py}` to a `trail` array each frame. If `trail.length > TRAIL_LEN`, call `trail.shift()` to drop the oldest entry. When drawing trail dots, set `ctx.globalAlpha = (i + 1) / trail.length` so earlier dots are faint. Reset `ctx.globalAlpha = 1` after the loop.',
              code: `app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:8px;overflow:hidden}
    .bar{display:flex;gap:8px;align-items:center;flex-shrink:0}
    button{padding:5px 14px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);color:var(--text);cursor:pointer;font-size:13px}
    canvas{flex:1;min-height:0;width:100%;display:block;background:var(--surface);border-radius:4px}
  </style>
  <div class="wrap">
    <div class="bar"><button id="btn">▶ Play</button></div>
    <canvas id="cn"></canvas>
  </div>
\`
const cn=app.querySelector('#cn')
cn.width=cn.clientWidth||440; cn.height=cn.clientHeight||280
const ctx=cn.getContext('2d')
const W=cn.width,H=cn.height,cx=W/2,cy=H/2,R=Math.min(W,H)*0.38
const TRAIL_LEN=60
function getC(){const s=getComputedStyle(document.documentElement),g=v=>s.getPropertyValue(v).trim()||'#888';return{muted:g('--muted'),accent:g('--accent'),border:g('--border')}}
let animId=null,running=false,theta=0,lastTs=null
const trail=[]

function loop(ts){
  const dt=lastTs?Math.min((ts-lastTs)/1000,0.05):0
  lastTs=ts; theta=(theta+dt)%(2*Math.PI)
  const cosT=Math.cos(theta),sinT=Math.sin(theta),px=cx+cosT*R,py=cy-sinT*R

  // TODO: push {px, py} onto trail
  // TODO: if trail.length > TRAIL_LEN, remove the oldest entry with trail.shift()

  const C=getC()
  ctx.clearRect(0,0,W,H)
  ctx.strokeStyle=C.muted;ctx.lineWidth=1
  ctx.beginPath();ctx.moveTo(0,cy);ctx.lineTo(W,cy);ctx.stroke()
  ctx.beginPath();ctx.moveTo(cx,0);ctx.lineTo(cx,H);ctx.stroke()
  ctx.strokeStyle=C.border;ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,R,0,2*Math.PI);ctx.stroke()

  // TODO: draw trail dots
  // loop over trail with index i, set ctx.globalAlpha = (i + 1) / trail.length
  // draw a small circle at trail[i].px, trail[i].py
  // after the loop: ctx.globalAlpha = 1

  ctx.strokeStyle=C.accent;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,py);ctx.stroke()
  ctx.fillStyle=C.accent;ctx.beginPath();ctx.arc(px,py,6,0,2*Math.PI);ctx.fill()
  animId=requestAnimationFrame(loop)
}
ctx.strokeStyle=getC().border;ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,R,0,2*Math.PI);ctx.stroke()
app.querySelector('#btn').addEventListener('click',()=>{
  if(running){cancelAnimationFrame(animId);animId=null;running=false;lastTs=null;app.querySelector('#btn').textContent='▶ Play'}
  else{running=true;app.querySelector('#btn').textContent='⏸ Pause';animId=requestAnimationFrame(loop)}
})`,
            },
            {
              id: 'c3',
              isChallenge: true,
              mode: 'html',
              challengeTitle: 'Linked Sin(t) Graph',
              difficulty: 'hard',
              prompt: 'Draw the unit circle on the left half of the canvas and a scrolling sin(t) graph on the right. As the point moves around the circle, new samples get appended to the right — proving the sine wave is the y-coordinate of circular motion recorded over time.',
              hint: 'Split the canvas: circle on left (cx = W/4), sin plot on right (x from W/2 to W). Push sinT to a `history` array each frame, shift when `history.length > W/2`. Plot history as a polyline: `x = W/2 + (i / history.length) * (W/2)`, `y = cy - history[i] * R`.',
              code: `app.innerHTML = \`
  <style>
    html,body{height:100%;margin:0}*{box-sizing:border-box;font-family:system-ui,sans-serif}
    body,#app{background:var(--bg)}
    .wrap{height:100%;padding:12px;display:flex;flex-direction:column;gap:8px;overflow:hidden}
    .bar{display:flex;gap:8px;align-items:center;flex-shrink:0}
    button{padding:5px 14px;border-radius:6px;border:1px solid var(--border);background:var(--surface2);color:var(--text);cursor:pointer;font-size:13px}
    canvas{flex:1;min-height:0;width:100%;display:block;background:var(--surface);border-radius:4px}
  </style>
  <div class="wrap">
    <div class="bar"><button id="btn">▶ Play</button></div>
    <canvas id="cn"></canvas>
  </div>
\`
const cn=app.querySelector('#cn')
cn.width=cn.clientWidth||440; cn.height=cn.clientHeight||280
const ctx=cn.getContext('2d')
const W=cn.width,H=cn.height
const cx=W/4,cy=H/2,R=Math.min(W/2,H)*0.38   // circle on left half
const PLOT_X=W/2,PLOT_W=W/2,MAX=Math.floor(PLOT_W)
const history=[]

function getC(){const s=getComputedStyle(document.documentElement),g=v=>s.getPropertyValue(v).trim()||'#888';return{muted:g('--muted'),accent:g('--accent'),border:g('--border')}}
let animId=null,running=false,theta=0,lastTs=null

function loop(ts){
  const dt=lastTs?Math.min((ts-lastTs)/1000,0.05):0
  lastTs=ts; theta=(theta+dt)%(2*Math.PI)
  const cosT=Math.cos(theta),sinT=Math.sin(theta),px=cx+cosT*R,py=cy-sinT*R

  // TODO: push sinT to history; if history.length > MAX, shift

  const C=getC()
  ctx.clearRect(0,0,W,H)

  // Divider
  ctx.strokeStyle=C.border;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(PLOT_X,0);ctx.lineTo(PLOT_X,H);ctx.stroke()

  // Left: unit circle
  ctx.strokeStyle=C.muted;ctx.lineWidth=1
  ctx.beginPath();ctx.moveTo(0,cy);ctx.lineTo(PLOT_X-2,cy);ctx.stroke()
  ctx.beginPath();ctx.moveTo(cx,0);ctx.lineTo(cx,H);ctx.stroke()
  ctx.strokeStyle=C.border;ctx.lineWidth=2;ctx.beginPath();ctx.arc(cx,cy,R,0,2*Math.PI);ctx.stroke()
  ctx.setLineDash([4,4]);ctx.strokeStyle='#f59e0b';ctx.lineWidth=1.5
  ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(cx,py);ctx.stroke()
  ctx.setLineDash([])
  ctx.strokeStyle=C.accent;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(px,py);ctx.stroke()
  ctx.fillStyle=C.accent;ctx.beginPath();ctx.arc(px,py,6,0,2*Math.PI);ctx.fill()
  // Guide line from circle to plot panel
  ctx.strokeStyle='#f59e0b55';ctx.lineWidth=1;ctx.setLineDash([4,4])
  ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(PLOT_X,py);ctx.stroke()
  ctx.setLineDash([])

  // Right: sin(t) history
  ctx.strokeStyle=C.muted;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(PLOT_X,cy);ctx.lineTo(W,cy);ctx.stroke()

  // TODO: draw history as a polyline on the right panel
  // x = PLOT_X + (i / history.length) * PLOT_W
  // y = cy - history[i] * R
  // Use ctx.beginPath(), moveTo for first point, lineTo for the rest, then ctx.stroke()

  animId=requestAnimationFrame(loop)
}
app.querySelector('#btn').addEventListener('click',()=>{
  if(running){cancelAnimationFrame(animId);animId=null;running=false;lastTs=null;app.querySelector('#btn').textContent='▶ Play'}
  else{running=true;app.querySelector('#btn').textContent='⏸ Pause';animId=requestAnimationFrame(loop)}
})`,
            },
          ],
        },
      },
    ],
  },

  quiz: [
    {
      id: 'q1',
      type: 'choice',
      text: 'In the canvas, y increases downward. To draw a unit circle point (cos(θ), sin(θ)) in the correct orientation, how do you convert the math y-coordinate to canvas pixels?',
      options: [
        'cy = centerY + sin(θ) × scale — same sign as math',
        'cy = centerY − sin(θ) × scale — the minus sign flips the canvas y-axis back to math convention. Without it, the circle would appear reflected vertically: the point that should be at the top (θ=π/2, sin=1) would appear at the bottom',
        'cy = centerY × sin(θ) — multiply rather than add',
      ],
      correct: 1,
    },
    {
      id: 'q2',
      type: 'choice',
      text: 'The lesson says "watch the animated dot\'s shadow on the y-axis and you have literally drawn a sine wave." What does this mean?',
      options: [
        'The dot casts a shadow that the canvas renders automatically',
        'As the dot moves around the unit circle, its y-coordinate (height) oscillates smoothly between −1 and +1. Plotting that y-coordinate against time produces the sine wave graph. The circular motion is the "cause" and the wave is its projection — they are the same motion viewed from different angles',
        'Sine waves are defined as the shadow of a circle on a flat wall',
      ],
      correct: 1,
    },
    {
      id: 'q3',
      type: 'choice',
      text: 'cos²(θ) + sin²(θ) = 1. Looking at the unit circle picture, why is this identity geometrically obvious?',
      options: [
        'It follows from the quadratic formula applied to the circle equation',
        '(cos(θ), sin(θ)) is a point on the circle of radius 1. By the Pythagorean theorem, the distance from the origin to that point is √(cos²+sin²). Since the radius is 1, the distance is 1, so cos²+sin² = 1. The algebra is a restatement of the distance formula',
        'It is not geometrically obvious; it requires calculus to prove',
      ],
      correct: 1,
    },
    {
      id: 'q4',
      type: 'choice',
      text: 'Constants like `const R = 150` are computed once outside the loop. What would go wrong if you computed them inside the rAF callback?',
      options: [
        'JavaScript would not allow const declarations inside functions',
        'They would be recomputed 60 times per second with identical results — pure wasted work. More importantly, if the computation involved reading a DOM property (like canvas.width), frequent reads trigger style recalculations. Computing once outside the loop is a simple optimization that adds zero code complexity',
        'Variables declared inside the callback are not visible to drawing functions',
      ],
      correct: 1,
    },
  ],
}

export default sim2_006
